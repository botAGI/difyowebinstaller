.DS_Store
# .claude/ — only settings.json tracked, rest ignored
.claude/*
!.claude/settings.json

# Python
__pycache__/
*.pyc
*.pyo

# Secrets / runtime
.env
*.env.enc
*.key
.age/

# Internal dev artifacts (kept locally, not in repo)
.planning/
/workflows/
CLAUDE.md
SPEC.md
references/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Editor
*.swp
*.swo
*~

# Installer-generated runtime files (MUST not conflict with git pull)
credentials.txt
RELEASE
docker/
logs/
.rollback/
.dify_initialized
.admin_password
.checkpoint_*
*.bak
*.tmp
