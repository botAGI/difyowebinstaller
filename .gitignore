.DS_Store
# .claude/ — fully local, not tracked
.claude/

# Python
__pycache__/
*.pyc
*.pyo

# Secrets / runtime
.env
*.env.enc
*.key
.age/

# Internal dev artifacts (kept locally, not in repo)
.planning/
/dify-workflows/
/workflows/
_data_staging/
_food_staging/
CLAUDE.md
SPEC.md
references/
plugins/
pipelines/
documentation/
benchmarks/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Editor
*.swp
*.swo
*~

# Installer-generated runtime files (MUST not conflict with git pull)
credentials.txt
RELEASE
docker/
logs/
.rollback/
.dify_initialized
.admin_password
.checkpoint_*
*.bak
*.tmp
.vscode/

# Allow test fixtures that have the same names as runtime-generated files
!tests/fixtures/creds/credentials.txt
!tests/fixtures/creds/.admin_password
!tests/fixtures/security_audit/credentials.txt
agmind-handoff-fixed.zip
