---
phase: 02-security-hardening-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/nginx.conf.template
  - lib/security.sh
autonomous: true
requirements: [SECV-05, SECV-07]

must_haves:
  truths:
    - "Fail2ban nginx jail is removed; only SSH jail remains"
    - "Login endpoint rate limited to 1 request per 10 seconds with burst=3"
    - "/v1/ and /files/ routes have rate limiting (zone=api burst=20 nodelay)"
    - "Existing /console/api and /api rate limits preserved"
  artifacts:
    - path: "lib/security.sh"
      provides: "Fail2ban with SSH jail only, nginx jail removed"
      contains: "[sshd]"
    - path: "templates/nginx.conf.template"
      provides: "Rate limiting on all API routes including /v1/ and /files/"
      contains: "limit_req zone=api"
  key_links:
    - from: "lib/security.sh"
      to: "fail2ban jail config"
      via: "configure_fail2ban() writes /etc/fail2ban/jail.d/agmind.conf"
      pattern: "\\[sshd\\]"
    - from: "templates/nginx.conf.template"
      to: "rate limit zones"
      via: "limit_req_zone directives at http level"
      pattern: "limit_req_zone.*zone=login.*rate=1r/10s"
---

<objective>
Harden nginx rate limiting and clean up broken fail2ban nginx jail.

Purpose: SECV-05 removes the broken fail2ban nginx jail (Docker logpath mismatch makes it useless) and replaces it with nginx-native rate limiting. SECV-07 extends rate limiting to previously unprotected /v1/ and /files/ routes and tightens the login endpoint rate.

Output: Updated nginx.conf.template with rate limits on all API routes; cleaned lib/security.sh with SSH-only fail2ban.
</objective>

<execution_context>
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/workflows/execute-plan.md
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening-v2/02-CONTEXT.md

<interfaces>
<!-- Current nginx rate limit zones (templates/nginx.conf.template lines 37-40) -->
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=3r/s;
limit_conn_zone $binary_remote_addr zone=ws:10m;

<!-- Current rate limit usage in location blocks -->
location /console/api { limit_req zone=api burst=20 nodelay; ... }
location /api { limit_req zone=api burst=20 nodelay; ... }
location /v1 { /* NO rate limiting currently */ }
location /files { /* NO rate limiting currently */ }

<!-- lib/security.sh:configure_fail2ban() currently creates both [sshd] and [agmind-nginx] jails -->
<!-- lib/security.sh also creates /etc/fail2ban/filter.d/agmind-nginx.conf filter file -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend nginx rate limiting to /v1/ and /files/, tighten login rate</name>
  <read_first>
    - templates/nginx.conf.template
    - .planning/phases/02-security-hardening-v2/02-CONTEXT.md
  </read_first>
  <files>templates/nginx.conf.template</files>
  <action>
Modify `templates/nginx.conf.template`:

1. Change the login rate limit zone definition (line 39) from:
   `limit_req_zone $binary_remote_addr zone=login:10m rate=3r/s;`
   to:
   `limit_req_zone $binary_remote_addr zone=login:10m rate=1r/10s;`

2. Add a dedicated login location block inside the Dify Console server (port 3000), BEFORE the existing `/console/api` location block (before line 158). This new block must:
   - Match `location /console/api/login`
   - Apply `limit_req zone=login burst=3 nodelay;`
   - Proxy to `http://dify_api` with same headers as /console/api block
   - Include `proxy_http_version 1.1`, Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto headers
   - Set `proxy_read_timeout 3600s; proxy_send_timeout 3600s;`

3. Add rate limiting to the `/v1` location block (currently around line 184):
   Add `limit_req zone=api burst=20 nodelay;` as the first directive inside the location block, right after the `location /v1 {` line.

4. Add rate limiting to the `/files` location block (currently around line 196):
   Add `limit_req zone=api burst=20 nodelay;` as the first directive inside the location block, right after the `location /files {` line.

5. Do NOT modify the WebSocket location, the Open WebUI location, or the TLS server blocks. Leave `limit_conn_zone $binary_remote_addr zone=ws:10m;` unchanged.
  </action>
  <verify>
    <automated>grep -c "limit_req zone=" templates/nginx.conf.template | grep -q "^5$" && grep "zone=login.*rate=1r/10s" templates/nginx.conf.template && grep -A1 "location /v1" templates/nginx.conf.template | grep -q "limit_req" && grep -A1 "location /files" templates/nginx.conf.template | grep -q "limit_req" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - templates/nginx.conf.template contains `zone=login:10m rate=1r/10s`
    - templates/nginx.conf.template contains `location /console/api/login` block with `limit_req zone=login burst=3 nodelay`
    - `/v1` location block contains `limit_req zone=api burst=20 nodelay`
    - `/files` location block contains `limit_req zone=api burst=20 nodelay`
    - `/console/api` and `/api` location blocks still have `limit_req zone=api burst=20 nodelay` (unchanged)
    - Total `limit_req zone=` directives in template: exactly 5 (console/api/login, console/api, api, v1, files)
    - `bash -n install.sh` still passes (template is not bash, but verify installer syntax is not broken)
  </acceptance_criteria>
  <done>All Dify API routes (/console/api/login, /console/api, /api, /v1, /files) have nginx rate limiting. Login is at 1r/10s with burst=3, others at 10r/s with burst=20.</done>
</task>

<task type="auto">
  <name>Task 2: Remove fail2ban nginx jail, keep SSH jail only</name>
  <read_first>
    - lib/security.sh
    - .planning/phases/02-security-hardening-v2/02-CONTEXT.md
  </read_first>
  <files>lib/security.sh</files>
  <action>
Modify `lib/security.sh:configure_fail2ban()` function:

1. Remove the "Create AGMind nginx filter" block entirely — delete the `cat > /etc/fail2ban/filter.d/agmind-nginx.conf << 'EOF'` heredoc (lines 63-67 approximately). This filter is broken because Docker runs nginx inside a container and the logpath `/opt/agmind/docker/volumes/nginx/logs/access.log` does not exist on the host.

2. In the jail config heredoc (`cat > /etc/fail2ban/jail.d/agmind.conf`), remove the `[agmind-nginx]` section entirely (lines 77-83). Keep ONLY the `[sshd]` section:
   ```
   [sshd]
   enabled = true
   maxretry = 3
   bantime = 864000
   findtime = 600
   ```

3. Do NOT change any other function in the file (configure_ufw, encrypt_secrets, harden_docker_compose, setup_security).

4. Do NOT change the function signature or the `if [[ "${ENABLE_FAIL2BAN:-false}" != "true" ]]` guard.

The result: `configure_fail2ban()` installs fail2ban if missing, creates SSH jail only, enables and restarts fail2ban. No nginx-related config.
  </action>
  <verify>
    <automated>grep -c "agmind-nginx" lib/security.sh | grep -q "^0$" && grep -c "\[sshd\]" lib/security.sh | grep -q "^1$" && grep -c "filter.d" lib/security.sh | grep -q "^0$" && bash -n lib/security.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "agmind-nginx" lib/security.sh` returns 0
    - `grep -c "filter.d" lib/security.sh` returns 0
    - `grep -c "\[sshd\]" lib/security.sh` returns 1
    - lib/security.sh contains `maxretry = 3` and `bantime = 864000` in [sshd] section
    - `bash -n lib/security.sh` exits 0
    - `configure_fail2ban()` function still exists with ENABLE_FAIL2BAN guard
    - `setup_security()` function still calls `configure_fail2ban`
  </acceptance_criteria>
  <done>Fail2ban nginx jail removed. SSH jail preserved. Nginx rate limiting (Task 1) replaces fail2ban for HTTP brute-force protection.</done>
</task>

</tasks>

<verification>
- `grep "rate=1r/10s" templates/nginx.conf.template` matches the login zone
- `grep -c "limit_req zone=" templates/nginx.conf.template` returns 5
- `grep -c "agmind-nginx" lib/security.sh` returns 0
- `grep "\[sshd\]" lib/security.sh` matches
- `bash -n install.sh` passes
- `bats tests/` passes (existing tests unbroken)
</verification>

<success_criteria>
- Login endpoint limited to 1 attempt per 10 seconds (burst=3)
- All API routes (/console/api, /api, /v1, /files) rate limited at 10r/s
- Fail2ban only manages SSH jail — no broken nginx jail
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening-v2/02-01-SUMMARY.md`
</output>
