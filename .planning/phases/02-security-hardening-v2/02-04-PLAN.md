---
phase: 02-security-hardening-v2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SECV-02
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md SECV-02 text accurately describes the implemented behavior: Authelia 2FA on /console/* only, API routes bypass Authelia"
    - "ROADMAP.md Phase 2 deliverables text matches the actual Authelia implementation"
    - "No planning document claims 2FA covers all Dify routes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate SECV-02 requirement text"
      contains: "bypass Authelia"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate Phase 2 deliverable description for Authelia"
      contains: "Authelia 2FA on /console/*"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "templates/authelia/configuration.yml.template"
      via: "documentation accuracy"
      pattern: "bypass Authelia"
---

<objective>
Fix documentation drift in REQUIREMENTS.md and ROADMAP.md where the SECV-02 text still claims Authelia 2FA covers all Dify routes, contradicting the intentional design pivot (approved in 02-CONTEXT.md) where API routes bypass Authelia and are protected by Dify API key auth + nginx rate limiting instead.

Purpose: Prevent future developers from misinterpreting the Authelia bypass as a bug. Align planning documents with the accepted design decision and actual implementation.
Output: Updated REQUIREMENTS.md and ROADMAP.md with accurate SECV-02 / Authelia descriptions.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening-v2/02-CONTEXT.md
@.planning/phases/02-security-hardening-v2/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SECV-02 text in REQUIREMENTS.md and ROADMAP.md to reflect Authelia bypass design</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/02-security-hardening-v2/02-CONTEXT.md, .planning/phases/02-security-hardening-v2/02-VERIFICATION.md</read_first>
  <action>
Update two files to fix the documentation drift identified in 02-VERIFICATION.md:

**File 1: `.planning/REQUIREMENTS.md`**

Find the SECV-02 line (line ~21). Replace the entire line with:

```
- [x] **SECV-02**: Authelia 2FA on /console/* (human login). API routes (/api/, /v1/, /files/) bypass Authelia — protected by Dify API key auth + nginx rate limiting (10r/s).
```

This matches the CONTEXT.md directive: "Update SECV-02 text: 'Authelia 2FA on /console/*. API routes use Dify's own API key auth + nginx rate limiting.'" with the addition of the specific rate (10r/s) for precision.

Also update the `Last updated` line at the bottom of REQUIREMENTS.md to: `*Last updated: 2026-03-18 after Phase 2 gap closure — SECV-02 text aligned with Authelia bypass design*`

**File 2: `.planning/ROADMAP.md`**

Find the Phase 2 Key deliverables bullet about Authelia (line ~53). It currently reads:

```
- Authelia covers /console/api/, /api/, /v1/, /files/
```

Replace with:

```
- Authelia 2FA on /console/* (human login); API routes bypass Authelia (Dify API key auth + rate limiting)
```

Also update the Phase 2 Plans line to show 3/3 complete (since 02-03 is also done):

```
**Plans:** 3 plans
```

And update the plan list to show all three plans checked plus the gap closure plan:

```
Plans:
- [x] 02-01-PLAN.md — Nginx rate limiting extension + fail2ban nginx jail removal (SECV-05, SECV-07)
- [x] 02-02-PLAN.md — Wizard admin-UI opt-in, credential suppression, Squid ACL, Authelia policy (SECV-01, SECV-02, SECV-03, SECV-04)
- [x] 02-03-PLAN.md — Backup/restore fixes + BATS test (SECV-06)
- [ ] 02-04-PLAN.md — Gap closure: SECV-02 documentation drift fix (SECV-02)
```

Update the `Last updated` line at the bottom of ROADMAP.md to: `*Last updated: 2026-03-18 after Phase 2 gap closure plan — SECV-02 documentation fix*`

Do NOT change any other content in either file.
  </action>
  <verify>
    <automated>cd "D:/Agmind/difyowebinstaller" && grep "SECV-02" .planning/REQUIREMENTS.md | grep -q "bypass Authelia" && grep "Authelia" .planning/ROADMAP.md | grep -q "bypass Authelia" && echo "PASS: Both files updated" || echo "FAIL: Text not updated"</automated>
  </verify>
  <acceptance_criteria>
    - grep "SECV-02" .planning/REQUIREMENTS.md returns line containing "bypass Authelia" and "10r/s"
    - grep "SECV-02" .planning/REQUIREMENTS.md does NOT return "covers all Dify routes"
    - grep "Authelia" .planning/ROADMAP.md returns line containing "bypass Authelia" within Phase 2 deliverables
    - grep "Authelia" .planning/ROADMAP.md does NOT return "covers /console/api/, /api/, /v1/, /files/"
    - grep "02-04-PLAN" .planning/ROADMAP.md returns the gap closure plan entry
    - No other lines in either file are changed beyond the targeted updates and timestamps
  </acceptance_criteria>
  <done>REQUIREMENTS.md SECV-02 reads "Authelia 2FA on /console/* (human login). API routes (/api/, /v1/, /files/) bypass Authelia — protected by Dify API key auth + nginx rate limiting (10r/s)." and ROADMAP.md Phase 2 Authelia deliverable reads "Authelia 2FA on /console/* (human login); API routes bypass Authelia (Dify API key auth + rate limiting)". Documentation now matches the implemented design.</done>
</task>

</tasks>

<verification>
After task completion, verify:
1. `grep "SECV-02" .planning/REQUIREMENTS.md` — must contain "bypass Authelia" and "10r/s"
2. `grep -c "covers all Dify routes" .planning/REQUIREMENTS.md` — must return 0
3. `grep "Authelia" .planning/ROADMAP.md` — Phase 2 deliverable must say "bypass Authelia"
4. `grep -c "/console/api/, /api/, /v1/, /files/" .planning/ROADMAP.md` — must return 0
5. `grep "02-04" .planning/ROADMAP.md` — gap closure plan listed
</verification>

<success_criteria>
- REQUIREMENTS.md SECV-02 text accurately describes the Authelia bypass design decision
- ROADMAP.md Phase 2 deliverables reflect the actual Authelia implementation
- No planning document claims Authelia 2FA covers all Dify routes
- Phase 2 plan list in ROADMAP.md shows all 4 plans (3 original + 1 gap closure)
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening-v2/02-04-SUMMARY.md`
</output>
