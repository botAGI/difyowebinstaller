---
phase: 01-surgery-remove-dify-api-automation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - workflows/README.md
autonomous: true
requirements: [SURG-04]

must_haves:
  truths:
    - "workflows/README.md explains how to import rag-assistant.json into Dify UI"
    - "workflows/README.md lists required plugins per provider"
    - "workflows/README.md includes manual pipeline reconnect instructions"
    - "rag-assistant.json is preserved unchanged"
  artifacts:
    - path: "workflows/README.md"
      provides: "User guide for importing RAG workflow into Dify"
      contains: "DSL Import"
    - path: "workflows/rag-assistant.json"
      provides: "RAG assistant workflow template"
  key_links:
    - from: "workflows/README.md"
      to: "workflows/rag-assistant.json"
      via: "references the JSON file by name"
      pattern: "rag-assistant.json"
---

<objective>
Create workflows/README.md documenting how to manually import the RAG assistant workflow into Dify and reconnect Open WebUI via pipeline.

Purpose: With import.py deleted, users need clear instructions on how to import the workflow template, install required plugins, and optionally reconnect Open WebUI to Dify via the pipeline bridge.

Output: A comprehensive README.md alongside rag-assistant.json.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-surgery-remove-dify-api-automation/01-CONTEXT.md
@.planning/phases/01-surgery-remove-dify-api-automation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows/README.md with import and plugin instructions</name>
  <files>workflows/README.md</files>
  <read_first>workflows/rag-assistant.json, .planning/phases/01-surgery-remove-dify-api-automation/01-CONTEXT.md</read_first>
  <action>
Create `workflows/README.md` with the following content structure and sections:

```markdown
# RAG Assistant Workflow

Pre-built workflow template for Dify that provides a Retrieval-Augmented Generation assistant.

## What This Does

The `rag-assistant.json` file is a Dify DSL (Domain Specific Language) workflow that:
- Accepts user questions via chat interface
- Retrieves relevant context from a Knowledge Base
- Generates answers using your configured LLM
- Supports conversation history

## How to Import

1. Open Dify Console in your browser (default: `http://<your-ip>:3000`)
2. On first visit, set up your admin account using the init password shown during installation
3. Go to **Studio** (top nav)
4. Click **Create from DSL** (or **Import DSL** in the app list)
5. Upload `rag-assistant.json` from `/opt/agmind/workflows/`
6. The workflow will appear in your app list

## Required Plugins

After importing, install the plugins your provider needs. Go to **Plugins** in Dify and search for:

### Ollama (default)
- `langgenius/ollama` — Ollama model provider

### vLLM / External OpenAI-compatible API
- `langgenius/openai_api_compatible` — OpenAI-compatible model provider

### Enhanced ETL (if enabled during install)
- `s20ss/docling` — Advanced document parsing

## Post-Import Configuration

After importing the workflow, configure these nodes:

1. **LLM Node** — Select your model (e.g., `qwen2.5:14b` for Ollama)
2. **Knowledge Base Node** — Create a Knowledge Base first, then select it in this node
3. **Embedding Node** — Select your embedding model (e.g., `bge-m3` for Ollama)

### Setting Up Model Providers

1. Go to **Settings > Model Providers**
2. Add your provider:
   - **Ollama**: Base URL = `http://ollama:11434`
   - **vLLM**: Base URL = `http://vllm:8000/v1`
   - **External API**: Enter your provider's URL and API key

## Connecting Open WebUI to Dify (Optional)

By default, Open WebUI connects directly to Ollama for chat. If you want Open WebUI
to use Dify workflows (for RAG, agents, etc.), you need to set up the pipeline bridge:

1. Create an app in Dify UI and publish it
2. Go to **API Access** in your app and copy the **Service API Key**
3. Edit `/opt/agmind/docker/.env`:
   ```
   DIFY_API_KEY=app-xxxxxxxxxxxxxxxx
   ```
4. Rebuild and restart the pipeline service:
   ```bash
   cd /opt/agmind/docker
   docker compose up -d --build pipeline
   docker compose restart open-webui
   ```

> **Note:** The pipeline service container is not started by default.
> It requires a valid DIFY_API_KEY to function.

## Files

- `rag-assistant.json` — Dify DSL workflow template (do not edit unless customizing)
- `README.md` — This file
```

Ensure the file is saved at `workflows/README.md` (alongside `rag-assistant.json`).
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && test -f workflows/README.md && grep -c "DSL" workflows/README.md && grep -c "langgenius/ollama" workflows/README.md && grep -c "langgenius/openai_api_compatible" workflows/README.md && grep -c "s20ss/docling" workflows/README.md && grep -c "DIFY_API_KEY" workflows/README.md && grep -c "rag-assistant.json" workflows/README.md; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `workflows/README.md` exists
    - `grep 'DSL Import\|Import DSL\|Create from DSL' workflows/README.md` matches (import instructions)
    - `grep 'langgenius/ollama' workflows/README.md` matches (Ollama plugin)
    - `grep 'langgenius/openai_api_compatible' workflows/README.md` matches (vLLM/external plugin)
    - `grep 's20ss/docling' workflows/README.md` matches (ETL plugin)
    - `grep 'DIFY_API_KEY=app-' workflows/README.md` matches (pipeline reconnect instructions)
    - `grep 'rag-assistant.json' workflows/README.md` matches (references the template file)
    - `grep 'http://ollama:11434' workflows/README.md` matches (Ollama base URL)
    - `grep 'Knowledge Base' workflows/README.md` matches (post-import config)
    - File `workflows/rag-assistant.json` still exists (preserved, not modified)
  </acceptance_criteria>
  <done>workflows/README.md exists with complete instructions for importing RAG workflow, installing plugins per provider, configuring model providers, and optionally reconnecting Open WebUI via pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Final sweep — verify no stale references remain across entire codebase</name>
  <files>install.sh</files>
  <read_first>install.sh</read_first>
  <action>
Run a comprehensive grep across the codebase for any remaining references to removed code.

Search for these patterns in all `.sh` files, `.template` files, and `.yml` files:
- `import\.py` (should only appear in git history, not in tracked files)
- `phase_workflow` (should be zero)
- `phase_connectivity` (should be zero)
- `setup_dify_account` (should be zero)
- `import_workflow` (should be zero — was in lib/workflow.sh)
- `setup_workflow` (should be zero — was in lib/workflow.sh)
- `build_difypkg_from_github` (should be zero)
- `ADMIN_EMAIL` in install.sh (should be zero)
- `ADMIN_PASSWORD` in install.sh (should be zero — note: lib/config.sh may still reference it internally for INIT_PASSWORD generation, that's OK)

For any remaining stale references found:
- In `install.sh`: remove the line or update it
- In `lib/*.sh`: remove or update
- In `templates/*`: remove or update
- In `scripts/multi-instance.sh`: this file has `DIFY_API_KEY=` and `COMPANY_NAME=${name}` — leave it alone, multi-instance is a separate tool

If `bash -n install.sh` fails after Plan 01 changes, fix any syntax errors (missing `fi`, unbalanced quotes, dangling `\` from removed sed lines, etc.).

Also verify that `grep -rn '/11\]' install.sh` returns zero matches — all phase labels should say `/9]`.
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && bash -n install.sh 2>&1 && echo "SYNTAX:OK" && grep -rn 'import\.py\|phase_workflow\|phase_connectivity\|setup_dify_account\|import_workflow\|setup_workflow\|build_difypkg' install.sh lib/config.sh lib/authelia.sh templates/docker-compose.yml templates/env.*.template 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0 — no syntax errors
    - `grep -rn 'import\.py' install.sh lib/ templates/` returns zero matches
    - `grep -rn 'phase_workflow\|phase_connectivity' install.sh` returns zero matches
    - `grep -rn 'setup_dify_account\|import_workflow\|setup_workflow' install.sh lib/` returns zero matches
    - `grep -rn 'build_difypkg' install.sh lib/ workflows/` returns zero matches
    - `grep -c '/11\]' install.sh` returns 0
    - `grep -c '/9\]' install.sh` returns 9
    - `grep -rn 'ADMIN_EMAIL' install.sh` returns 0 matches
    - `grep -rn 'DIFY_API_KEY' install.sh` returns 0 matches
  </acceptance_criteria>
  <done>Zero stale references to deleted code remain in any installer file. install.sh passes bash -n syntax check. All 9 phase headers are correctly numbered.</done>
</task>

</tasks>

<verification>
Full phase verification after both plans complete:
1. `bash -n install.sh` — syntax valid
2. `grep -rn 'import\.py\|DIFY_API_KEY\|ADMIN_EMAIL\|COMPANY_NAME\|phase_workflow\|phase_connectivity' install.sh lib/config.sh templates/docker-compose.yml templates/env.*.template` — zero matches
3. `ls workflows/import.py pipeline/ lib/workflow.sh 2>&1` — all "No such file"
4. `ls workflows/README.md workflows/rag-assistant.json` — both exist
5. `grep '/9\]' install.sh | wc -l` — exactly 9
6. `grep 'INIT_PASSWORD' install.sh` — matches in phase_complete
7. `grep 'langgenius/ollama' workflows/README.md` — matches
</verification>

<success_criteria>
- workflows/README.md exists with complete import instructions, plugin list, and pipeline reconnect guide
- Final sweep confirms zero stale references across codebase
- install.sh is syntactically valid
- rag-assistant.json preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-surgery-remove-dify-api-automation/01-02-SUMMARY.md`
</output>
