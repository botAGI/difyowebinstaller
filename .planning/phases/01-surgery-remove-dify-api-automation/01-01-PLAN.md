---
phase: 01-surgery-remove-dify-api-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - lib/workflow.sh
  - lib/config.sh
  - lib/authelia.sh
  - workflows/import.py
  - pipeline/Dockerfile
  - pipeline/dify_pipeline.py
  - pipeline/requirements.txt
  - templates/docker-compose.yml
  - templates/env.lan.template
  - templates/env.vps.template
  - templates/env.vpn.template
  - templates/env.offline.template
autonomous: true
requirements: [SURG-01, SURG-02, SURG-03, SURG-05]

must_haves:
  truths:
    - "install.sh runs with bash -n (no syntax errors) and has 9 phases, not 11"
    - "No references to import.py, phase_workflow, phase_connectivity, ADMIN_EMAIL prompt, ADMIN_PASSWORD prompt, or COMPANY_NAME prompt remain in install.sh"
    - "pipeline/ directory and workflows/import.py are deleted"
    - "lib/workflow.sh is deleted"
    - "docker-compose.yml template has no pipeline service"
    - "Open WebUI connects directly to Ollama (no pipeline proxy)"
    - "INIT_PASSWORD appears in credentials summary and credentials.txt"
    - "create_openwebui_admin uses INIT_PASSWORD, not ADMIN_EMAIL/ADMIN_PASSWORD"
  artifacts:
    - path: "install.sh"
      provides: "9-phase installer without Dify API automation"
      contains: "[9/9] complete"
    - path: "templates/docker-compose.yml"
      provides: "Compose without pipeline service"
  key_links:
    - from: "install.sh main()"
      to: "9 phase_* calls"
      via: "sequential function calls"
      pattern: "phase_complete.*9/9"
    - from: "templates/docker-compose.yml open-webui"
      to: "ollama"
      via: "OLLAMA_BASE_URL"
      pattern: "OLLAMA_BASE_URL.*ollama:11434"
---

<objective>
Delete all Dify API automation code and restructure install.sh from 11 to 9 phases.

Purpose: Remove import.py, pipeline proxy, and phase_workflow/phase_connectivity from the installer. Remove wizard fields ADMIN_EMAIL, ADMIN_PASSWORD, COMPANY_NAME. Simplify Open WebUI to connect directly to Ollama instead of through pipeline. Add INIT_PASSWORD to credentials output.

Output: A clean install.sh with 9 phases, no Dify API calls, no pipeline service, simplified wizard, and updated credentials summary.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-surgery-remove-dify-api-automation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete files and perform full install.sh surgery</name>
  <files>install.sh, workflows/import.py, pipeline/Dockerfile, pipeline/dify_pipeline.py, pipeline/requirements.txt, lib/workflow.sh</files>
  <read_first>install.sh, lib/workflow.sh, .planning/phases/01-surgery-remove-dify-api-automation/01-CONTEXT.md</read_first>
  <action>
**Step A — Delete files:**
- Delete `workflows/import.py` (entire file)
- Delete `pipeline/Dockerfile`, `pipeline/dify_pipeline.py`, `pipeline/requirements.txt`
- Delete the now-empty `pipeline/` directory
- Delete `lib/workflow.sh` (entire file)

**Step B — install.sh source lines (around line 62-73):**
- Remove: `source "${INSTALLER_DIR}/lib/workflow.sh"` (line 67)
- Remove: `source "${INSTALLER_DIR}/lib/tunnel.sh"` (line 68)
- Remove: `source "${INSTALLER_DIR}/lib/dokploy.sh"` (line 70)
- Keep `lib/tunnel.sh` and `lib/dokploy.sh` files on disk (for future Phase 5 reuse) — only remove their `source` lines

**Step C — Global state variables (around line 76-111):**
- Remove these variable declarations entirely:
  - `COMPANY_NAME=""` (line 79)
  - `ADMIN_EMAIL=""` (line 83)
  - `ADMIN_PASSWORD=""` (line 84)
  - `DOKPLOY_ENABLED=""` (line 92)
  - `TUNNEL_VPS_HOST=""` (line 93)
  - `TUNNEL_VPS_PORT="22"` (line 94)
  - `TUNNEL_REMOTE_PORT="8080"` (line 95)
  - `TUNNEL_LOCAL_PORT="80"` (line 96)
  - `TUNNEL_SSH_REMOTE_PORT="8022"` (line 97)

**Step D — Wizard (phase_wizard function, starts ~line 207):**
- Change header from `[2/11]` to `[2/9]`
- Remove the "Branding" section (lines ~259-272): the `COMPANY_NAME` prompt and `LOGO_PATH` prompt — both interactive and non-interactive branches
- Remove the "Admin" section (lines ~443-459): the `ADMIN_EMAIL` prompt and `ADMIN_PASSWORD` prompt — both interactive and non-interactive branches. Also remove the `validate_email` call for ADMIN_EMAIL.
- Remove the "Dokploy" section (lines ~629-645): the `DOKPLOY_ENABLED` prompt
- Remove the "Reverse SSH" section (lines ~647-671): the `TUNNEL_VPS_HOST` prompt
- In the wizard summary block (lines ~674-698), remove these echo lines:
  - `echo "  Компания:     ${COMPANY_NAME}"`
  - `echo "  Админ:        ${ADMIN_EMAIL}"`
  - `[[ -n "$DOKPLOY_ENABLED" ]] && echo "  Dokploy:      да"`
  - `[[ -n "$TUNNEL_VPS_HOST" ]] && echo "  Tunnel:       ${TUNNEL_VPS_HOST}:${TUNNEL_REMOTE_PORT}"`

**Step E — phase_config (starts ~line 714):**
- Change header from `[4/11]` to `[4/9]`
- In the export block (lines ~722-734), remove:
  - `export INSTALL_DIR ADMIN_EMAIL COMPANY_NAME` — change to just `export INSTALL_DIR`
  - (LLM_MODEL, EMBEDDING_MODEL, DOMAIN, CERTBOT_EMAIL, DEPLOY_PROFILE, VECTOR_STORE, ETL_ENHANCED remain)
- Remove line 758: `cp "${INSTALLER_DIR}/workflows/import.py" "${INSTALL_DIR}/workflows/"`

**Step F — phase_start (starts ~line 830):**
- Change header from `[5/11]` to `[5/9]`

**Step G — create_openwebui_admin (starts ~line 936):**
- Change `local admin_email="${ADMIN_EMAIL:-admin@admin.com}"` to use the INIT_PASSWORD from .env:
  ```
  local admin_email="admin@localhost"
  ```
  (Open WebUI admin is a local-only account; email is irrelevant since import.py is gone)
- Change `local admin_password="${ADMIN_PASSWORD:-}"` to auto-generate:
  ```
  local admin_password
  admin_password=$(grep '^INIT_PASSWORD=' "${INSTALL_DIR}/docker/.env" 2>/dev/null | cut -d'=' -f2- | base64 -d 2>/dev/null || head -c 256 /dev/urandom | LC_ALL=C tr -dc 'a-zA-Z0-9' | head -c 16)
  ```
  This reuses the INIT_PASSWORD (base64-decoded) as the Open WebUI admin password, falling back to random generation.
- Change `local admin_name="${COMPANY_NAME:-AGMind} Admin"` to:
  ```
  local admin_name="AGMind Admin"
  ```

**Step H — phase_health, phase_models, phase_backups:**
- `phase_health`: change `[6/11]` to `[6/9]`
- `phase_models`: change `[7/11]` to `[7/9]`
- `phase_backups`: change `[9/11]` to `[8/9]`

**Step I — Delete phase_workflow function (lines ~1147-1168):**
- Delete the entire `phase_workflow()` function

**Step J — Delete phase_connectivity function (lines ~1186-1201):**
- Delete the entire `phase_connectivity()` function

**Step K — phase_complete (starts ~line 1215):**
- Change to `[9/9]` label (from `[11/11]`)
- In the credentials summary block:
  - Remove `ADMIN_EMAIL` line: `summary+="$(printf '...' "${ADMIN_EMAIL}")"`
  - Remove `Admin pass` line: `summary+="$(printf '...' "${admin_pass:-N/A}")"`
  - Remove `Dify API key` lines (both the variable read and the summary line)
  - Remove the `dify_api_key` variable and its file read from `${INSTALL_DIR}/.dify_service_api_key`
  - Add INIT_PASSWORD to summary — read it from .env:
    ```
    local init_password=""
    if [[ -f "${INSTALL_DIR}/docker/.env" ]]; then
        init_password=$(grep '^INIT_PASSWORD=' "${INSTALL_DIR}/docker/.env" 2>/dev/null | cut -d'=' -f2-)
    fi
    ```
    Then add a summary line after the Dify Console URL:
    ```
    summary+="$(printf '║  Dify init pwd: %-*s║\n' $((W-18)) "${init_password:-N/A}")"
    ```
  - Remove `admin_pass` variable and its file read from `${INSTALL_DIR}/.admin_password`
  - Add Open WebUI password to summary. Read it from .admin_password (still written by create_openwebui_admin):
    ```
    local owui_pass=""
    if [[ -f "${INSTALL_DIR}/.admin_password" ]]; then
        owui_pass=$(cat "${INSTALL_DIR}/.admin_password" 2>/dev/null)
    fi
    ```
    Add summary line: `summary+="$(printf '║  WebUI pass:    %-*s║\n' $((W-18)) "${owui_pass:-N/A}")"` — placed after the Open WebUI URL line.
  - In the credentials.txt file write section:
    - Remove `echo "Admin email:   $ADMIN_EMAIL"`
    - Remove `echo "Admin pass:    ${admin_pass:-N/A}"`
    - Remove `[[ -n "$dify_api_key" ]] && echo "Dify API key:  $dify_api_key"`
    - Add `echo "Dify init pwd: ${init_password:-N/A}"`
    - Add `echo "WebUI pass:    ${owui_pass:-N/A}"`

**Step L — main() function (starts ~line 1358):**
- Remove CLI args: `--company`, `--company=*`, `--admin-email`, `--admin-email=*` (lines ~1375-1378)
- Remove from help text: the `--company` and `--admin-email` lines
- Update the phase call sequence in main() to 9 phases:
  ```
  phase_diagnostics  # [1/9]
  phase_wizard       # [2/9]
  phase_docker       # [3/9]
  phase_config       # [4/9]
  phase_start        # [5/9]
  phase_health       # [6/9]
  phase_models       # [7/9]
  phase_backups      # [8/9]
  phase_complete     # [9/9]
  ```
  (Delete `phase_workflow` and `phase_connectivity` calls)

**Step M — Diagnostics:**
- `phase_diagnostics`: change `[1/11]` to `[1/9]`
- `phase_docker`: change `[3/11]` to `[3/9]`
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && bash -n install.sh 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits with code 0 (no syntax errors)
    - `grep -c '/11\]' install.sh` returns 0 (no old phase numbering)
    - `grep -c '/9\]' install.sh` returns 9 (one per phase header)
    - `grep -c 'phase_workflow' install.sh` returns 0
    - `grep -c 'phase_connectivity' install.sh` returns 0
    - `grep -c 'ADMIN_EMAIL' install.sh` returns 0
    - `grep -c 'ADMIN_PASSWORD' install.sh` returns 0
    - `grep -c 'COMPANY_NAME' install.sh` returns 0
    - `grep -c 'import\.py' install.sh` returns 0
    - `grep -c 'DOKPLOY_ENABLED' install.sh` returns 0
    - `grep -c 'TUNNEL_VPS_HOST' install.sh` returns 0
    - `grep 'INIT_PASSWORD' install.sh` matches in phase_complete credentials section
    - `grep 'Dify init pwd' install.sh` matches (new summary line)
    - `grep 'WebUI pass' install.sh` matches (new summary line)
    - File `workflows/import.py` does not exist
    - File `lib/workflow.sh` does not exist
    - Directory `pipeline/` does not exist
    - File `lib/tunnel.sh` still exists (kept for Phase 5)
    - File `lib/dokploy.sh` still exists (kept for Phase 5)
  </acceptance_criteria>
  <done>install.sh has 9 phases, no Dify API automation references, no ADMIN_EMAIL/ADMIN_PASSWORD/COMPANY_NAME wizard prompts, pipeline and import.py deleted, credentials summary shows INIT_PASSWORD and WebUI password</done>
</task>

<task type="auto">
  <name>Task 2: Clean up downstream config files and docker-compose template</name>
  <files>lib/config.sh, lib/authelia.sh, templates/docker-compose.yml, templates/env.lan.template, templates/env.vps.template, templates/env.vpn.template, templates/env.offline.template</files>
  <read_first>lib/config.sh, lib/authelia.sh, templates/docker-compose.yml, templates/env.lan.template, templates/env.vps.template</read_first>
  <action>
**Step A — lib/config.sh (generate_config function, around lines 230-360):**
- Line 270: `safe_admin_email=$(escape_sed "${ADMIN_EMAIL:-admin@admin.com}")` — remove this line
- Line 271: `safe_company=$(escape_sed "${COMPANY_NAME:-AGMind}")` — remove this line
- Line 290: `-e "s|__ADMIN_EMAIL__|${safe_admin_email}|g" \` — remove this sed line
- Line 292: `-e "s|__COMPANY_NAME__|${safe_company}|g" \` — remove this sed line
- Note: Keep line 291 `-e "s|__ADMIN_PASSWORD_B64__|${admin_password_b64}|g" \` because the `INIT_PASSWORD` in env templates still uses `__ADMIN_PASSWORD_B64__` placeholder. The admin password is still generated and base64-encoded for Dify's INIT_PASSWORD — it's just no longer collected from the wizard. It auto-generates in config.sh (lines 243-248 already handle this with fallback to `generate_random 16`).

**Step B — lib/authelia.sh:**
- Line 24: `local admin_password="${ADMIN_PASSWORD:-}"` — this is used for Authelia user hash. Replace with reading INIT_PASSWORD from .env and decoding it:
  ```
  local admin_password
  admin_password=$(grep '^INIT_PASSWORD=' "${INSTALL_DIR}/docker/.env" 2>/dev/null | cut -d'=' -f2- | base64 -d 2>/dev/null || echo "")
  if [[ -z "$admin_password" ]]; then
      admin_password=$(head -c 256 /dev/urandom | LC_ALL=C tr -dc 'a-zA-Z0-9' | head -c 16)
  fi
  ```
- Line 46: `safe_company=$(printf '%s' "${COMPANY_NAME:-AGMind}" | sed 's/[&/|\]/\\&/g')` — hardcode to "AGMind":
  ```
  safe_company=$(printf '%s' "AGMind" | sed 's/[&/|\]/\\&/g')
  ```
- Line 59: `-e "s|__ADMIN_EMAIL__|${ADMIN_EMAIL:-admin@${DOMAIN:-localhost}}|g" \` — replace with hardcoded fallback since admin email is no longer collected:
  ```
  -e "s|__ADMIN_EMAIL__|admin@${DOMAIN:-localhost}|g" \
  ```
  (Authelia still needs an email for its user database; use domain-based default)

**Step C — templates/docker-compose.yml:**
Remove the entire `pipeline` service block (lines ~216-234, from `# ==================== Pipeline` comment through the service definition including its healthcheck).

In the `open-webui` service:
- Remove: `- OPENAI_API_BASE_URLS=http://pipeline:9099/v1` (line ~256)
- Remove: `- OPENAI_API_KEYS=${DIFY_API_KEY:-sk-placeholder}` (line ~257)
- Remove: `- ENABLE_OPENAI_API=true` (line ~259, or set to false)
- Change to add: `- ENABLE_OPENAI_API=false`
  (Open WebUI now talks to Ollama directly, no OpenAI-compatible proxy needed)
- In `depends_on`, remove:
  ```
      pipeline:
        condition: service_healthy
  ```
  (Keep the `ollama: condition: service_healthy` dependency)

In the `nginx` service depends_on (line ~659), remove:
  ```
      pipeline:
        condition: service_healthy
  ```

**Step D — env templates (all 4: lan, vps, vpn, offline):**
In each template file, make these changes:
- Remove the line `DIFY_API_KEY=` (and its comment `# --- Pipeline (OpenAI proxy to Dify) ---`)
- Remove the line `COMPANY_NAME=__COMPANY_NAME__`
- Keep `INIT_PASSWORD=__ADMIN_PASSWORD_B64__` (still needed for Dify first-boot)
- Keep `ENABLE_SIGNUP=false` and `DEFAULT_USER_ROLE=user`

Files to edit:
- `templates/env.lan.template`: remove lines ~85-86 (Pipeline comment + DIFY_API_KEY), remove line ~89 (COMPANY_NAME)
- `templates/env.vps.template`: remove lines ~89-90 (Pipeline comment + DIFY_API_KEY), remove line ~93 (COMPANY_NAME)
- `templates/env.vpn.template`: remove lines ~85-86 (Pipeline comment + DIFY_API_KEY), remove line ~89 (COMPANY_NAME)
- `templates/env.offline.template`: remove lines ~86-87 (Pipeline comment + DIFY_API_KEY), remove line ~90 (COMPANY_NAME)
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && grep -c 'ADMIN_EMAIL\|COMPANY_NAME' lib/config.sh templates/env.*.template 2>&1 && grep -c 'pipeline' templates/docker-compose.yml 2>&1; echo "DONE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '__ADMIN_EMAIL__' lib/config.sh` returns 0
    - `grep -c '__COMPANY_NAME__' lib/config.sh` returns 0
    - `grep -c 'safe_admin_email' lib/config.sh` returns 0
    - `grep -c 'safe_company' lib/config.sh` returns 0
    - `grep '__ADMIN_PASSWORD_B64__' lib/config.sh` still matches (INIT_PASSWORD still uses this)
    - `grep -c 'pipeline' templates/docker-compose.yml` returns 0
    - `grep -c 'DIFY_API_KEY' templates/docker-compose.yml` returns 0
    - `grep 'ENABLE_OPENAI_API=false' templates/docker-compose.yml` matches
    - `grep -c 'OPENAI_API_BASE_URLS' templates/docker-compose.yml` returns 0
    - `grep -c 'DIFY_API_KEY' templates/env.lan.template` returns 0
    - `grep -c 'COMPANY_NAME' templates/env.lan.template` returns 0
    - `grep 'INIT_PASSWORD' templates/env.lan.template` still matches
    - `grep -c 'DIFY_API_KEY' templates/env.vps.template` returns 0
    - `grep -c 'DIFY_API_KEY' templates/env.vpn.template` returns 0
    - `grep -c 'DIFY_API_KEY' templates/env.offline.template` returns 0
    - `grep -c 'COMPANY_NAME' templates/env.vps.template` returns 0
    - `grep -c 'COMPANY_NAME' templates/env.vpn.template` returns 0
    - `grep -c 'COMPANY_NAME' templates/env.offline.template` returns 0
    - `grep 'ADMIN_PASSWORD' lib/authelia.sh` does NOT match (replaced with INIT_PASSWORD read)
    - `grep 'INIT_PASSWORD' lib/authelia.sh` matches (new source of password)
    - `grep 'AGMind' lib/authelia.sh` matches (hardcoded company name)
  </acceptance_criteria>
  <done>All downstream files cleaned: config.sh no longer substitutes ADMIN_EMAIL/COMPANY_NAME, docker-compose has no pipeline service, env templates have no DIFY_API_KEY or COMPANY_NAME, authelia.sh reads password from INIT_PASSWORD instead of ADMIN_PASSWORD variable</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash -n install.sh` passes (no syntax errors)
2. `grep -rn 'import\.py\|phase_workflow\|phase_connectivity\|DIFY_API_KEY' install.sh lib/config.sh templates/` returns nothing
3. `grep -rn 'ADMIN_EMAIL' install.sh lib/config.sh lib/authelia.sh templates/env.*.template` returns nothing (authelia template file may still have __ADMIN_EMAIL__ placeholder — that's OK, it's in the .yml.template)
4. `ls pipeline/ 2>/dev/null` returns nothing (directory gone)
5. `ls workflows/import.py 2>/dev/null` returns nothing (file gone)
6. `ls lib/workflow.sh 2>/dev/null` returns nothing (file gone)
7. `ls lib/tunnel.sh lib/dokploy.sh 2>/dev/null` both exist (kept for Phase 5)
8. `grep -c '/9\]' install.sh` returns 9
</verification>

<success_criteria>
- install.sh is syntactically valid bash with 9 phases
- Zero references to Dify API automation (import.py, phase_workflow, DIFY_API_KEY)
- Zero wizard prompts for ADMIN_EMAIL, ADMIN_PASSWORD, COMPANY_NAME
- Pipeline service removed from docker-compose template
- Open WebUI talks to Ollama directly
- Credentials summary shows INIT_PASSWORD and WebUI password
- All env templates cleaned of DIFY_API_KEY and COMPANY_NAME
</success_criteria>

<output>
After completion, create `.planning/phases/01-surgery-remove-dify-api-automation/01-01-SUMMARY.md`
</output>
