---
phase: 19-bugfixes-gpu-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wizard.sh
  - lib/models.sh
  - templates/env.lan.template
  - templates/env.vps.template
  - templates/env.vpn.template
  - templates/env.offline.template
autonomous: true
requirements:
  - BFIX-44
  - BFIX-45

must_haves:
  truths:
    - "VRAM guard использует effective_vram = gpu_vram - 2 при проверке моделей vLLM"
    - "NON_INTERACTIVE с моделью > effective_vram завершается exit 1"
    - "Интерактивный визард показывает effective_vram в предупреждении"
    - "load_reranker() не пытается загружать модель и выводит log_warn о broken"
    - "ETL визард не упоминает reranker в опции 2"
    - "Env templates содержат закомментированный RERANK_MODEL_NAME с пометкой BROKEN"
  artifacts:
    - path: "lib/wizard.sh"
      provides: "TEI offset in VRAM guard calculations + ETL wizard text fix"
      contains: "TEI_VRAM_OFFSET"
    - path: "lib/models.sh"
      provides: "Disabled reranker with broken warning"
      contains: "BROKEN"
    - path: "templates/env.lan.template"
      provides: "Commented out reranker config"
      contains: "# BROKEN"
  key_links:
    - from: "lib/wizard.sh"
      to: "_get_vllm_vram_req"
      via: "VRAM comparison with effective_vram"
      pattern: "effective_vram|TEI_VRAM_OFFSET"
    - from: "lib/models.sh"
      to: "log_warn"
      via: "broken reranker skip"
      pattern: "log_warn.*reranker.*broken"
---

<objective>
Fix VRAM guard to account for TEI memory overhead and disable broken Xinference reranker.

Purpose: Prevent OOM in NON_INTERACTIVE mode by subtracting 2 GB TEI offset from available VRAM, and stop installation failures from broken bce-reranker in Xinference v2.3.0.
Output: Updated wizard.sh with effective_vram calculation, disabled load_reranker(), updated ETL wizard text, commented reranker in env templates.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-bugfixes-gpu-enhancement/19-CONTEXT.md
@lib/wizard.sh
@lib/models.sh
@templates/env.lan.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFIX-45 — Apply TEI VRAM offset in VRAM guard calculations</name>
  <files>lib/wizard.sh</files>
  <read_first>lib/wizard.sh (lines 420-542 — _wizard_vllm_model interactive VRAM guard at 462-494 and _wizard_llm_model NON_INTERACTIVE guard at 496-542)</read_first>
  <action>
Modify lib/wizard.sh to apply a TEI VRAM offset of 2 GB in all VRAM guard paths:

1. Add a constant near the top of the VRAM-related code. Insert BEFORE the `_wizard_vllm_model()` function (around line 380, before the model arrays):
```bash
# TEI VRAM offset: reserve 2 GB for TEI embedding container
readonly TEI_VRAM_OFFSET=2
```

2. In `_wizard_vllm_model()` interactive VRAM guard (around line 472), change the comparison to use effective_vram:
BEFORE:
```bash
        if [[ "$vram_gb" -gt 0 && "${vram_req[$REPLY]}" -gt "$vram_gb" ]]; then
            echo ""
            echo -e "  ${YELLOW}Модель требует ${vram_req[$REPLY]} GB VRAM, доступно ${vram_gb} GB. Возможен OOM.${NC}"
```
AFTER:
```bash
        local effective_vram=$(( vram_gb - TEI_VRAM_OFFSET ))
        if [[ "$vram_gb" -gt 0 && "${vram_req[$REPLY]}" -gt "$effective_vram" ]]; then
            echo ""
            echo -e "  ${YELLOW}Модель требует ${vram_req[$REPLY]} GB VRAM, доступно ${effective_vram} GB effective (${vram_gb} GB - ${TEI_VRAM_OFFSET} GB TEI). Возможен OOM.${NC}"
```

3. In `_wizard_llm_model()` NON_INTERACTIVE VRAM guard (around lines 527-536), change to use effective_vram:
BEFORE (line 530):
```bash
                ni_vram_gb=$(( DETECTED_GPU_VRAM / 1024 ))
```
AFTER:
```bash
                ni_vram_gb=$(( DETECTED_GPU_VRAM / 1024 ))
                local ni_effective_vram=$(( ni_vram_gb - TEI_VRAM_OFFSET ))
```

BEFORE (line 532):
```bash
            if [[ "$ni_vram_gb" -gt 0 && "$ni_vram_req" -gt "$ni_vram_gb" ]]; then
                log_error "Model ${VLLM_MODEL} requires ${ni_vram_req} GB VRAM, available: ${ni_vram_gb} GB"
```
AFTER:
```bash
            if [[ "$ni_vram_gb" -gt 0 && "$ni_vram_req" -gt "$ni_effective_vram" ]]; then
                log_error "Model ${VLLM_MODEL} requires ${ni_vram_req} GB VRAM, effective available: ${ni_effective_vram} GB (${ni_vram_gb} GB - ${TEI_VRAM_OFFSET} GB TEI)"
```

Key points:
- `TEI_VRAM_OFFSET=2` is a hardcoded constant (per user decision — not configurable)
- `readonly` prevents accidental modification
- Both interactive and NON_INTERACTIVE paths use effective_vram
- Error/warning messages show the breakdown: effective = raw - TEI offset
- NON_INTERACTIVE still does `exit 1` when model exceeds effective_vram (unchanged behavior, just threshold changed)
- The `ni_vram_gb -eq 0` fallback warning (line 537-538) stays unchanged — if GPU not detected, still warns
  </action>
  <verify>
    <automated>grep -n "TEI_VRAM_OFFSET" lib/wizard.sh && grep -n "effective_vram" lib/wizard.sh && grep -c "exit 1" lib/wizard.sh && bash -n lib/wizard.sh</automated>
  </verify>
  <acceptance_criteria>
- lib/wizard.sh contains `readonly TEI_VRAM_OFFSET=2`
- lib/wizard.sh contains `effective_vram=$(( vram_gb - TEI_VRAM_OFFSET ))` in interactive guard
- lib/wizard.sh contains `ni_effective_vram=$(( ni_vram_gb - TEI_VRAM_OFFSET ))` in NON_INTERACTIVE guard
- lib/wizard.sh compares `ni_vram_req` against `ni_effective_vram` (not `ni_vram_gb`) in NON_INTERACTIVE
- lib/wizard.sh compares `vram_req[$REPLY]` against `effective_vram` (not `vram_gb`) in interactive
- Error message in NON_INTERACTIVE contains string `effective available:`
- Warning message in interactive contains string `effective` or `TEI`
- `bash -n lib/wizard.sh` exits 0 (no syntax errors)
  </acceptance_criteria>
  <done>VRAM guard subtracts 2 GB TEI offset in both interactive and NON_INTERACTIVE paths; models that fit raw VRAM but not effective_vram are rejected/warned</done>
</task>

<task type="auto">
  <name>Task 2: BFIX-44 — Disable broken Xinference reranker</name>
  <files>lib/models.sh, lib/wizard.sh, templates/env.lan.template, templates/env.vps.template, templates/env.vpn.template, templates/env.offline.template</files>
  <read_first>lib/models.sh (lines 147-183 — load_reranker function), lib/wizard.sh (lines 193-209 — _wizard_etl function), templates/env.lan.template (lines 54-58 — reranker env vars)</read_first>
  <action>
Four changes to disable the broken bce-reranker:

**A. lib/models.sh — Disable load_reranker():**
Replace the body of `load_reranker()` (lines 151-183) with an early-return that logs a warning:
```bash
load_reranker() {
    if [[ "${ETL_ENHANCED:-false}" != "true" ]]; then
        return 0
    fi

    # BROKEN: bce-reranker-base_v1 fails in Xinference v2.3.0
    # Reranker will return via TEI in Phase 22 (RNKR-01..03)
    log_warn "Reranker disabled: bce-reranker-base_v1 is broken in Xinference v2.3.0"
    return 0
}
```
Delete the old Xinference curl logic (lines 156-182). Keep the function signature so callers don't break.

**B. lib/wizard.sh — Update ETL wizard text (lines 199-201):**
BEFORE:
```bash
    echo "Расширенная обработка документов (Docling + Xinference reranker)?"
    echo "  1) Нет — стандартный Dify ETL (по умолчанию)"
    echo "  2) Да — Docling + bce-reranker-base_v1"
```
AFTER:
```bash
    echo "Расширенная обработка документов (Docling)?"
    echo "  1) Нет — стандартный Dify ETL (по умолчанию)"
    echo "  2) Да — Docling (улучшенный парсинг документов)"
```
Remove all mentions of "Xinference reranker" and "bce-reranker-base_v1" from the wizard prompt.

**C. All 4 env templates — Comment out RERANK_MODEL_NAME:**
In each of these files, replace lines 55-57:
- `templates/env.lan.template`
- `templates/env.vps.template`
- `templates/env.vpn.template`
- `templates/env.offline.template`

BEFORE:
```
# --- Reranker (Xinference) ---
XINFERENCE_BASE_URL=http://xinference:9997
RERANK_MODEL_NAME=bce-reranker-base_v1
```
AFTER:
```
# --- Reranker (Xinference) ---
XINFERENCE_BASE_URL=http://xinference:9997
# RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN in xinference v2.3.0 — reranker via TEI in Phase 22
```

Key points:
- `load_reranker()` function kept (not deleted) to avoid breaking callers in `download_models()`
- ETL wizard still sets `ETL_ENHANCED=true` which enables Docling — only reranker text removed
- `XINFERENCE_BASE_URL` line kept uncommented (still needed by Xinference service if running)
- `RERANK_MODEL_NAME` commented with `# BROKEN` annotation for traceability
  </action>
  <verify>
    <automated>grep -n "BROKEN" lib/models.sh && grep -n "Docling" lib/wizard.sh | grep -v "reranker\|Xinference" | head -3 && grep "# RERANK_MODEL_NAME" templates/env.lan.template && grep "# RERANK_MODEL_NAME" templates/env.vps.template && bash -n lib/models.sh && bash -n lib/wizard.sh</automated>
  </verify>
  <acceptance_criteria>
- lib/models.sh `load_reranker()` contains `log_warn "Reranker disabled: bce-reranker-base_v1 is broken in Xinference v2.3.0"`
- lib/models.sh `load_reranker()` does NOT contain `curl -sf -X POST http://localhost:9997/v1/models`
- lib/models.sh `load_reranker()` contains `return 0` after the log_warn
- lib/wizard.sh `_wizard_etl()` contains string `Docling (улучшенный парсинг документов)`
- lib/wizard.sh `_wizard_etl()` does NOT contain string `bce-reranker-base_v1`
- lib/wizard.sh `_wizard_etl()` does NOT contain string `Xinference reranker`
- templates/env.lan.template contains `# RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN`
- templates/env.vps.template contains `# RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN`
- templates/env.vpn.template contains `# RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN`
- templates/env.offline.template contains `# RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN`
- `bash -n lib/models.sh` exits 0
- `bash -n lib/wizard.sh` exits 0
  </acceptance_criteria>
  <done>Xinference reranker disabled with BROKEN annotation, wizard ETL text updated to Docling-only, env templates have commented RERANK_MODEL_NAME</done>
</task>

</tasks>

<verification>
1. `grep -n "TEI_VRAM_OFFSET" lib/wizard.sh` — constant defined
2. `grep -n "effective_vram" lib/wizard.sh` — used in both guards
3. `grep "BROKEN" lib/models.sh` — reranker marked broken
4. `grep "# RERANK_MODEL_NAME" templates/env.*.template` — commented in all 4 templates
5. `grep "Docling" lib/wizard.sh | grep -v reranker` — wizard text updated
6. `bash -n lib/wizard.sh && bash -n lib/models.sh` — syntax valid
</verification>

<success_criteria>
- VRAM guard uses effective_vram (raw - 2 GB) in both interactive and NON_INTERACTIVE paths
- Model exceeding effective_vram but fitting raw VRAM is rejected in NON_INTERACTIVE (exit 1) and warned in interactive
- load_reranker() skips model loading with log_warn about broken status
- ETL wizard option 2 says "Docling" without mentioning reranker or Xinference
- All 4 env templates have RERANK_MODEL_NAME commented with BROKEN annotation
- All modified scripts pass bash -n syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/19-bugfixes-gpu-enhancement/19-02-SUMMARY.md`
</output>
