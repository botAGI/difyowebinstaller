---
phase: 10-release-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - COMPONENTS.md
autonomous: true
requirements:
  - BFIX-01
  - RELS-02

must_haves:
  truths:
    - "BFIX-01 already applied — update.sh sets LC_ALL=C before any grep/sed calls"
    - "COMPONENTS.md в корне описывает 5 dependency groups с перечнем компонентов"
    - "Мейнтейнер понимает какие компоненты тестировать вместе"
  artifacts:
    - path: "COMPONENTS.md"
      provides: "Dependency group documentation for maintainers"
      contains: "dify-core"
    - path: "scripts/update.sh"
      provides: "Locale-safe regex (already fixed)"
      contains: "export LC_ALL=C"
  key_links:
    - from: "COMPONENTS.md"
      to: "templates/versions.env"
      via: "component names match version keys"
      pattern: "dify-api.*dify-web.*plugin-daemon.*sandbox"
---

<objective>
Verify the locale bugfix in update.sh (BFIX-01) and create COMPONENTS.md documenting dependency groups (RELS-02).

Purpose: BFIX-01 was already fixed in commit ad214f9 — this plan verifies correctness and creates the maintainer documentation for component dependency groups needed before v2.1.0 release.
Output: COMPONENTS.md in repo root, verified BFIX-01
</objective>

<execution_context>
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/workflows/execute-plan.md
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@templates/versions.env
@scripts/update.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify BFIX-01 locale fix in update.sh</name>
  <files>scripts/update.sh</files>
  <read_first>
    - scripts/update.sh (verify LC_ALL=C is present at top, before any grep/sed usage)
  </read_first>
  <action>
Verify that BFIX-01 is correctly implemented in scripts/update.sh:

1. Confirm line 8 contains `export LC_ALL=C` — this is set BEFORE any grep/sed calls, so all subsequent calls inherit it.
2. Verify there are NO subshells or `env -i` calls that would reset LC_ALL within the script.
3. Run `bash -n scripts/update.sh` to confirm script has no syntax errors.
4. If verification passes, no code changes needed — BFIX-01 was already fixed in commit ad214f9.
5. If LC_ALL=C is missing or placed after grep/sed calls — add `export LC_ALL=C` as the first line after `set -euo pipefail`.

Note: The fix uses `export LC_ALL=C` at script top (line 8), which is the correct approach — it covers ALL grep/sed/awk calls in the entire script without needing per-call prefixes.
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && head -10 scripts/update.sh | grep -q "export LC_ALL=C" && echo "BFIX-01 OK" && bash -n scripts/update.sh && echo "Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/update.sh line 8 contains exactly `export LC_ALL=C`
    - `export LC_ALL=C` appears BEFORE any grep or sed call in the file
    - `bash -n scripts/update.sh` exits with code 0 (no syntax errors)
    - No subshells with `env -i` or `LC_ALL=` override exist after line 8
  </acceptance_criteria>
  <done>BFIX-01 verified: LC_ALL=C is set at script top, all grep/sed calls are locale-safe</done>
</task>

<task type="auto">
  <name>Task 2: Create COMPONENTS.md dependency groups documentation</name>
  <files>COMPONENTS.md</files>
  <read_first>
    - templates/versions.env (source of truth for all component version keys — must match groups)
    - scripts/update.sh (lines 26-80 — NAME_TO_VERSION_KEY map to verify component short names)
  </read_first>
  <action>
Create `COMPONENTS.md` in repo root with the following exact content structure:

```markdown
# Component Dependency Groups

Components within each group should be updated and tested together.
Groups are based on shared configuration, version coupling, or runtime dependencies.

## dify-core

| Component | Version Key | Notes |
|-----------|-------------|-------|
| dify-api | DIFY_VERSION | API backend |
| dify-web | DIFY_VERSION | Frontend |
| plugin-daemon | PLUGIN_DAEMON_VERSION | Plugin execution engine |
| sandbox | SANDBOX_VERSION | Code execution sandbox |

> Always use the same `DIFY_VERSION` for api and web.
> Check plugin-daemon and sandbox compatibility on each Dify release.

## gpu-inference

| Component | Version Key | Notes |
|-----------|-------------|-------|
| vllm | VLLM_VERSION | GPU inference server |
| tei | TEI_VERSION | Text embeddings |

> Both depend on CUDA version and GPU driver.
> Update together when changing CUDA base.

## monitoring

| Component | Version Key | Notes |
|-----------|-------------|-------|
| grafana | GRAFANA_VERSION | Dashboards |
| prometheus | PROMETHEUS_VERSION | Metrics collection |
| loki | LOKI_VERSION | Log aggregation |
| promtail | PROMTAIL_VERSION | Log shipping |
| alertmanager | ALERTMANAGER_VERSION | Alert routing |
| node-exporter | NODE_EXPORTER_VERSION | Host metrics |
| cadvisor | CADVISOR_VERSION | Container metrics |

> Independent from AI stack. Can be updated freely.
> Keep loki and promtail on the same major version.

## standalone

| Component | Version Key | Notes |
|-----------|-------------|-------|
| ollama | OLLAMA_VERSION | Local LLM runtime |
| openwebui | OPENWEBUI_VERSION | Chat UI |
| portainer | PORTAINER_VERSION | Container management |
| authelia | AUTHELIA_VERSION | SSO/2FA gateway |
| certbot | CERTBOT_VERSION | TLS certificates |
| docling | DOCLING_SERVE_VERSION | Document conversion |
| xinference | XINFERENCE_VERSION | Model serving |

> No cross-dependencies within this group. Update individually.

## infra

| Component | Version Key | Notes |
|-----------|-------------|-------|
| postgres | POSTGRES_VERSION | Primary database |
| redis | REDIS_VERSION | Cache and locks |
| nginx | NGINX_VERSION | Reverse proxy |
| weaviate | WEAVIATE_VERSION | Vector store |
| qdrant | QDRANT_VERSION | Vector store (alternative) |
| squid | SQUID_VERSION | SSRF proxy |

> **Postgres major version upgrade requires migration** (pg_dump / pg_restore).
> Redis and nginx are backward compatible — safe to update.
> Vector stores: update only when Dify release notes mention compatibility.
```

Ensure every version key in `templates/versions.env` appears in exactly one group (except VLLM_CUDA_SUFFIX and PIPELINES_VERSION which are config flags, not standalone components).
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && test -f COMPONENTS.md && grep -c "^## " COMPONENTS.md | grep -q "5" && echo "5 groups OK" && grep -q "dify-core" COMPONENTS.md && grep -q "gpu-inference" COMPONENTS.md && grep -q "monitoring" COMPONENTS.md && grep -q "standalone" COMPONENTS.md && grep -q "infra" COMPONENTS.md && echo "All groups present"</automated>
  </verify>
  <acceptance_criteria>
    - COMPONENTS.md exists in repo root
    - Contains exactly 5 H2 sections: dify-core, gpu-inference, monitoring, standalone, infra
    - dify-core lists: dify-api, dify-web, plugin-daemon, sandbox
    - gpu-inference lists: vllm, tei
    - monitoring lists: grafana, prometheus, loki, promtail, alertmanager, node-exporter, cadvisor
    - standalone lists: ollama, openwebui, portainer, authelia, certbot, docling, xinference
    - infra lists: postgres, redis, nginx, weaviate, qdrant, squid
    - Each component has its version key from versions.env
    - Contains warning about postgres major version migration
    - Contains note about loki/promtail same major version
  </acceptance_criteria>
  <done>COMPONENTS.md created with all 5 dependency groups, every component from versions.env mapped to a group, maintainer notes included</done>
</task>

</tasks>

<verification>
1. `head -10 scripts/update.sh` shows `export LC_ALL=C` before any grep/sed
2. `bash -n scripts/update.sh` passes
3. `COMPONENTS.md` has 5 groups matching all components from versions.env
4. Every version key from templates/versions.env (except VLLM_CUDA_SUFFIX, PIPELINES_VERSION) appears in COMPONENTS.md
</verification>

<success_criteria>
- BFIX-01 verified as correctly implemented (LC_ALL=C at script top)
- COMPONENTS.md exists with 5 dependency groups covering all managed components
- A maintainer reading COMPONENTS.md knows which components to test together before release
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-foundation/10-01-SUMMARY.md`
</output>
