---
phase: 20-xinference-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docker-compose.yml
  - lib/compose.sh
  - lib/wizard.sh
  - lib/config.sh
  - lib/models.sh
  - lib/health.sh
  - templates/env.lan.template
  - templates/env.vps.template
  - templates/env.vpn.template
  - templates/env.offline.template
autonomous: true
requirements: [XINF-01, XINF-02, XINF-03]

must_haves:
  truths:
    - "Xinference service block removed from docker-compose.yml"
    - "Profile etl renamed to docling in docker-compose.yml"
    - "ETL_ENHANCED replaced by ENABLE_DOCLING in wizard, compose, config"
    - "Backward compat shim converts ETL_ENHANCED=true to ENABLE_DOCLING=true"
    - "load_reranker() function and call sites removed from models.sh"
    - "Env templates use ENABLE_DOCLING instead of Xinference vars"
  artifacts:
    - path: "templates/docker-compose.yml"
      provides: "Docling in profile docling, no xinference service or volume"
      contains: "profiles:\\n      - docling"
    - path: "lib/compose.sh"
      provides: "build_compose_profiles with ENABLE_DOCLING and backward compat shim"
      contains: "ENABLE_DOCLING"
    - path: "lib/wizard.sh"
      provides: "ENABLE_DOCLING variable, fixed summary line"
      contains: "ENABLE_DOCLING"
    - path: "lib/models.sh"
      provides: "No load_reranker function, no xinference references"
  key_links:
    - from: "lib/wizard.sh"
      to: "lib/compose.sh"
      via: "ENABLE_DOCLING export"
      pattern: "export.*ENABLE_DOCLING"
    - from: "lib/compose.sh"
      to: "templates/docker-compose.yml"
      via: "profile docling in COMPOSE_PROFILE_STRING"
      pattern: "docling"
    - from: "lib/config.sh"
      to: "templates/env.*.template"
      via: "ENABLE_DOCLING → ETL_TYPE=unstructured_api mapping"
      pattern: "ENABLE_DOCLING"
---

<objective>
Remove Xinference from core infrastructure (docker-compose, lib/ scripts, env templates) and replace ETL_ENHANCED with ENABLE_DOCLING. Rename profile `etl` to `docling`.

Purpose: Xinference is broken (bce-reranker in v2.3.0) and will be replaced by TEI in Phase 22. This phase decouples Docling from Xinference and cleans up the flag naming.
Output: docker-compose.yml without xinference, lib/ scripts using ENABLE_DOCLING, env templates cleaned.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-xinference-removal/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Xinference from docker-compose + rename etl profile to docling</name>
  <files>templates/docker-compose.yml</files>
  <read_first>
    - templates/docker-compose.yml (lines 494-548 for Docling+Xinference services, line 1042 for volume)
  </read_first>
  <action>
1. In `templates/docker-compose.yml`:
   - Change Docling service profile from `- etl` to `- docling` (line 502)
   - Delete the entire Xinference service block (lines 515-543): the `# ==================== Xinference (Reranker) ====================` comment through the `- agmind-backend` network line
   - Delete `agmind_xinference_data:` from the volumes section (line 1042)

The Docling service block (lines 494-513) remains unchanged except for the profile rename.
  </action>
  <verify>
    <automated>grep -c "xinference" templates/docker-compose.yml | grep -q "^0$" && grep "- docling" templates/docker-compose.yml | head -1 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xinference" templates/docker-compose.yml` returns 0
    - `grep -c "agmind_xinference_data" templates/docker-compose.yml` returns 0
    - `grep "- docling" templates/docker-compose.yml` shows the Docling service profile
    - `grep "- etl" templates/docker-compose.yml` returns 0 matches (no remaining etl profile)
    - Docling service block (container_name: agmind-docling, image, healthcheck) remains intact
  </acceptance_criteria>
  <done>Xinference service and volume removed from docker-compose.yml. Docling profile renamed from etl to docling.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ETL_ENHANCED to ENABLE_DOCLING in lib/ scripts + env templates</name>
  <files>lib/wizard.sh, lib/compose.sh, lib/config.sh, lib/models.sh, lib/health.sh, templates/env.lan.template, templates/env.vps.template, templates/env.vpn.template, templates/env.offline.template</files>
  <read_first>
    - lib/wizard.sh (lines 23-39 for defaults, lines 193-210 for _wizard_etl, lines 858-927 for summary+exports)
    - lib/compose.sh (lines 17-38 for build_compose_profiles)
    - lib/config.sh (lines 240-244 for ETL mapping)
    - lib/models.sh (lines 1-8 for header, lines 145-212 for load_reranker + download_models)
    - lib/health.sh (lines 55-67 for ETL services)
    - templates/env.lan.template (lines 55-57 for Xinference vars)
  </read_first>
  <action>
**lib/wizard.sh:**
1. Line 28: Change `ETL_ENHANCED="${ETL_ENHANCED:-false}"` to `ENABLE_DOCLING="${ENABLE_DOCLING:-${ETL_ENHANCED:-false}}"` (backward compat: reads ETL_ENHANCED if ENABLE_DOCLING not set)
2. In `_wizard_etl()` (lines 193-210):
   - Line 195: Change `ETL_ENHANCED="false"` to `ENABLE_DOCLING="false"`
   - Line 206: Change `ETL_ENHANCED="true"` to `ENABLE_DOCLING="true"`
   - Line 207: Change `ETL_ENHANCED="false"` to `ENABLE_DOCLING="false"`
3. Line 863: Change `[[ "$ETL_ENHANCED" == "true" ]] && echo "  ETL:          Docling + Xinference"` to `[[ "$ENABLE_DOCLING" == "true" ]] && echo "  ETL:          Docling"`
4. Line 918: In the export line, change `ETL_ENHANCED` to `ENABLE_DOCLING`

**lib/compose.sh:**
1. In `build_compose_profiles()` (lines 23-26): Replace the ETL block:
   ```bash
   # Docling: check both wizard var (ENABLE_DOCLING) and .env var (ETL_TYPE) for resume support
   # Backward compat: ETL_ENHANCED=true without ENABLE_DOCLING → treat as ENABLE_DOCLING=true
   local docling_enabled="${ENABLE_DOCLING:-${ETL_ENHANCED:-false}}"
   if [[ "$docling_enabled" == "true" || "${ETL_TYPE:-dify}" == "unstructured_api" ]]; then
       profiles="${profiles:+$profiles,}docling"
   fi
   ```
   This replaces the old block that checked ETL_ENHANCED and appended profile `etl`. The backward compat shim: if old .env has ETL_ENHANCED=true but no ENABLE_DOCLING, the `${ENABLE_DOCLING:-${ETL_ENHANCED:-false}}` expansion handles it.

**lib/config.sh:**
1. Lines 240-244: Change `ETL_ENHANCED` to `ENABLE_DOCLING`:
   ```bash
   # ETL type mapping
   local etl_type="dify"
   if [[ "${ENABLE_DOCLING:-${ETL_ENHANCED:-false}}" == "true" ]]; then
       etl_type="unstructured_api"
   fi
   ```
   The backward compat nested default handles old .env files.

**lib/models.sh:**
1. Line 2: Update header comment — remove "Xinference reranker" from description: `# models.sh — Download LLM/embedding models (Ollama pull, vLLM/TEI info).`
2. Lines 4-5: Remove `load_reranker()` from Functions list and `ETL_ENHANCED` from Expects list
3. Delete the entire XINFERENCE RERANKER section (lines 145-160): the separator comment, function, and all content
4. Line 179 (in download_models, offline block): Remove `load_reranker` call
5. Line 208 (in download_models, near end): Remove `load_reranker` call

**lib/health.sh:**
1. Line 60: Change `services+=(docling xinference)` to `services+=(docling)` — remove xinference from expected healthy services when ETL is active

**All 4 env templates** (`templates/env.lan.template`, `templates/env.vps.template`, `templates/env.vpn.template`, `templates/env.offline.template`):
1. Replace lines 55-57:
   ```
   # --- Reranker (Xinference) ---
   XINFERENCE_BASE_URL=http://xinference:9997
   # RERANK_MODEL_NAME=bce-reranker-base_v1  # BROKEN in xinference v2.3.0 -- reranker via TEI in Phase 22
   ```
   With:
   ```
   # --- Document Processing ---
   ENABLE_DOCLING=false
   ```
  </action>
  <verify>
    <automated>grep -rn "ETL_ENHANCED" lib/wizard.sh lib/compose.sh lib/config.sh lib/models.sh 2>/dev/null | grep -v "ETL_ENHANCED:-false" | grep -v "^$" ; grep -c "load_reranker" lib/models.sh | grep -q "^0$" && grep -c "XINFERENCE" templates/env.lan.template | grep -q "^0$" && grep "ENABLE_DOCLING" lib/wizard.sh | head -3 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "load_reranker" lib/models.sh` returns 0
    - `grep "XINFERENCE RERANKER" lib/models.sh` returns no matches
    - `grep "ENABLE_DOCLING" lib/wizard.sh` shows the default, _wizard_etl assignments, summary line, and export
    - `grep "ENABLE_DOCLING" lib/compose.sh` shows the docling profile check
    - `grep "docling" lib/compose.sh` shows profile name `docling` (not `etl`)
    - `grep "ENABLE_DOCLING" lib/config.sh` shows the ETL_TYPE mapping
    - `grep "xinference" lib/health.sh` returns 0 matches
    - `grep "XINFERENCE_BASE_URL" templates/env.lan.template` returns 0 matches
    - `grep "ENABLE_DOCLING=false" templates/env.lan.template` returns a match
    - All 4 env templates have `ENABLE_DOCLING=false` and no Xinference vars
    - `grep "ETL:          Docling" lib/wizard.sh` shows summary without "Xinference"
    - Backward compat: `ENABLE_DOCLING:-${ETL_ENHANCED:-false}` pattern appears in wizard.sh defaults and compose.sh
  </acceptance_criteria>
  <done>ETL_ENHANCED fully replaced by ENABLE_DOCLING with backward compat. load_reranker removed. Xinference vars removed from env templates. Profile name changed to docling.</done>
</task>

</tasks>

<verification>
- `grep -rn "xinference\|XINFERENCE" templates/docker-compose.yml lib/ templates/env.*.template` returns 0 matches
- `grep -rn "ETL_ENHANCED" lib/wizard.sh lib/compose.sh lib/config.sh` only appears inside `${ETL_ENHANCED:-false}` backward compat expansions
- `grep "profile.*etl\|etl.*profile" templates/docker-compose.yml lib/compose.sh` returns 0 (profile etl no longer exists)
- `grep "ENABLE_DOCLING" lib/wizard.sh lib/compose.sh lib/config.sh` returns matches in all three files
</verification>

<success_criteria>
- Xinference service, volume, and all direct references removed from docker-compose.yml and lib/ scripts
- ENABLE_DOCLING replaces ETL_ENHANCED as the primary flag, with backward compat shim
- Docling runs in profile `docling` (not `etl`)
- load_reranker() fully deleted
- Env templates cleaned: no XINFERENCE_BASE_URL, no RERANK_MODEL_NAME
</success_criteria>

<output>
After completion, create `.planning/phases/20-xinference-removal/20-01-SUMMARY.md`
</output>
