---
phase: 20-xinference-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/agmind.sh
  - scripts/check-upstream.sh
  - scripts/update.sh
  - scripts/uninstall.sh
  - scripts/generate-manifest.sh
  - scripts/check-manifest-versions.py
  - templates/versions.env
  - templates/release-manifest.json
  - COMPONENTS.md
  - COMPATIBILITY.md
autonomous: true
requirements: [XINF-01]

must_haves:
  truths:
    - "agmind gpu assign no longer lists xinference as valid service"
    - "check-upstream.sh no longer checks Xinference version"
    - "update.sh has cleanup block to remove orphan xinference container+volume"
    - "versions.env has no XINFERENCE_VERSION"
    - "COMPONENTS.md and COMPATIBILITY.md have no xinference entries"
  artifacts:
    - path: "scripts/agmind.sh"
      provides: "gpu assign without xinference option"
    - path: "scripts/update.sh"
      provides: "Xinference orphan cleanup in update flow"
      contains: "agmind-xinference"
    - path: "templates/versions.env"
      provides: "No XINFERENCE_VERSION line"
  key_links:
    - from: "scripts/update.sh"
      to: "docker compose"
      via: "cleanup orphan xinference container on update"
      pattern: "agmind-xinference"
---

<objective>
Remove all Xinference references from peripheral scripts (agmind.sh, check-upstream.sh, update.sh, uninstall.sh, generate-manifest.sh), config files (versions.env, release-manifest.json), and documentation (COMPONENTS.md, COMPATIBILITY.md). Add xinference orphan cleanup to update.sh for existing installations.

Purpose: Complete the Xinference removal across the entire codebase. Ensure `agmind update` on existing installations cleans up the orphaned xinference container and volume.
Output: All scripts, configs, and docs free of xinference references. update.sh has cleanup block.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-xinference-removal/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Xinference from agmind.sh, check-upstream.sh, generate-manifest.sh, check-manifest-versions.py</name>
  <files>scripts/agmind.sh, scripts/check-upstream.sh, scripts/generate-manifest.sh, scripts/check-manifest-versions.py</files>
  <read_first>
    - scripts/agmind.sh (lines 575-590 for gpu assign service case, lines 640-650 for help text)
    - scripts/check-upstream.sh (line 29 for Xinference entry)
    - scripts/generate-manifest.sh (line 66 for xinference entry)
    - scripts/check-manifest-versions.py (lines 60-65 and 90-95 for xinference references)
  </read_first>
  <action>
**scripts/agmind.sh:**
1. Line 582: Delete the `xinference)  env_var="XINFERENCE_CUDA_DEVICE" ;;` case
2. Line 585: Change error message from `"Valid services: vllm, tei, xinference"` to `"Valid services: vllm, tei"`
3. Line 647: Change help text from `assign <svc> <id>  Assign GPU to service (vllm, tei, xinference)` to `assign <svc> <id>  Assign GPU to service (vllm, tei)`

**scripts/check-upstream.sh:**
1. Line 29: Delete the entire Xinference entry: `"Xinference|XINFERENCE_VERSION|xorbitsai/inference|gh"`

**scripts/generate-manifest.sh:**
1. Line 66: Delete the xinference entry: `"xinference|docker.io|xprobe/xinference|${XINFERENCE_VERSION}|linux/amd64"`

**scripts/check-manifest-versions.py:**
1. Line ~63: Remove `"xinference"` from the known_images list
2. Line ~93: Remove `"xinference": "XINFERENCE_VERSION"` from the version_env_map dict
  </action>
  <verify>
    <automated>grep -c "xinference" scripts/agmind.sh scripts/check-upstream.sh scripts/generate-manifest.sh scripts/check-manifest-versions.py 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xinference" scripts/agmind.sh` returns 0
    - `grep -c "xinference" scripts/check-upstream.sh` returns 0
    - `grep -c "xinference" scripts/generate-manifest.sh` returns 0
    - `grep -ci "xinference" scripts/check-manifest-versions.py` returns 0
    - `grep "Valid services:" scripts/agmind.sh` shows "vllm, tei" without xinference
    - `grep "assign.*svc.*id" scripts/agmind.sh` shows "(vllm, tei)" without xinference
  </acceptance_criteria>
  <done>All Xinference references removed from agmind.sh, check-upstream.sh, generate-manifest.sh, check-manifest-versions.py.</done>
</task>

<task type="auto">
  <name>Task 2: Xinference cleanup in update.sh + uninstall.sh + config files + docs</name>
  <files>scripts/update.sh, scripts/uninstall.sh, templates/versions.env, templates/release-manifest.json, COMPONENTS.md, COMPATIBILITY.md</files>
  <read_first>
    - scripts/update.sh (lines 35-45 for version map, lines 70-80 for container map, lines 800-810 for service list)
    - scripts/uninstall.sh (line 101 for volume cleanup grep)
    - templates/versions.env (line 28 for XINFERENCE_VERSION)
    - templates/release-manifest.json (lines 105-110 for xinference entry)
    - COMPONENTS.md (line 53 for xinference row)
    - COMPATIBILITY.md (line 20 for xinference row, line 58 for ARM64 note)
  </read_first>
  <action>
**scripts/update.sh:**
1. Line ~41: Delete `[xinference]=XINFERENCE_VERSION` from the version map associative array
2. Line ~73: Delete `[xinference]="xinference"` from the container name map
3. Line ~805: Remove `"xinference"` from the optional services list
4. Add a xinference orphan cleanup block BEFORE the main update logic (after backup but before pull). Insert near the beginning of the `cmd_update()` function or the update flow:
   ```bash
   # --- Xinference removal cleanup (v2.5) ---
   # Xinference was removed in v2.5. Stop orphan container and remove volume if present.
   if docker ps -a --format '{{.Names}}' 2>/dev/null | grep -q '^agmind-xinference$'; then
       log_info "Removing legacy Xinference container (removed in v2.5)..."
       docker stop agmind-xinference 2>/dev/null || true
       docker rm agmind-xinference 2>/dev/null || true
   fi
   if docker volume ls -q 2>/dev/null | grep -q '^agmind_xinference_data$'; then
       log_info "Removing legacy Xinference volume..."
       docker volume rm agmind_xinference_data 2>/dev/null || true
   fi
   ```

**scripts/uninstall.sh:**
1. Line 101: Remove `_xinference_data` from the grep pattern. Change:
   `grep -E "^agmind_|_openwebui_data$|_ollama_data$|_xinference_data$|_grafana_data$|_portainer_data$|_prometheus_data$|_loki_data$|_authelia_data$"`
   To:
   `grep -E "^agmind_|_openwebui_data$|_ollama_data$|_grafana_data$|_portainer_data$|_prometheus_data$|_loki_data$|_authelia_data$"`

**templates/versions.env:**
1. Line 28: Delete `XINFERENCE_VERSION=v2.3.0`

**templates/release-manifest.json:**
1. Delete the xinference entry block (lines 105-110):
   ```json
   "xinference": {
     ...
   },
   ```
   Ensure valid JSON after deletion (watch for trailing commas).

**COMPONENTS.md:**
1. Line 53: Delete the row `| xinference | XINFERENCE_VERSION | Model serving |`

**COMPATIBILITY.md:**
1. Line 20: Delete the row `| Xinference | v0.16.3 | v0.14.0 | Reranker model serving |`
2. Line 58: Change `"docling, xinference"` to just `"docling"` in the ARM64 note. Update to: `- **ARM64**: Most images support arm64 except sandbox, plugin-daemon, docling.`
  </action>
  <verify>
    <automated>grep -c "xinference\|XINFERENCE" scripts/update.sh scripts/uninstall.sh templates/versions.env COMPONENTS.md COMPATIBILITY.md 2>/dev/null; grep "agmind-xinference" scripts/update.sh | head -2 && echo "cleanup block present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "XINFERENCE_VERSION" templates/versions.env` returns 0
    - `grep "xinference" templates/release-manifest.json` returns no matches
    - `python3 -m json.tool templates/release-manifest.json > /dev/null 2>&1` exits 0 (valid JSON)
    - `grep -c "xinference" COMPONENTS.md` returns 0
    - `grep -c "[Xx]inference" COMPATIBILITY.md` returns 0
    - `grep "xinference_data" scripts/uninstall.sh` returns no matches
    - `grep "agmind-xinference" scripts/update.sh` shows the cleanup block (docker stop/rm)
    - `grep "agmind_xinference_data" scripts/update.sh` shows volume cleanup (docker volume rm)
    - `grep "\[xinference\]" scripts/update.sh` returns no matches (removed from maps)
    - `grep "XINFERENCE" scripts/update.sh` only appears in the cleanup block comments, not in version/container maps
  </acceptance_criteria>
  <done>Xinference removed from all peripheral scripts, configs, and docs. update.sh has orphan cleanup block for existing installations.</done>
</task>

</tasks>

<verification>
- `grep -rn "xinference\|XINFERENCE" scripts/ templates/versions.env templates/release-manifest.json COMPONENTS.md COMPATIBILITY.md` returns only the cleanup block in update.sh
- `grep -rn "xinference\|XINFERENCE" lib/ templates/docker-compose.yml templates/env.*.template` returns 0 matches (verified by Plan 01)
- The only remaining xinference mention in the entire codebase should be: update.sh cleanup block, CHANGELOG.md (historical), and .planning/ files
</verification>

<success_criteria>
- All scripts, config files, and documentation free of Xinference references (except update.sh cleanup and CHANGELOG.md history)
- update.sh has cleanup block that removes orphan agmind-xinference container and agmind_xinference_data volume on update
- release-manifest.json remains valid JSON
- versions.env has no XINFERENCE_VERSION
</success_criteria>

<output>
After completion, create `.planning/phases/20-xinference-removal/20-02-SUMMARY.md`
</output>
