---
phase: 29-docling-gpu-ocr
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docker-compose.yml
  - scripts/build-offline-bundle.sh
autonomous: true
requirements: [DOCL-01, DOCL-04]
must_haves:
  truths:
    - "Docling контейнер использует image из DOCLING_IMAGE переменной .env"
    - "GPU-доступ Docling контейнера управляется через NVIDIA_VISIBLE_DEVICES env var"
    - "OCR_LANG передаётся в контейнер через environment"
    - "Offline bundle поддерживает INCLUDE_DOCLING_CUDA флаг для CUDA-образа"
  artifacts:
    - path: "templates/docker-compose.yml"
      provides: "Docling service with dynamic image and GPU/OCR env vars"
      contains: "image: ${DOCLING_IMAGE}"
    - path: "scripts/build-offline-bundle.sh"
      provides: "INCLUDE_DOCLING_CUDA support for offline bundle"
      contains: "INCLUDE_DOCLING_CUDA"
  key_links:
    - from: "templates/docker-compose.yml"
      to: ".env"
      via: "docker compose reads DOCLING_IMAGE, OCR_LANG, NVIDIA_VISIBLE_DEVICES from .env"
      pattern: "DOCLING_IMAGE"
    - from: "scripts/build-offline-bundle.sh"
      to: "templates/versions.env"
      via: "bundle reads DOCLING_IMAGE_CUDA from versions.env"
      pattern: "DOCLING_IMAGE_CUDA"
---

<objective>
Docker-compose Docling service на динамический image + GPU env + OCR_LANG, offline bundle с CUDA флагом.

Purpose: DOCL-01 (compose использует выбранный образ), DOCL-04 (offline bundle включает CUDA образ по флагу; предзагрузка моделей отложена — per user decision, модели скачиваются при первом запросе).
Output: Обновлённые docker-compose.yml и build-offline-bundle.sh.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docling-gpu-ocr/29-CONTEXT.md
</context>

<interfaces>
<!-- Current docker-compose docling service (lines 512-533) -->
```yaml
  docling:
    <<: *logging-defaults
    image: ghcr.io/docling-project/docling-serve:${DOCLING_SERVE_VERSION:-v1.14.3}
    container_name: agmind-docling
    restart: on-failure:5
    mem_limit: ${DOCLING_MEM_LIMIT:-4g}
    profiles:
      - docling
    expose:
      - "8765"
    command: ["docling-serve", "run", "--port", "8765", "--host", "0.0.0.0"]
    volumes:
      - agmind_docling_cache:/home/docling/.cache
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8765/health')\" 2>/dev/null || curl -sf http://localhost:8765/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend
```

<!-- GPU passthrough pattern used by vLLM/TEI (compose lines 291-297) -->
```yaml
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
```

<!-- enable_gpu_compose() in config.sh handles #__GPU__ uncommenting -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml — Docling service: dynamic image, GPU env, OCR_LANG</name>
  <files>templates/docker-compose.yml</files>
  <read_first>
    - templates/docker-compose.yml
    - .planning/phases/29-docling-gpu-ocr/29-CONTEXT.md
  </read_first>
  <action>
В `templates/docker-compose.yml`, секция `docling:` (строки ~512-533) — заменить на:

```yaml
  docling:
    <<: *logging-defaults
    image: ${DOCLING_IMAGE:-ghcr.io/docling-project/docling-serve:v1.14.3}
    container_name: agmind-docling
    restart: on-failure:5
    mem_limit: ${DOCLING_MEM_LIMIT:-4g}
    profiles:
      - docling
    expose:
      - "8765"
    command: ["docling-serve", "run", "--port", "8765", "--host", "0.0.0.0"]
    environment:
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-}
      - OCR_LANG=${OCR_LANG:-rus,eng}
    volumes:
      - agmind_docling_cache:/home/docling/.cache
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8765/health')\" 2>/dev/null || curl -sf http://localhost:8765/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend
```

Ключевые изменения:
1. `image:` — `${DOCLING_IMAGE:-ghcr.io/docling-project/docling-serve:v1.14.3}` вместо хардкода с `DOCLING_SERVE_VERSION`. Fallback на CPU-образ по умолчанию.
2. Добавлен блок `environment:` с двумя переменными:
   - `NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-}` — пустая строка = CPU mode, "all" = GPU mode. НЕ используем `#__GPU__` паттерн — по user decision GPU-доступ управляется через env var, не через deploy.resources.
   - `OCR_LANG=${OCR_LANG:-rus,eng}` — русский OCR по умолчанию
3. Volume `agmind_docling_cache:/home/docling/.cache` — БЕЗ ИЗМЕНЕНИЙ (DOCL-02: существующий volume достаточен)
4. Остальное (healthcheck, profiles, expose, networks) — без изменений

НЕ добавлять `#__GPU__deploy:` блок для docling — per user decision, GPU управляется через `NVIDIA_VISIBLE_DEVICES` env var, а не через deploy.resources.reservations.
  </action>
  <verify>
    <automated>grep -q 'image: ${DOCLING_IMAGE:-' templates/docker-compose.yml && grep -q 'NVIDIA_VISIBLE_DEVICES' templates/docker-compose.yml && grep -q 'OCR_LANG' templates/docker-compose.yml && ! grep -q 'DOCLING_SERVE_VERSION' templates/docker-compose.yml && grep -q 'agmind_docling_cache:/home/docling/.cache' templates/docker-compose.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml содержит `image: ${DOCLING_IMAGE:-ghcr.io/docling-project/docling-serve:v1.14.3}`
    - docker-compose.yml содержит `NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-}` в environment секции docling
    - docker-compose.yml содержит `OCR_LANG=${OCR_LANG:-rus,eng}` в environment секции docling
    - docker-compose.yml НЕ содержит `DOCLING_SERVE_VERSION` (заменено на DOCLING_IMAGE)
    - docker-compose.yml содержит `agmind_docling_cache:/home/docling/.cache` (volume сохранён)
    - docker-compose.yml НЕ содержит `#__GPU__` блок для docling сервиса
  </acceptance_criteria>
  <done>Docling сервис использует динамический image из .env, GPU через NVIDIA_VISIBLE_DEVICES, OCR_LANG передаётся в контейнер</done>
</task>

<task type="auto">
  <name>Task 2: build-offline-bundle.sh — INCLUDE_DOCLING_CUDA флаг для CUDA-образа</name>
  <files>scripts/build-offline-bundle.sh</files>
  <read_first>
    - scripts/build-offline-bundle.sh
    - templates/versions.env
    - .planning/phases/29-docling-gpu-ocr/29-CONTEXT.md
  </read_first>
  <action>
В `scripts/build-offline-bundle.sh` — добавить поддержку CUDA Docling образа в offline bundle:

1. В начало скрипта (рядом с другими переменными/флагами) добавить парсинг флага:
```bash
INCLUDE_DOCLING_CUDA="${INCLUDE_DOCLING_CUDA:-false}"
```

2. В секцию справки (usage) добавить описание:
```
  INCLUDE_DOCLING_CUDA=true  Include Docling CUDA image in bundle (+5-8 GB)
```

3. Найти место где скрипт выполняет `docker save` или формирует список образов для сохранения. Добавить условный блок:
```bash
# Docling CUDA image (optional, +5-8 GB)
if [[ "${INCLUDE_DOCLING_CUDA}" == "true" ]]; then
    log_info "Including Docling CUDA image: ${DOCLING_IMAGE_CUDA}"
    # Add DOCLING_IMAGE_CUDA to the list of images to save
    # (follow existing pattern for adding images in this script)
fi
```

4. Убедиться что `DOCLING_IMAGE_CPU` (стандартный CPU образ) ВСЕГДА включается в bundle (он уже должен быть через текущий docling image ref, но проверить что ref обновлён с `DOCLING_SERVE_VERSION` на `DOCLING_IMAGE_CPU`).

Важно: следовать существующему паттерну скрипта для docker save/pull. Если скрипт использует массив образов — добавить в массив. Если поочерёдно docker save — добавить аналогичный вызов.
  </action>
  <verify>
    <automated>grep -q "INCLUDE_DOCLING_CUDA" scripts/build-offline-bundle.sh && grep -q "DOCLING_IMAGE_CUDA" scripts/build-offline-bundle.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-offline-bundle.sh содержит `INCLUDE_DOCLING_CUDA="${INCLUDE_DOCLING_CUDA:-false}"`
    - scripts/build-offline-bundle.sh содержит условный блок `if [[ "${INCLUDE_DOCLING_CUDA}" == "true" ]]`
    - scripts/build-offline-bundle.sh содержит ссылку на `DOCLING_IMAGE_CUDA`
    - scripts/build-offline-bundle.sh содержит `DOCLING_IMAGE_CPU` (CPU образ всегда в bundle)
    - `bash -n scripts/build-offline-bundle.sh` проходит без ошибок
  </acceptance_criteria>
  <done>Offline bundle по умолчанию содержит CPU Docling образ; INCLUDE_DOCLING_CUDA=true добавляет CUDA-образ (+5-8 GB)</done>
</task>

</tasks>

<verification>
1. `grep -q 'DOCLING_IMAGE' templates/docker-compose.yml` — dynamic image
2. `grep -q 'OCR_LANG' templates/docker-compose.yml` — OCR lang передаётся
3. `grep -q 'INCLUDE_DOCLING_CUDA' scripts/build-offline-bundle.sh` — offline bundle поддержка
4. `bash -n templates/docker-compose.yml 2>/dev/null; docker compose -f templates/docker-compose.yml config --quiet 2>/dev/null || true` — compose syntax (best effort)
5. `bash -n scripts/build-offline-bundle.sh` — shell syntax valid
</verification>

<success_criteria>
- Docling сервис в compose использует ${DOCLING_IMAGE} вместо хардкода
- NVIDIA_VISIBLE_DEVICES и OCR_LANG передаются в контейнер
- Volume agmind_docling_cache сохранён без изменений
- Offline bundle включает CPU образ по умолчанию, CUDA по флагу
- bash -n проходит для всех модифицированных файлов
</success_criteria>

<output>
After completion, create `.planning/phases/29-docling-gpu-ocr/29-02-SUMMARY.md`
</output>
