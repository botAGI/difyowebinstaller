---
phase: 29-docling-gpu-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wizard.sh
  - lib/config.sh
  - templates/versions.env
  - templates/env.lan.template
  - templates/env.vpn.template
  - templates/env.vps.template
  - templates/env.offline.template
autonomous: true
requirements: [DOCL-01, DOCL-02, DOCL-03]
must_haves:
  truths:
    - "Wizard предлагает тройной выбор Docling: Нет / CPU / GPU(CUDA)"
    - "GPU-пункт скрыт если DETECTED_GPU != nvidia"
    - "DOCLING_IMAGE в .env содержит полный image:tag"
    - "OCR_LANG=rus,eng записывается в .env по умолчанию"
    - "Existing agmind_docling_cache volume остаётся единственным volume"
  artifacts:
    - path: "lib/wizard.sh"
      provides: "Triple Docling choice with GPU detection"
      contains: "DOCLING_IMAGE"
    - path: "lib/config.sh"
      provides: "Sed replacements for __DOCLING_IMAGE__ and __OCR_LANG__"
      contains: "__DOCLING_IMAGE__"
    - path: "templates/versions.env"
      provides: "DOCLING_IMAGE_CPU and DOCLING_IMAGE_CUDA image refs"
      contains: "DOCLING_IMAGE_CPU"
    - path: "templates/env.lan.template"
      provides: "DOCLING_IMAGE and OCR_LANG placeholders"
      contains: "DOCLING_IMAGE=__DOCLING_IMAGE__"
  key_links:
    - from: "lib/wizard.sh"
      to: "lib/config.sh"
      via: "export DOCLING_IMAGE → sed __DOCLING_IMAGE__"
      pattern: "export.*DOCLING_IMAGE"
    - from: "templates/versions.env"
      to: "lib/wizard.sh"
      via: "wizard reads DOCLING_IMAGE_CPU/CUDA from versions.env"
      pattern: "DOCLING_IMAGE_CPU"
    - from: "lib/config.sh"
      to: "templates/env.*.template"
      via: "sed replaces __DOCLING_IMAGE__ and __OCR_LANG__"
      pattern: "__DOCLING_IMAGE__"
---

<objective>
Wizard GPU/CPU выбор Docling + env pipeline: wizard предлагает тройной выбор (Нет/CPU/GPU), GPU скрыт если nvidia runtime не обнаружен, DOCLING_IMAGE и OCR_LANG пишутся в .env через стандартный sed-pipeline.

Purpose: DOCL-01 (wizard выбор), DOCL-02 (volume уже работает, не ломаем), DOCL-03 (русский OCR по умолчанию).
Output: Обновлённые wizard.sh, config.sh, versions.env, все 4 env шаблона.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docling-gpu-ocr/29-CONTEXT.md
</context>

<interfaces>
<!-- Existing patterns executor must follow -->

From lib/detect.sh (GPU detection, already exists):
```bash
DETECTED_GPU="nvidia"  # or "none", "amd", "intel", "apple"
DETECTED_GPU_NAME="NVIDIA GeForce RTX 4090"
DETECTED_GPU_VRAM="24564"
```

From lib/wizard.sh (export pattern, line 1102):
```bash
export DEPLOY_PROFILE DOMAIN CERTBOT_EMAIL VECTOR_STORE ENABLE_DOCLING
```

From lib/config.sh (_generate_env_file sed pattern, lines 261-295):
```bash
sed \
    -e "s|__SECRET_KEY__|${_SECRET_KEY}|g" \
    ...
    "$env_file" > "$env_tmp"
```

From templates/versions.env (image version pattern):
```
DOCLING_SERVE_VERSION=v1.14.3
```

From templates/env.lan.template (current Docling vars):
```
ENABLE_DOCLING=false
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: versions.env + env templates — добавить DOCLING_IMAGE_CPU/CUDA, OCR_LANG, DOCLING_IMAGE placeholder</name>
  <files>templates/versions.env, templates/env.lan.template, templates/env.vpn.template, templates/env.vps.template, templates/env.offline.template</files>
  <read_first>
    - templates/versions.env
    - templates/env.lan.template
    - templates/env.vpn.template
    - templates/env.vps.template
    - templates/env.offline.template
  </read_first>
  <action>
1. В `templates/versions.env` — рядом с существующей строкой `DOCLING_SERVE_VERSION=v1.14.3` добавить:
```
DOCLING_IMAGE_CPU=ghcr.io/docling-project/docling-serve:v1.14.3
DOCLING_IMAGE_CUDA=quay.io/docling-project/docling-serve-cu128:v1.14.3
```
Удалить строку `DOCLING_SERVE_VERSION=v1.14.3` — она заменяется двумя конкретными image:tag.

2. В каждом из 4 env шаблонов (`env.lan.template`, `env.vpn.template`, `env.vps.template`, `env.offline.template`) — рядом с существующей строкой `ENABLE_DOCLING=false` добавить ниже:
```
DOCLING_IMAGE=__DOCLING_IMAGE__
OCR_LANG=__OCR_LANG__
NVIDIA_VISIBLE_DEVICES=__NVIDIA_VISIBLE_DEVICES__
```
Строка `ENABLE_DOCLING=false` остаётся (она нужна для compose profiles).
  </action>
  <verify>
    <automated>grep -q "DOCLING_IMAGE_CPU=ghcr.io/docling-project/docling-serve:v1.14.3" templates/versions.env && grep -q "DOCLING_IMAGE_CUDA=quay.io/docling-project/docling-serve-cu128:v1.14.3" templates/versions.env && grep -q "DOCLING_IMAGE=__DOCLING_IMAGE__" templates/env.lan.template && grep -q "OCR_LANG=__OCR_LANG__" templates/env.lan.template && grep -q "NVIDIA_VISIBLE_DEVICES=__NVIDIA_VISIBLE_DEVICES__" templates/env.lan.template && grep -q "DOCLING_IMAGE=__DOCLING_IMAGE__" templates/env.vpn.template && grep -q "DOCLING_IMAGE=__DOCLING_IMAGE__" templates/env.vps.template && grep -q "DOCLING_IMAGE=__DOCLING_IMAGE__" templates/env.offline.template && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/versions.env содержит `DOCLING_IMAGE_CPU=ghcr.io/docling-project/docling-serve:v1.14.3`
    - templates/versions.env содержит `DOCLING_IMAGE_CUDA=quay.io/docling-project/docling-serve-cu128:v1.14.3`
    - templates/versions.env НЕ содержит `DOCLING_SERVE_VERSION=`
    - Все 4 env шаблона содержат `DOCLING_IMAGE=__DOCLING_IMAGE__`
    - Все 4 env шаблона содержат `OCR_LANG=__OCR_LANG__`
    - Все 4 env шаблона содержат `NVIDIA_VISIBLE_DEVICES=__NVIDIA_VISIBLE_DEVICES__`
    - Все 4 env шаблона по-прежнему содержат `ENABLE_DOCLING=false`
  </acceptance_criteria>
  <done>versions.env имеет два Docling image ref, все env шаблоны имеют placeholders DOCLING_IMAGE, OCR_LANG, NVIDIA_VISIBLE_DEVICES</done>
</task>

<task type="auto">
  <name>Task 2: wizard.sh — тройной выбор Docling (Нет/CPU/GPU) с GPU-детектом + config.sh sed-замены</name>
  <files>lib/wizard.sh, lib/config.sh</files>
  <read_first>
    - lib/wizard.sh
    - lib/config.sh
    - lib/detect.sh
    - templates/versions.env
  </read_first>
  <action>
1. В `lib/wizard.sh` — заменить функцию `_wizard_etl()` (строки ~196-213). Новая логика:

```bash
_wizard_etl() {
    if [[ "$DEPLOY_PROFILE" == "offline" ]]; then
        ENABLE_DOCLING="false"
        DOCLING_IMAGE=""
        OCR_LANG="rus,eng"
        NVIDIA_VISIBLE_DEVICES=""
        return 0
    fi

    # Detect nvidia container runtime availability
    local has_nvidia_runtime="false"
    if [[ "${DETECTED_GPU:-none}" == "nvidia" ]]; then
        if docker info 2>/dev/null | grep -qi "nvidia"; then
            has_nvidia_runtime="true"
        fi
    fi

    echo "Расширенная обработка документов (Docling)?"
    echo "  1) Нет — стандартный Dify ETL (по умолчанию)"
    echo "  2) Да — Docling CPU"
    if [[ "$has_nvidia_runtime" == "true" ]]; then
        echo "  3) Да — Docling GPU (CUDA)"
    fi
    echo ""

    local max_choice=2
    [[ "$has_nvidia_runtime" == "true" ]] && max_choice=3

    _ask_choice "Выбор [1-${max_choice}, Enter=1]: " 1 "$max_choice" 1
    case "$REPLY" in
        2)
            ENABLE_DOCLING="true"
            DOCLING_IMAGE="${DOCLING_IMAGE_CPU}"
            NVIDIA_VISIBLE_DEVICES=""
            ;;
        3)
            ENABLE_DOCLING="true"
            DOCLING_IMAGE="${DOCLING_IMAGE_CUDA}"
            NVIDIA_VISIBLE_DEVICES="all"
            ;;
        *)
            ENABLE_DOCLING="false"
            DOCLING_IMAGE=""
            NVIDIA_VISIBLE_DEVICES=""
            ;;
    esac
    OCR_LANG="rus,eng"
    echo ""
}
```

Ключевые моменты:
- `DOCLING_IMAGE_CPU` и `DOCLING_IMAGE_CUDA` берутся из `versions.env` (который sourced в install.sh)
- GPU-пункт (3) СКРЫТ если `has_nvidia_runtime=false` — пользователь его не видит
- `NVIDIA_VISIBLE_DEVICES="all"` только для GPU-выбора; пустая строка для CPU/нет
- `OCR_LANG="rus,eng"` всегда устанавливается (DOCL-03)
- Offline профиль — Docling отключён (без изменений)

2. В `lib/wizard.sh` — в блоке export (строка ~1102) добавить:
```bash
export DOCLING_IMAGE OCR_LANG NVIDIA_VISIBLE_DEVICES
```
рядом с существующим `export ... ENABLE_DOCLING`.

3. В `lib/wizard.sh` — в начале файла (строка ~28, рядом с `ENABLE_DOCLING="${ENABLE_DOCLING:-...}"`) добавить дефолты:
```bash
DOCLING_IMAGE="${DOCLING_IMAGE:-}"
OCR_LANG="${OCR_LANG:-rus,eng}"
NVIDIA_VISIBLE_DEVICES="${NVIDIA_VISIBLE_DEVICES:-}"
```

4. В `lib/wizard.sh` — в функции сводки (строка ~1001, `[[ "$ENABLE_DOCLING" == "true" ]]`) добавить тип:
```bash
[[ "$ENABLE_DOCLING" == "true" ]] && echo "  ETL:          Docling (${DOCLING_IMAGE##*/})"
```
Это покажет `docling-serve:v1.14.3` или `docling-serve-cu128:v1.14.3` — полезно для пользователя.

5. В `lib/config.sh` — в функции `_generate_env_file()` (блок sed, строки ~261-295) добавить 3 новые замены перед закрывающим `"$env_file" > "$env_tmp"`:
```bash
        -e "s|__DOCLING_IMAGE__|${DOCLING_IMAGE:-}|g" \
        -e "s|__OCR_LANG__|${OCR_LANG:-rus,eng}|g" \
        -e "s|__NVIDIA_VISIBLE_DEVICES__|${NVIDIA_VISIBLE_DEVICES:-}|g" \
```

6. В `lib/config.sh` — в NON_INTERACTIVE блоке (если есть) добавить аналогичные дефолты для `DOCLING_IMAGE`, `OCR_LANG`.
  </action>
  <verify>
    <automated>grep -q "DOCLING_IMAGE_CPU" lib/wizard.sh && grep -q "DOCLING_IMAGE_CUDA" lib/wizard.sh && grep -q "has_nvidia_runtime" lib/wizard.sh && grep -q "OCR_LANG=" lib/wizard.sh && grep -q "export.*DOCLING_IMAGE.*OCR_LANG" lib/wizard.sh && grep -q "__DOCLING_IMAGE__" lib/config.sh && grep -q "__OCR_LANG__" lib/config.sh && grep -q "__NVIDIA_VISIBLE_DEVICES__" lib/config.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/wizard.sh содержит `has_nvidia_runtime` переменную для проверки nvidia runtime
    - lib/wizard.sh содержит `DOCLING_IMAGE="${DOCLING_IMAGE_CPU}"` для CPU-выбора
    - lib/wizard.sh содержит `DOCLING_IMAGE="${DOCLING_IMAGE_CUDA}"` для GPU-выбора
    - lib/wizard.sh содержит `NVIDIA_VISIBLE_DEVICES="all"` только в case 3
    - lib/wizard.sh содержит `OCR_LANG="rus,eng"` установку
    - lib/wizard.sh содержит `export DOCLING_IMAGE OCR_LANG NVIDIA_VISIBLE_DEVICES`
    - lib/config.sh содержит `-e "s|__DOCLING_IMAGE__` sed-замену
    - lib/config.sh содержит `-e "s|__OCR_LANG__` sed-замену
    - lib/config.sh содержит `-e "s|__NVIDIA_VISIBLE_DEVICES__` sed-замену
    - Функция _wizard_etl показывает пункт 3 (GPU) ТОЛЬКО когда has_nvidia_runtime=true
  </acceptance_criteria>
  <done>Wizard предлагает 3 варианта Docling, GPU скрыт без nvidia runtime, DOCLING_IMAGE/OCR_LANG/NVIDIA_VISIBLE_DEVICES экспортируются и подставляются в .env</done>
</task>

</tasks>

<verification>
1. `grep -q "DOCLING_IMAGE_CPU" templates/versions.env` — образы определены
2. `grep -q "__DOCLING_IMAGE__" templates/env.lan.template` — placeholder в шаблонах
3. `grep -q "has_nvidia_runtime" lib/wizard.sh` — GPU-детект в wizard
4. `grep -q "__DOCLING_IMAGE__" lib/config.sh` — sed-замена в config
5. `bash -n lib/wizard.sh && bash -n lib/config.sh` — синтаксис валиден
</verification>

<success_criteria>
- Wizard показывает тройной выбор (1-Нет, 2-CPU, 3-GPU), GPU скрыт без nvidia runtime
- DOCLING_IMAGE содержит полный image:tag из versions.env
- OCR_LANG=rus,eng записывается в .env по умолчанию
- Все 4 env шаблона имеют placeholders
- config.sh подставляет значения через sed
- bash -n проходит для всех модифицированных .sh файлов
</success_criteria>

<output>
After completion, create `.planning/phases/29-docling-gpu-ocr/29-01-SUMMARY.md`
</output>
