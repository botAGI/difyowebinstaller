---
phase: 08-health-verification-ux-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/security.sh
  - LICENSE
autonomous: true
requirements:
  - UXPL-01
  - UXPL-03

must_haves:
  truths:
    - "When installer would disable SSH PasswordAuthentication, a prominent WARNING is printed first with SSH key setup instructions"
    - "SSH hardening asks for confirmation before making the change (unless --non-interactive)"
    - "LICENSE file exists in repo root with Apache 2.0 text"
    - "LICENSE copyright line says AGMind Contributors"
  artifacts:
    - path: "lib/security.sh"
      provides: "SSH hardening function with warning and key instructions"
      contains: "harden_ssh"
    - path: "LICENSE"
      provides: "Apache 2.0 license file"
      contains: "Apache License"
  key_links:
    - from: "lib/security.sh:setup_security()"
      to: "lib/security.sh:harden_ssh()"
      via: "function call in setup_security chain"
      pattern: "harden_ssh"
    - from: "lib/security.sh:harden_ssh()"
      to: "/etc/ssh/sshd_config"
      via: "sed command to disable PasswordAuthentication"
      pattern: "PasswordAuthentication"
---

<objective>
Add SSH lockout prevention with prominent warning and key setup instructions before disabling password auth. Create Apache 2.0 LICENSE file for public release.

Purpose: Operators can lose SSH access if PasswordAuthentication is disabled without a key configured. A warning with instructions prevents lockout. LICENSE is required for public release on GitHub.

Output: lib/security.sh with harden_ssh() function, LICENSE file in repo root.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-health-verification-ux-polish/08-CONTEXT.md

<interfaces>
<!-- Key functions and patterns the executor needs -->

From lib/security.sh (existing):
```bash
setup_security()          # Main entry: calls configure_ufw, configure_fail2ban, harden_docker_compose, encrypt_secrets
configure_ufw()           # UFW firewall setup (guarded by ENABLE_UFW)
configure_fail2ban()      # fail2ban SSH jail (guarded by ENABLE_FAIL2BAN)
harden_docker_compose()   # Docker no-new-privileges (guarded by SKIP_DOCKER_HARDENING)
encrypt_secrets()         # SOPS/age encryption (guarded by ENABLE_SOPS)
# Pattern: each function checks guard variable, returns 0 if disabled
# Colors/logging: log_info, log_success, log_warn, log_error from common.sh
```

From install.sh (context):
```bash
# NON_INTERACTIVE variable controls whether prompts are shown
# NON_INTERACTIVE=true skips interactive prompts
# lib/security.sh is sourced at line 30
# setup_security() is called inside phase_config() at line 129
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add harden_ssh() with lockout prevention to lib/security.sh</name>
  <files>lib/security.sh</files>
  <read_first>
    - lib/security.sh (full file — understand guard variable pattern, function structure, setup_security chain)
    - install.sh lines 30-50 (NON_INTERACTIVE variable, how security.sh is sourced)
    - install.sh lines 125-135 (phase_config calls setup_security)
  </read_first>
  <action>
Add `harden_ssh()` function to `lib/security.sh`. Insert it AFTER `configure_fail2ban()` (after line 89) and BEFORE `encrypt_secrets()`.

Then add `harden_ssh` call to `setup_security()` — insert after `configure_fail2ban` and before `harden_docker_compose`.

**The harden_ssh() function:**

```bash
# ============================================================================
# SSH HARDENING
# ============================================================================

harden_ssh() {
    if [[ "${ENABLE_SSH_HARDENING:-true}" == "false" ]]; then return 0; fi

    local sshd_config="/etc/ssh/sshd_config"
    [[ -f "$sshd_config" ]] || { log_warn "sshd_config not found — skipping SSH hardening"; return 0; }

    # Check if PasswordAuthentication is already disabled
    if grep -qE '^\s*PasswordAuthentication\s+no' "$sshd_config" 2>/dev/null; then
        log_info "SSH PasswordAuthentication already disabled"
        return 0
    fi

    # Check if any SSH keys are authorized for current login user
    local login_user
    login_user="$(logname 2>/dev/null || echo "${SUDO_USER:-root}")"
    local user_home
    user_home="$(eval echo "~${login_user}" 2>/dev/null || echo "/root")"
    local has_keys=false
    if [[ -f "${user_home}/.ssh/authorized_keys" ]] && [[ -s "${user_home}/.ssh/authorized_keys" ]]; then
        has_keys=true
    fi

    # ====== PROMINENT WARNING ======
    echo ""
    echo -e "${RED}${BOLD}  ╔════════════════════════════════════════════════════════════╗${NC}"
    echo -e "${RED}${BOLD}  ║           ВНИМАНИЕ: ОТКЛЮЧЕНИЕ SSH ПАРОЛЕЙ                ║${NC}"
    echo -e "${RED}${BOLD}  ╚════════════════════════════════════════════════════════════╝${NC}"
    echo ""
    echo -e "  Сейчас будет отключён вход по паролю через SSH."
    echo -e "  ${RED}Если у вас нет SSH-ключа — вы потеряете удалённый доступ!${NC}"
    echo ""

    if [[ "$has_keys" == "true" ]]; then
        echo -e "  ${GREEN}[OK] SSH-ключ найден:${NC} ${user_home}/.ssh/authorized_keys"
    else
        echo -e "  ${RED}[!!] SSH-ключ НЕ НАЙДЕН${NC} для пользователя ${login_user}"
        echo ""
        echo -e "  ${BOLD}Как настроить SSH-ключ (выполните на ЛОКАЛЬНОЙ машине):${NC}"
        echo ""
        echo -e "  ${CYAN}1. Сгенерируйте ключ (если нет):${NC}"
        echo -e "     ssh-keygen -t ed25519 -C \"your_email@example.com\""
        echo ""
        echo -e "  ${CYAN}2. Скопируйте ключ на сервер:${NC}"
        echo -e "     ssh-copy-id ${login_user}@$(hostname -I 2>/dev/null | awk '{print $1}' || echo 'SERVER_IP')"
        echo ""
        echo -e "  ${CYAN}3. Проверьте вход по ключу (в новом терминале!):${NC}"
        echo -e "     ssh ${login_user}@$(hostname -I 2>/dev/null | awk '{print $1}' || echo 'SERVER_IP')"
        echo ""
    fi

    # Ask confirmation (unless non-interactive)
    if [[ "${NON_INTERACTIVE:-false}" != "true" ]]; then
        local answer
        echo -e "  Отключить вход по паролю? (yes/no): \c"
        read -r answer
        if [[ "$answer" != "yes" && "$answer" != "y" ]]; then
            log_warn "SSH hardening пропущен по выбору пользователя"
            return 0
        fi
    else
        if [[ "$has_keys" != "true" ]]; then
            log_warn "Non-interactive: SSH-ключ не найден, пропускаем отключение паролей"
            return 0
        fi
        log_info "Non-interactive: SSH-ключ найден, отключаем пароли"
    fi

    # Backup sshd_config
    cp "$sshd_config" "${sshd_config}.bak.$(date +%s)"

    # Disable PasswordAuthentication
    if grep -qE '^\s*#?\s*PasswordAuthentication' "$sshd_config"; then
        sed -i 's/^\s*#\?\s*PasswordAuthentication.*/PasswordAuthentication no/' "$sshd_config"
    else
        echo "PasswordAuthentication no" >> "$sshd_config"
    fi

    # Also disable ChallengeResponseAuthentication (prevents password fallback)
    if grep -qE '^\s*#?\s*ChallengeResponseAuthentication' "$sshd_config"; then
        sed -i 's/^\s*#\?\s*ChallengeResponseAuthentication.*/ChallengeResponseAuthentication no/' "$sshd_config"
    fi

    # Reload sshd (do NOT restart to keep existing sessions alive)
    if systemctl is-active sshd >/dev/null 2>&1; then
        systemctl reload sshd
    elif systemctl is-active ssh >/dev/null 2>&1; then
        systemctl reload ssh
    fi

    log_success "SSH: PasswordAuthentication отключён"
    echo -e "  ${YELLOW}Резервная копия: ${sshd_config}.bak.*${NC}"
    echo ""
}
```

**Update setup_security():**

Change the `setup_security()` function from:
```bash
setup_security() {
    log_info "Setting up security..."
    configure_ufw
    configure_fail2ban
    harden_docker_compose
    encrypt_secrets
    echo ""
}
```
To:
```bash
setup_security() {
    log_info "Setting up security..."
    configure_ufw
    configure_fail2ban
    harden_ssh
    harden_docker_compose
    encrypt_secrets
    echo ""
}
```

**Key design decisions:**
- Default `ENABLE_SSH_HARDENING=true` (opt-out, not opt-in) — SSH hardening is a security best practice
- In non-interactive mode: only disable passwords if SSH key is found (prevents lockout on automated installs)
- `systemctl reload` not `restart` — keeps existing SSH sessions alive
- Backup with timestamp before modification
- ChallengeResponseAuthentication also disabled to prevent password fallback
- `logname` / `SUDO_USER` to detect the real login user (not root running sudo)
  </action>
  <verify>
    <automated>bash -n lib/security.sh && grep -c "harden_ssh" lib/security.sh && grep "PasswordAuthentication" lib/security.sh | head -3 && grep "ssh-keygen\|ssh-copy-id" lib/security.sh && grep "ВНИМАНИЕ\|ОТКЛЮЧЕНИЕ" lib/security.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - lib/security.sh contains `harden_ssh()` function definition
    - lib/security.sh `setup_security()` contains `harden_ssh` call (between configure_fail2ban and harden_docker_compose)
    - lib/security.sh contains `ENABLE_SSH_HARDENING` guard variable check
    - lib/security.sh contains prominent WARNING banner with `ВНИМАНИЕ` and `ОТКЛЮЧЕНИЕ SSH ПАРОЛЕЙ`
    - lib/security.sh contains SSH key detection via `authorized_keys` check
    - lib/security.sh contains `ssh-keygen -t ed25519` instruction
    - lib/security.sh contains `ssh-copy-id` instruction
    - lib/security.sh contains confirmation prompt `Отключить вход по паролю? (yes/no)`
    - lib/security.sh contains NON_INTERACTIVE guard (skips if no key in non-interactive mode)
    - lib/security.sh contains `sshd_config.bak` backup before modification
    - lib/security.sh contains `sed` command to set `PasswordAuthentication no`
    - lib/security.sh contains `systemctl reload sshd` (reload, not restart)
    - `bash -n lib/security.sh` exits 0
  </acceptance_criteria>
  <done>SSH hardening function with prominent warning, key detection, setup instructions, confirmation prompt, and safe non-interactive behavior exists in lib/security.sh and is called from setup_security()</done>
</task>

<task type="auto">
  <name>Task 2: Create Apache 2.0 LICENSE file</name>
  <files>LICENSE</files>
  <read_first>
    - (no existing file — this is a new file)
  </read_first>
  <action>
Create `LICENSE` file in the repository root with the standard Apache License 2.0 text.

The copyright line must read:
```
Copyright 2024-2026 AGMind Contributors
```

Use the exact standard Apache 2.0 text from https://www.apache.org/licenses/LICENSE-2.0.txt — the full text including all sections (TERMS AND CONDITIONS, Definitions, Grant of Copyright License, Grant of Patent License, Redistribution, Submission of Contributions, Trademarks, Disclaimer of Warranty, Limitation of Liability, Accepting Warranty or Additional Liability).

The file must start with:
```
                                 Apache License
                           Version 2.0, January 2004
                        http://www.apache.org/licenses/
```

And end with the standard APPENDIX section including the boilerplate notice template.
  </action>
  <verify>
    <automated>head -3 LICENSE && grep "Apache License" LICENSE && grep "AGMind Contributors" LICENSE && grep "Version 2.0" LICENSE && wc -l LICENSE | awk '{print ($1 > 150) ? "PASS" : "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE file exists in repo root
    - LICENSE contains "Apache License" on first non-empty line
    - LICENSE contains "Version 2.0, January 2004"
    - LICENSE contains "Copyright 2024-2026 AGMind Contributors"
    - LICENSE contains "TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION"
    - LICENSE contains "WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND"
    - LICENSE file is longer than 150 lines (full Apache 2.0 text)
  </acceptance_criteria>
  <done>Apache 2.0 LICENSE file exists in repo root with AGMind Contributors copyright</done>
</task>

</tasks>

<verification>
1. `bash -n lib/security.sh` — syntax valid
2. `grep "harden_ssh" lib/security.sh` — function defined and called in setup_security
3. `grep "PasswordAuthentication" lib/security.sh` — SSH hardening present
4. `grep "ВНИМАНИЕ" lib/security.sh` — prominent warning present
5. `grep "ssh-keygen\|ssh-copy-id" lib/security.sh` — key setup instructions present
6. `head -3 LICENSE` — Apache License header
7. `grep "AGMind Contributors" LICENSE` — correct copyright
</verification>

<success_criteria>
- SSH hardening function shows prominent warning before disabling PasswordAuthentication
- Warning includes SSH key detection, setup instructions, and confirmation prompt
- Non-interactive mode safely skips if no SSH key found
- sshd_config is backed up before modification
- sshd is reloaded (not restarted) to keep existing sessions
- Apache 2.0 LICENSE file exists in repo root with correct copyright
</success_criteria>

<output>
After completion, create `.planning/phases/08-health-verification-ux-polish/08-03-SUMMARY.md`
</output>
