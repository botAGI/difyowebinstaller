---
phase: 06-v3-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docker-compose.yml
  - templates/init-dify-plugin-db.sql
  - scripts/redis-lock-cleanup.sh
autonomous: true
requirements: [STAB-01, STAB-02]

must_haves:
  truths:
    - "plugin-daemon does not start until dify_plugin database exists in PostgreSQL"
    - "PostgreSQL healthcheck verifies dify_plugin DB presence, not just pg_isready"
    - "Fresh installs create dify_plugin via init SQL script automatically"
    - "Redis stale locks are cleaned before plugin-daemon starts"
    - "Init-container uses same redis:7-alpine image (no new pull for offline profile)"
  artifacts:
    - path: "templates/init-dify-plugin-db.sql"
      provides: "SQL init script for PostgreSQL entrypoint"
      contains: "CREATE DATABASE"
    - path: "scripts/redis-lock-cleanup.sh"
      provides: "Redis lock cleanup script for init-container"
      contains: "redis-cli"
    - path: "templates/docker-compose.yml"
      provides: "Enhanced db healthcheck, redis-lock-cleaner service, plugin_daemon depends_on chain"
      contains: "redis-lock-cleaner"
  key_links:
    - from: "templates/docker-compose.yml (db healthcheck)"
      to: "dify_plugin database"
      via: "psql SELECT 1 FROM pg_database"
      pattern: "pg_database.*dify_plugin"
    - from: "templates/docker-compose.yml (redis-lock-cleaner)"
      to: "redis service"
      via: "depends_on redis service_healthy"
      pattern: "redis-lock-cleaner"
    - from: "templates/docker-compose.yml (plugin_daemon)"
      to: "redis-lock-cleaner + db"
      via: "depends_on service_completed_successfully + service_healthy"
      pattern: "service_completed_successfully"
---

<objective>
Make plugin-daemon startup reliable: PostgreSQL must have dify_plugin DB ready before plugin-daemon starts (STAB-01), and stale Redis locks from crashed sessions are cleaned automatically (STAB-02).

Purpose: Eliminate the two most common "second startup" and "fresh install" failures — plugin-daemon connecting to a non-existent database, and stale Redis locks blocking initialization.

Output: Enhanced docker-compose.yml with init SQL, improved healthcheck, redis-lock-cleaner init-container, and updated dependency chain.
</objective>

<execution_context>
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/workflows/execute-plan.md
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-v3-bugfixes/06-CONTEXT.md

<interfaces>
<!-- Current PostgreSQL service (lines 372-410 in docker-compose.yml) -->
db:
  image: postgres:${POSTGRES_VERSION:-16-alpine}
  container_name: agmind-db
  environment:
    PGUSER: ${DB_USERNAME:-postgres}
    POSTGRES_PASSWORD: ${DB_PASSWORD:-}
    POSTGRES_DB: ${DB_DATABASE:-dify}
  healthcheck:
    test: ["CMD", "pg_isready", "-U", "${DB_USERNAME:-postgres}"]
    interval: 10s / timeout: 5s / retries: 5 / start_period: 30s

<!-- Current plugin_daemon depends_on (lines 683-688) -->
plugin_daemon:
  depends_on:
    db: condition: service_healthy
    redis: condition: service_healthy

<!-- Current Redis service (lines 412-438) -->
redis:
  image: redis:${REDIS_VERSION:-7.4.1-alpine}
  container_name: agmind-redis
  healthcheck:
    test: ["CMD-SHELL", "REDISCLI_AUTH='${REDIS_PASSWORD:-}' redis-cli ping"]

<!-- Redis ACL (config.sh line 355-356) -->
user default on >PASSWORD ~* &* +@all -@dangerous
<!-- SCAN is in @generic (NOT @dangerous), so it works with current ACL -->

<!-- create_plugin_db() fallback in compose.sh (lines 286-321) — stays as-is -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostgreSQL init SQL and Redis cleanup scripts</name>
  <files>templates/init-dify-plugin-db.sql, scripts/redis-lock-cleanup.sh</files>
  <read_first>
    - templates/docker-compose.yml (lines 372-410 for db service, lines 608-689 for plugin_daemon)
    - lib/compose.sh (lines 286-321 for create_plugin_db pattern)
    - lib/config.sh (lines 349-370 for Redis ACL — confirms SCAN/DEL allowed)
  </read_first>
  <action>
1. Create `templates/init-dify-plugin-db.sql`:
```sql
-- AGMind: Create dify_plugin database for plugin-daemon
-- Mounted into /docker-entrypoint-initdb.d/ — runs on first DB init only
-- For existing volumes, the enhanced healthcheck + create_plugin_db() fallback handles it

SELECT 'CREATE DATABASE dify_plugin'
WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'dify_plugin')\gexec
```
This uses PostgreSQL's `\gexec` to conditionally create the database. The `/docker-entrypoint-initdb.d/` scripts run ONLY on first init (empty data directory), so this handles fresh installs. Existing volumes rely on `create_plugin_db()` fallback in compose.sh.

2. Create `scripts/redis-lock-cleanup.sh`:
```bash
#!/usr/bin/env bash
# Redis stale lock cleanup — runs as init-container before plugin-daemon
# Deletes all plugin_daemon lock keys unconditionally (any lock present at
# startup is guaranteed stale since the daemon hasn't started yet)
set -euo pipefail

REDIS_HOST="${REDIS_HOST:-redis}"
REDIS_PORT="${REDIS_PORT:-6379}"
REDIS_PASSWORD="${REDIS_PASSWORD:-}"

AUTH_ARGS=()
if [[ -n "$REDIS_PASSWORD" ]]; then
    AUTH_ARGS=(-a "$REDIS_PASSWORD" --no-auth-warning)
fi

echo "Scanning for stale plugin_daemon locks..."
cursor=0
deleted=0
while true; do
    result=$(redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" "${AUTH_ARGS[@]}" \
        SCAN "$cursor" MATCH 'plugin_daemon:*lock*' COUNT 100)
    cursor=$(echo "$result" | head -1)
    keys=$(echo "$result" | tail -n +2)
    for key in $keys; do
        redis-cli -h "$REDIS_HOST" -p "$REDIS_PORT" "${AUTH_ARGS[@]}" DEL "$key" >/dev/null
        echo "  Deleted stale lock: $key"
        deleted=$((deleted + 1))
    done
    [[ "$cursor" == "0" ]] && break
done

if [[ $deleted -gt 0 ]]; then
    echo "Cleaned $deleted stale lock(s)"
else
    echo "No stale locks found"
fi
echo "Lock cleanup complete"
```
Make the script executable: `chmod +x scripts/redis-lock-cleanup.sh`
  </action>
  <verify>
    <automated>bash -c "test -f templates/init-dify-plugin-db.sql && grep -q 'dify_plugin' templates/init-dify-plugin-db.sql && test -f scripts/redis-lock-cleanup.sh && grep -q 'plugin_daemon.*lock' scripts/redis-lock-cleanup.sh && test -x scripts/redis-lock-cleanup.sh && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/init-dify-plugin-db.sql contains `CREATE DATABASE dify_plugin`
    - templates/init-dify-plugin-db.sql contains `\gexec`
    - scripts/redis-lock-cleanup.sh contains `SCAN "$cursor" MATCH 'plugin_daemon:*lock*'`
    - scripts/redis-lock-cleanup.sh contains `DEL "$key"`
    - scripts/redis-lock-cleanup.sh has executable permission
    - scripts/redis-lock-cleanup.sh passes `bash -n scripts/redis-lock-cleanup.sh` (syntax check)
  </acceptance_criteria>
  <done>Both files exist, are syntactically valid, and contain the correct patterns for DB init and lock cleanup</done>
</task>

<task type="auto">
  <name>Task 2: Enhance docker-compose.yml with init SQL mount, healthcheck, init-container, and dependency chain</name>
  <files>templates/docker-compose.yml</files>
  <read_first>
    - templates/docker-compose.yml (full file — db service lines 372-410, redis lines 412-438, plugin_daemon lines 608-689, volumes section lines 1016-1029)
    - templates/init-dify-plugin-db.sql (just created in Task 1)
    - scripts/redis-lock-cleanup.sh (just created in Task 1)
  </read_first>
  <action>
Make four changes to `templates/docker-compose.yml`:

**Change 1: Mount init SQL into PostgreSQL (db service, after line 402)**
Add volume mount for init script. Current volumes section:
```yaml
    volumes:
      - ./volumes/db/data:/var/lib/postgresql/data
```
Change to:
```yaml
    volumes:
      - ./volumes/db/data:/var/lib/postgresql/data
      - ../templates/init-dify-plugin-db.sql:/docker-entrypoint-initdb.d/01-create-plugin-db.sql:ro
```
Note: docker-compose runs from `INSTALL_DIR/docker/`, templates are at `INSTALL_DIR/templates/`, so relative path is `../templates/`.

**Change 2: Enhance PostgreSQL healthcheck (db service, line 404)**
Replace current simple healthcheck:
```yaml
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${DB_USERNAME:-postgres}"]
```
With enhanced version that also checks dify_plugin DB exists:
```yaml
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres} && psql -U ${DB_USERNAME:-postgres} -d ${PLUGIN_DB_DATABASE:-dify_plugin} -c 'SELECT 1' >/dev/null 2>&1"]
```
Keep interval/timeout/retries/start_period unchanged (10s/5s/5/30s).

**Change 3: Add redis-lock-cleaner init-container (before plugin_daemon service, around line 607)**
Insert new service:
```yaml
  # ==================== Redis Lock Cleaner (init-container) ====================
  redis-lock-cleaner:
    image: redis:${REDIS_VERSION:-7.4.1-alpine}
    container_name: agmind-redis-lock-cleaner
    restart: "no"
    mem_limit: 64m
    volumes:
      - ../scripts/redis-lock-cleanup.sh:/cleanup.sh:ro
    environment:
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    entrypoint: ["sh", "/cleanup.sh"]
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - agmind-backend
```

**Change 4: Update plugin_daemon depends_on (line 683-688)**
Replace current:
```yaml
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
```
With:
```yaml
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      redis-lock-cleaner:
        condition: service_completed_successfully
```

These changes create the dependency chain: redis (healthy) -> redis-lock-cleaner (completes) -> plugin_daemon starts. And db healthcheck now confirms dify_plugin DB exists before plugin_daemon starts.
  </action>
  <verify>
    <automated>bash -c "grep -q 'init-dify-plugin-db.sql' templates/docker-compose.yml && grep -q 'pg_database\|dify_plugin' templates/docker-compose.yml && grep -q 'redis-lock-cleaner' templates/docker-compose.yml && grep -q 'service_completed_successfully' templates/docker-compose.yml && echo PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/docker-compose.yml db volumes contains `init-dify-plugin-db.sql:/docker-entrypoint-initdb.d/`
    - templates/docker-compose.yml db healthcheck contains `psql -U` and `dify_plugin`
    - templates/docker-compose.yml contains `redis-lock-cleaner:` service definition
    - redis-lock-cleaner has `restart: "no"` (init-container pattern)
    - redis-lock-cleaner has `depends_on: redis: condition: service_healthy`
    - redis-lock-cleaner uses `image: redis:${REDIS_VERSION:-7.4.1-alpine}` (same as main redis)
    - plugin_daemon depends_on contains `redis-lock-cleaner: condition: service_completed_successfully`
    - plugin_daemon still has `db: condition: service_healthy` in depends_on
    - YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('templates/docker-compose.yml'))"` exits 0
  </acceptance_criteria>
  <done>docker-compose.yml has enhanced PostgreSQL healthcheck checking dify_plugin DB, init SQL mount for fresh installs, redis-lock-cleaner init-container, and plugin_daemon waits for both DB and lock cleanup</done>
</task>

</tasks>

<verification>
1. `grep -q 'dify_plugin' templates/docker-compose.yml` — DB name referenced in healthcheck
2. `grep -q 'redis-lock-cleaner' templates/docker-compose.yml` — init-container exists
3. `grep -q 'service_completed_successfully' templates/docker-compose.yml` — dependency chain wired
4. `grep -q 'docker-entrypoint-initdb.d' templates/docker-compose.yml` — init SQL mounted
5. `python3 -c "import yaml; yaml.safe_load(open('templates/docker-compose.yml'))"` — valid YAML
6. `bash -n scripts/redis-lock-cleanup.sh` — valid bash syntax
</verification>

<success_criteria>
- plugin_daemon cannot start until PostgreSQL healthcheck confirms dify_plugin DB exists
- Fresh installs auto-create dify_plugin via /docker-entrypoint-initdb.d/ SQL script
- Redis locks are cleaned by init-container before plugin_daemon starts
- No new Docker images introduced (redis-lock-cleaner reuses redis:7-alpine)
- create_plugin_db() in compose.sh remains as additional fallback (untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/06-v3-bugfixes/06-01-SUMMARY.md`
</output>
