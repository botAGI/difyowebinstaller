---
phase: 03-provider-architecture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - install.sh
  - lib/config.sh
  - lib/models.sh
  - tests/test_wizard_provider.bats
  - tests/test_compose_profiles.bats
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03

must_haves:
  truths:
    - "Wizard asks LLM provider (1-4) with GPU-aware default before model selection"
    - "Wizard asks embedding provider (1-4) with 'Same as LLM' mapping correctly"
    - "HF_TOKEN prompt appears only when vLLM or TEI is selected"
    - "NON_INTERACTIVE mode respects LLM_PROVIDER and EMBED_PROVIDER env vars"
    - "COMPOSE_PROFILES includes ollama when LLM_PROVIDER=ollama or EMBED_PROVIDER=ollama"
    - "COMPOSE_PROFILES includes vllm when LLM_PROVIDER=vllm"
    - "COMPOSE_PROFILES includes tei when EMBED_PROVIDER=tei"
    - "Nuclear cleanup profile list includes ollama,vllm,tei"
    - "config.sh generates LLM_PROVIDER, EMBED_PROVIDER, VLLM_MODEL, HF_TOKEN, OLLAMA_BASE_URL, ENABLE_OLLAMA_API, ENABLE_OPENAI_API, OPENAI_API_BASE_URL in .env"
    - "models.sh download_models() dispatches by provider: Ollama pull only when needed, skip for vLLM/TEI/External"
    - "BATS tests verify wizard and compose profile logic"
  artifacts:
    - path: "install.sh"
      provides: "Wizard provider questions, COMPOSE_PROFILES builder with ollama/vllm/tei, phase_models provider dispatch"
      contains: "LLM_PROVIDER"
    - path: "lib/config.sh"
      provides: "Provider env vars generated in .env"
      contains: "__LLM_PROVIDER__"
    - path: "lib/models.sh"
      provides: "Provider-aware download dispatcher"
      contains: "LLM_PROVIDER"
    - path: "tests/test_wizard_provider.bats"
      provides: "BATS tests for wizard provider selection"
      contains: "@test"
    - path: "tests/test_compose_profiles.bats"
      provides: "BATS tests for COMPOSE_PROFILES builder"
      contains: "@test"
  key_links:
    - from: "install.sh phase_wizard()"
      to: "install.sh phase_start() COMPOSE_PROFILES"
      via: "LLM_PROVIDER and EMBED_PROVIDER global variables"
      pattern: "LLM_PROVIDER|EMBED_PROVIDER"
    - from: "lib/config.sh generate_config()"
      to: "templates/env.*.template"
      via: "sed replacement of __LLM_PROVIDER__ etc."
      pattern: "__LLM_PROVIDER__|__EMBED_PROVIDER__"
    - from: "install.sh phase_models()"
      to: "lib/models.sh download_models()"
      via: "export LLM_PROVIDER EMBED_PROVIDER"
      pattern: "export.*LLM_PROVIDER"
---

<objective>
Add provider selection to the installation wizard, wire config generation for provider env vars, make model download provider-aware, update COMPOSE_PROFILES builder, and create BATS tests.

Purpose: This is the core logic that connects user's provider choice to the compose profiles from Plan 01. Without this, the compose changes are inert.
Output: Working wizard flow, config generation, model dispatcher, and automated tests.
</objective>

<execution_context>
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/workflows/execute-plan.md
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-provider-architecture/03-CONTEXT.md
@.planning/phases/03-provider-architecture/03-RESEARCH.md
@.planning/phases/03-provider-architecture/03-01-SUMMARY.md

<interfaces>
<!-- Key patterns from existing codebase the executor needs -->

From install.sh (wizard pattern, lines 268-282 — vector store example):
```bash
    # --- Vector Store ---
    echo "Выберите векторное хранилище:"
    echo "  1) Weaviate  — стабильный, проверенный (по умолчанию)"
    echo "  2) Qdrant    — быстрый, REST/gRPC API"
    echo ""
    if [[ "$NON_INTERACTIVE" != "true" ]]; then
        read -rp "Выбор [1-2, Enter=1]: " choice
        choice="${choice:-1}"
    else
        choice="${VECTOR_STORE_CHOICE:-1}"
    fi
    case "$choice" in
        2) VECTOR_STORE="qdrant";;
        *) VECTOR_STORE="weaviate";;
    esac
```

From install.sh (COMPOSE_PROFILES builder, lines 776-783):
```bash
    local profiles=""
    [[ "$DEPLOY_PROFILE" == "vps" ]] && profiles="vps"
    [[ "$VECTOR_STORE" == "qdrant" ]] && profiles="${profiles:+$profiles,}qdrant"
    [[ "$VECTOR_STORE" == "weaviate" ]] && profiles="${profiles:+$profiles,}weaviate"
    [[ "$ETL_ENHANCED" == "yes" ]] && profiles="${profiles:+$profiles,}etl"
    [[ "$MONITORING_MODE" == "local" ]] && profiles="${profiles:+$profiles,}monitoring"
    [[ "$ENABLE_AUTHELIA" == "true" ]] && profiles="${profiles:+$profiles,}authelia"
```

From install.sh (nuclear cleanup, line 789):
```bash
    COMPOSE_PROFILES=vps,monitoring,qdrant,weaviate,etl,authelia \
        docker compose down --remove-orphans 2>/dev/null || true
```

From install.sh (phase_models, lines 1079-1084):
```bash
phase_models() {
    echo -e "${BOLD}[7/9] Загрузка моделей${NC}"
    export INSTALL_DIR LLM_MODEL EMBEDDING_MODEL DEPLOY_PROFILE
    download_models
    echo ""
}
```

From lib/config.sh (sed replacements, lines 276-301 — partial):
```bash
    sed -i.bak \
        -e "s|__LLM_MODEL__|${safe_llm_model}|g" \
        -e "s|__EMBEDDING_MODEL__|${safe_embedding_model}|g" \
        ...
        "$env_file"
```

From lib/models.sh (download_models, lines 124-157):
```bash
download_models() {
    local llm_model="${LLM_MODEL:-qwen2.5:14b}"
    local embedding_model="${EMBEDDING_MODEL:-bge-m3}"
    local profile="${DEPLOY_PROFILE:-lan}"
    # ... offline check, wait_for_ollama, pull_model
}
```

From lib/detect.sh (exports DETECTED_GPU):
```bash
export DETECTED_GPU  # "nvidia", "amd", "intel", "apple", "none"
```

From tests/test_backup.bats (BATS test pattern):
```bash
setup() {
    export ROOT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
    export INSTALL_DIR="$(mktemp -d)"
    ...
}
teardown() {
    rm -rf "$INSTALL_DIR"
}
@test "description" {
    run grep "pattern" "${ROOT_DIR}/file"
    [ "$status" -eq 0 ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider selection to wizard and wire COMPOSE_PROFILES, config.sh, models.sh</name>
  <files>install.sh, lib/config.sh, lib/models.sh</files>
  <read_first>
    - install.sh (full file — wizard at lines 268-393, COMPOSE_PROFILES at lines 776-789, phase_models at lines 1079-1084, phase_start nuclear cleanup at line 789)
    - lib/config.sh (full file — generate_config sed replacements at lines 276-301, escape_sed pattern)
    - lib/models.sh (full file — download_models at lines 124-157)
    - .planning/phases/03-provider-architecture/03-CONTEXT.md (all locked decisions)
    - .planning/phases/03-provider-architecture/03-RESEARCH.md (code examples for wizard, config, models dispatcher)
  </read_first>
  <action>
**A. install.sh — phase_wizard(): Add LLM provider selection BEFORE the LLM model selection block (before `# --- LLM Model (expanded list) ---` at ~line 304)**

Insert this block after the ETL Enhanced section (after ~line 302):

```bash
    # --- LLM Provider ---
    local default_llm_provider="vllm"
    local default_llm_idx=2
    local gpu_warning=""
    if [[ "${DETECTED_GPU:-none}" != "nvidia" ]]; then
        default_llm_provider="ollama"
        default_llm_idx=1
        gpu_warning="  (GPU NVIDIA не обнаружен — умолчание: Ollama)"
    fi

    echo "Выберите LLM провайдер:${gpu_warning}"
    echo "  1) Ollama"
    echo "  2) vLLM"
    echo "  3) External API"
    echo "  4) Skip"
    echo ""
    if [[ "$NON_INTERACTIVE" != "true" ]]; then
        read -rp "Выбор [1-4, Enter=${default_llm_idx}]: " choice
        choice="${choice:-${default_llm_idx}}"
    else
        case "${LLM_PROVIDER:-$default_llm_provider}" in
            ollama)   choice=1;; vllm) choice=2;; external) choice=3;; skip) choice=4;; *) choice="${default_llm_idx}";;
        esac
    fi
    case "$choice" in
        1) LLM_PROVIDER="ollama";;
        2) LLM_PROVIDER="vllm";;
        3) LLM_PROVIDER="external";;
        4) LLM_PROVIDER="skip";;
        *) LLM_PROVIDER="$default_llm_provider";;
    esac
    echo ""
```

**B. install.sh — phase_wizard(): Add vLLM model selection AFTER LLM provider block, wrap existing Ollama model list conditionally**

After the LLM_PROVIDER case block, add conditional model selection:

```bash
    # --- LLM Model (provider-dependent) ---
    if [[ "$LLM_PROVIDER" == "ollama" ]]; then
        # (existing 16-model Ollama list stays EXACTLY as-is, lines ~304-376)
        # ... no changes to existing Ollama model selection code ...
    elif [[ "$LLM_PROVIDER" == "vllm" ]]; then
        echo "Выберите модель для vLLM:"
        echo ""
        echo " ── 7-8B [14GB+ VRAM] ──"
        echo "  1) Qwen/Qwen2.5-7B-Instruct"
        echo "  2) mistralai/Mistral-7B-Instruct-v0.3"
        echo "  3) meta-llama/Llama-3.1-8B-Instruct  (требует HF_TOKEN)"
        echo ""
        echo " ── 14B [24GB+ VRAM] ──"
        echo "  4) Qwen/Qwen2.5-14B-Instruct           [рекомендуется]"
        echo "  5) Qwen/Qwen3-14B"
        echo "  6) microsoft/phi-4"
        echo ""
        echo " ── 32B+ [48GB+ VRAM] ──"
        echo "  7) Qwen/Qwen2.5-32B-Instruct"
        echo "  8) meta-llama/Llama-3.3-70B-Instruct  (требует HF_TOKEN)"
        echo ""
        echo " ── Своя модель ──"
        echo "  9) Указать HuggingFace repo (org/model-name)"
        echo ""
        if [[ "$NON_INTERACTIVE" != "true" ]]; then
            read -rp "Модель [1-9, Enter=4]: " choice
            choice="${choice:-4}"
            case "$choice" in
                1) VLLM_MODEL="Qwen/Qwen2.5-7B-Instruct";;
                2) VLLM_MODEL="mistralai/Mistral-7B-Instruct-v0.3";;
                3) VLLM_MODEL="meta-llama/Llama-3.1-8B-Instruct";;
                4) VLLM_MODEL="Qwen/Qwen2.5-14B-Instruct";;
                5) VLLM_MODEL="Qwen/Qwen3-14B";;
                6) VLLM_MODEL="microsoft/phi-4";;
                7) VLLM_MODEL="Qwen/Qwen2.5-32B-Instruct";;
                8) VLLM_MODEL="meta-llama/Llama-3.3-70B-Instruct";;
                9) read -rp "HuggingFace repo (org/model): " VLLM_MODEL
                   [[ -z "$VLLM_MODEL" ]] && VLLM_MODEL="Qwen/Qwen2.5-14B-Instruct";;
                *) VLLM_MODEL="Qwen/Qwen2.5-14B-Instruct";;
            esac
        else
            VLLM_MODEL="${VLLM_MODEL:-Qwen/Qwen2.5-14B-Instruct}"
        fi
        echo ""
    fi
    # external/skip: no model selection needed
```

**C. install.sh — phase_wizard(): Add embedding provider selection AFTER model selection, BEFORE the existing embedding model prompt**

```bash
    # --- Embedding Provider ---
    echo "Выберите Embedding провайдер:"
    echo "  1) Тот же, что LLM"
    echo "  2) TEI (Text Embeddings Inference)"
    echo "  3) External API"
    echo "  4) Skip"
    echo ""
    if [[ "$NON_INTERACTIVE" != "true" ]]; then
        read -rp "Выбор [1-4, Enter=1]: " choice
        choice="${choice:-1}"
    else
        case "${EMBED_PROVIDER:-same}" in
            same|"") choice=1;; tei) choice=2;; external) choice=3;; skip) choice=4;;
            ollama) choice=1;; # backwards compat
            *) choice=1;;
        esac
    fi
    case "$choice" in
        1) # "Same as LLM" mapping
           case "$LLM_PROVIDER" in
               ollama)   EMBED_PROVIDER="ollama";;
               vllm)     EMBED_PROVIDER="tei";;
               external) EMBED_PROVIDER="external";;
               skip)     EMBED_PROVIDER="skip";;
           esac
           ;;
        2) EMBED_PROVIDER="tei";;
        3) EMBED_PROVIDER="external";;
        4) EMBED_PROVIDER="skip";;
        *) EMBED_PROVIDER="ollama";;  # fallback
    esac
    echo ""
```

**D. install.sh — phase_wizard(): Wrap existing embedding model prompt (lines ~378-385) in provider check:**

Change the existing embedding model question to only show when EMBED_PROVIDER=ollama:
```bash
    if [[ "$EMBED_PROVIDER" == "ollama" ]]; then
        if [[ "$NON_INTERACTIVE" != "true" ]]; then
            read -rp "Embedding модель [bge-m3]: " EMBEDDING_MODEL
            EMBEDDING_MODEL="${EMBEDDING_MODEL:-bge-m3}"
            validate_model_name "$EMBEDDING_MODEL" || { EMBEDDING_MODEL="bge-m3"; echo "Using default embedding model"; }
        else
            EMBEDDING_MODEL="${EMBEDDING_MODEL:-bge-m3}"
        fi
        echo ""
    fi
```

**E. install.sh — phase_wizard(): Add HF_TOKEN prompt when vLLM or TEI selected, AFTER embedding provider selection:**

```bash
    # --- HuggingFace Token (optional, only for vLLM/TEI) ---
    HF_TOKEN="${HF_TOKEN:-}"
    if [[ "$LLM_PROVIDER" == "vllm" || "$EMBED_PROVIDER" == "tei" ]]; then
        if [[ "$NON_INTERACTIVE" != "true" ]]; then
            read -rp "HuggingFace token (Enter для пропуска): " HF_TOKEN
            HF_TOKEN="${HF_TOKEN:-}"
        fi
    fi
```

**F. install.sh — phase_wizard(): Update wizard summary display**

Find the existing summary line `"LLM: ${LLM_MODEL} (Ollama)"` in phase_complete (handled in Plan 03). For the wizard summary block at the end of phase_wizard (if any summary echo exists), update it. If no wizard-end summary exists, skip.

**G. install.sh — phase_wizard(): Update offline model warning to be provider-aware**

Replace the existing offline warning block (lines ~388-393):
```bash
    # --- Offline model warning ---
    if [[ "$DEPLOY_PROFILE" == "offline" ]]; then
        echo -e "${YELLOW}⚠ Профиль Offline: модели не будут скачиваться.${NC}"
        if [[ "$LLM_PROVIDER" == "ollama" || "$EMBED_PROVIDER" == "ollama" ]]; then
            echo "  Убедитесь, что модели предзагружены в ollama_data volume."
        fi
        if [[ "$LLM_PROVIDER" == "vllm" ]]; then
            echo "  Убедитесь, что образ vLLM и модель ${VLLM_MODEL:-} предзагружены."
        fi
        if [[ "$EMBED_PROVIDER" == "tei" ]]; then
            echo "  Убедитесь, что образ TEI и модель BAAI/bge-m3 предзагружены."
        fi
        echo ""
    fi
```

**H. install.sh — phase_start() COMPOSE_PROFILES builder: Add provider profiles (after line 783)**

Add these lines after the existing `ENABLE_AUTHELIA` line:
```bash
    [[ "$LLM_PROVIDER" == "ollama" || "$EMBED_PROVIDER" == "ollama" ]] && \
        profiles="${profiles:+$profiles,}ollama"
    [[ "$LLM_PROVIDER" == "vllm" ]] && profiles="${profiles:+$profiles,}vllm"
    [[ "$EMBED_PROVIDER" == "tei" ]] && profiles="${profiles:+$profiles,}tei"
```

**I. install.sh — phase_start() nuclear cleanup: Add provider profiles to cleanup list (line 789)**

Change:
```bash
    COMPOSE_PROFILES=vps,monitoring,qdrant,weaviate,etl,authelia \
```
To:
```bash
    COMPOSE_PROFILES=vps,monitoring,qdrant,weaviate,etl,authelia,ollama,vllm,tei \
```

**J. install.sh — phase_models(): Export provider variables**

Change line 1081 from:
```bash
    export INSTALL_DIR LLM_MODEL EMBEDDING_MODEL DEPLOY_PROFILE
```
To:
```bash
    export INSTALL_DIR LLM_MODEL EMBEDDING_MODEL DEPLOY_PROFILE LLM_PROVIDER EMBED_PROVIDER
```

**K. lib/config.sh — generate_config(): Add provider variable replacements**

After the existing `safe_embedding_model` line (~line 270), add:
```bash
    local safe_vllm_model safe_hf_token safe_llm_provider safe_embed_provider
    safe_llm_provider=$(escape_sed "${LLM_PROVIDER:-ollama}")
    safe_embed_provider=$(escape_sed "${EMBED_PROVIDER:-ollama}")
    safe_vllm_model=$(escape_sed "${VLLM_MODEL:-Qwen/Qwen2.5-14B-Instruct}")
    safe_hf_token=$(escape_sed "${HF_TOKEN:-}")
```

In the main `sed -i.bak` block (lines 276-301), add BEFORE the `"$env_file"` closing line:
```bash
        -e "s|__LLM_PROVIDER__|${safe_llm_provider}|g" \
        -e "s|__EMBED_PROVIDER__|${safe_embed_provider}|g" \
        -e "s|__VLLM_MODEL__|${safe_vllm_model}|g" \
        -e "s|__HF_TOKEN__|${safe_hf_token}|g" \
```

**L. lib/config.sh — generate_config(): After sed replacement, append Open WebUI provider-specific env vars to .env**

After the Authelia placeholder replacement block (~line 310), add:
```bash
    # Provider-specific Open WebUI env vars
    {
        echo ""
        echo "# --- Provider-specific WebUI vars ---"
        case "${LLM_PROVIDER:-ollama}" in
            ollama)
                echo "OLLAMA_BASE_URL=http://ollama:11434"
                echo "ENABLE_OLLAMA_API=true"
                echo "ENABLE_OPENAI_API=false"
                echo "OPENAI_API_BASE_URL="
                ;;
            vllm)
                echo "OLLAMA_BASE_URL="
                echo "ENABLE_OLLAMA_API=false"
                echo "ENABLE_OPENAI_API=true"
                echo "OPENAI_API_BASE_URL=http://vllm:8000/v1"
                ;;
            external|skip)
                echo "OLLAMA_BASE_URL="
                echo "ENABLE_OLLAMA_API=false"
                echo "ENABLE_OPENAI_API=false"
                echo "OPENAI_API_BASE_URL="
                ;;
        esac
    } >> "$env_file"
```

**M. lib/models.sh — Rewrite download_models() to dispatch by provider**

Replace the entire `download_models()` function (lines 124-157) with:
```bash
download_models() {
    local llm_model="${LLM_MODEL:-qwen2.5:14b}"
    local embedding_model="${EMBEDDING_MODEL:-bge-m3}"
    local profile="${DEPLOY_PROFILE:-lan}"
    local llm_provider="${LLM_PROVIDER:-ollama}"
    local embed_provider="${EMBED_PROVIDER:-ollama}"

    # Skip for offline profile
    if [[ "$profile" == "offline" ]]; then
        echo -e "${YELLOW}Профиль offline: пропуск загрузки моделей${NC}"
        if [[ "$llm_provider" == "ollama" || "$embed_provider" == "ollama" ]]; then
            echo "Проверка предзагруженных моделей..."
            check_ollama_models
        fi
        load_reranker
        return 0
    fi

    local need_ollama=false
    [[ "$llm_provider" == "ollama" ]] && need_ollama=true
    [[ "$embed_provider" == "ollama" ]] && need_ollama=true

    if [[ "$need_ollama" == "true" ]]; then
        wait_for_ollama || return 1
        echo ""
        echo -e "${YELLOW}=== Загрузка моделей ===${NC}"
        echo ""
        [[ "$llm_provider" == "ollama" ]] && pull_model "$llm_model" "LLM ($llm_model)"
        [[ "$embed_provider" == "ollama" ]] && pull_model "$embedding_model" "Embedding ($embedding_model)"
    fi

    # vLLM/TEI download models at container start — skip
    if [[ "$llm_provider" == "vllm" ]]; then
        echo -e "${YELLOW}vLLM: модель загружается при старте контейнера${NC}"
    fi
    if [[ "$embed_provider" == "tei" ]]; then
        echo -e "${YELLOW}TEI: модель загружается при старте контейнера${NC}"
    fi
    if [[ "$llm_provider" == "external" || "$llm_provider" == "skip" ]]; then
        [[ "$need_ollama" != "true" ]] && echo -e "${YELLOW}Провайдер ${llm_provider}: загрузка моделей не требуется${NC}"
    fi

    # Load reranker (depends on ETL_ENHANCED, not on provider)
    load_reranker

    echo ""
    echo -e "${GREEN}Фаза моделей завершена${NC}"
}
```
  </action>
  <verify>
    <automated>bash -n install.sh && bash -n lib/config.sh && bash -n lib/models.sh && grep "LLM_PROVIDER" install.sh | head -3 && grep "__LLM_PROVIDER__" lib/config.sh && grep "LLM_PROVIDER" lib/models.sh | head -2 && grep "ollama,vllm,tei" install.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0 (valid syntax)
    - `bash -n lib/config.sh` exits 0 (valid syntax)
    - `bash -n lib/models.sh` exits 0 (valid syntax)
    - install.sh contains `LLM_PROVIDER="ollama"` and `LLM_PROVIDER="vllm"` and `LLM_PROVIDER="external"` and `LLM_PROVIDER="skip"`
    - install.sh contains `EMBED_PROVIDER="ollama"` and `EMBED_PROVIDER="tei"` and `EMBED_PROVIDER="external"` and `EMBED_PROVIDER="skip"`
    - install.sh contains `VLLM_MODEL="Qwen/Qwen2.5-14B-Instruct"` (default)
    - install.sh contains `HuggingFace token` prompt string
    - install.sh contains `Выберите LLM провайдер:` display string
    - install.sh contains `Выберите Embedding провайдер:` display string
    - install.sh COMPOSE_PROFILES builder contains `ollama` condition with `LLM_PROVIDER`
    - install.sh COMPOSE_PROFILES builder contains `vllm` condition with `LLM_PROVIDER`
    - install.sh COMPOSE_PROFILES builder contains `tei` condition with `EMBED_PROVIDER`
    - install.sh nuclear cleanup contains `ollama,vllm,tei`
    - install.sh phase_models exports `LLM_PROVIDER EMBED_PROVIDER`
    - lib/config.sh contains `__LLM_PROVIDER__` sed replacement
    - lib/config.sh contains `__EMBED_PROVIDER__` sed replacement
    - lib/config.sh contains `__VLLM_MODEL__` sed replacement
    - lib/config.sh contains `__HF_TOKEN__` sed replacement
    - lib/config.sh contains `OLLAMA_BASE_URL=http://ollama:11434` for ollama case
    - lib/config.sh contains `OPENAI_API_BASE_URL=http://vllm:8000/v1` for vllm case
    - lib/models.sh contains `LLM_PROVIDER` variable usage
    - lib/models.sh contains `need_ollama` variable
    - lib/models.sh contains `vLLM: модель загружается при старте контейнера` string
    - No `depends_on: ollama` references added (regression check)
  </acceptance_criteria>
  <done>
    Wizard asks LLM provider with GPU-aware default, embedding provider with "Same as LLM" mapping, optional HF_TOKEN. config.sh generates all provider env vars. models.sh dispatches by provider. COMPOSE_PROFILES includes correct provider profiles. All three files pass `bash -n` syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BATS tests for wizard provider selection and COMPOSE_PROFILES builder</name>
  <files>tests/test_wizard_provider.bats, tests/test_compose_profiles.bats</files>
  <read_first>
    - tests/test_backup.bats (reference BATS pattern: setup/teardown, grep-based assertions)
    - install.sh (updated by Task 1 — read the provider wizard and COMPOSE_PROFILES sections)
    - lib/models.sh (updated by Task 1 — verify provider dispatch patterns)
  </read_first>
  <action>
**1. Create `tests/test_wizard_provider.bats`:**

```bash
#!/usr/bin/env bats

# test_wizard_provider.bats — Provider wizard selection validation tests

setup() {
    export ROOT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
}

# --- Syntax ---

@test "install.sh passes bash -n syntax check" {
    run bash -n "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "lib/models.sh passes bash -n syntax check" {
    run bash -n "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

@test "lib/config.sh passes bash -n syntax check" {
    run bash -n "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

# --- LLM Provider wizard (PROV-01) ---

@test "install.sh contains LLM provider selection menu" {
    run grep "Выберите LLM провайдер:" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh has all 4 LLM provider options" {
    run grep -c 'LLM_PROVIDER="' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
    [ "$output" -ge 4 ]
}

@test "install.sh sets LLM_PROVIDER=ollama for choice 1" {
    run grep 'LLM_PROVIDER="ollama"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets LLM_PROVIDER=vllm for choice 2" {
    run grep 'LLM_PROVIDER="vllm"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets LLM_PROVIDER=external for choice 3" {
    run grep 'LLM_PROVIDER="external"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets LLM_PROVIDER=skip for choice 4" {
    run grep 'LLM_PROVIDER="skip"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh has GPU detection fallback for default provider" {
    run grep 'DETECTED_GPU.*nvidia' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh NON_INTERACTIVE guard exists for LLM provider" {
    # Verify that NON_INTERACTIVE pattern is used around LLM_PROVIDER selection
    run grep -A5 "LLM провайдер" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
    # Verify NON_INTERACTIVE check exists nearby in the wizard
    run grep -c "NON_INTERACTIVE" "${ROOT_DIR}/install.sh"
    [ "$output" -ge 5 ]
}

# --- Embedding Provider wizard (PROV-02) ---

@test "install.sh contains Embedding provider selection menu" {
    run grep "Выберите Embedding провайдер:" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets EMBED_PROVIDER=tei" {
    run grep 'EMBED_PROVIDER="tei"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets EMBED_PROVIDER=external" {
    run grep 'EMBED_PROVIDER="external"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh sets EMBED_PROVIDER=skip" {
    run grep 'EMBED_PROVIDER="skip"' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh has Same as LLM mapping for embedding" {
    # Verify the "Same as LLM" dispatch exists
    run grep -A10 "Same as LLM\|Тот же, что LLM" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
    [[ "$output" == *"ollama"* ]]
    [[ "$output" == *"tei"* ]]
}

# --- HuggingFace token ---

@test "install.sh prompts for HuggingFace token" {
    run grep "HuggingFace token" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh HF_TOKEN prompt only for vllm or tei" {
    run grep -B5 "HuggingFace token" "${ROOT_DIR}/install.sh"
    [[ "$output" == *"vllm"* ]] || [[ "$output" == *"tei"* ]]
}

# --- vLLM model list ---

@test "install.sh has vLLM model selection" {
    run grep "Выберите модель для vLLM:" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh default vLLM model is Qwen2.5-14B-Instruct" {
    run grep "Qwen/Qwen2.5-14B-Instruct" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

# --- models.sh provider dispatch (PROV-01/02) ---

@test "models.sh uses LLM_PROVIDER variable" {
    run grep "LLM_PROVIDER" "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

@test "models.sh uses EMBED_PROVIDER variable" {
    run grep "EMBED_PROVIDER" "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

@test "models.sh has need_ollama dispatch logic" {
    run grep "need_ollama" "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

@test "models.sh skips download for vLLM provider" {
    run grep "vLLM.*загружается при старте" "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

@test "models.sh skips download for TEI provider" {
    run grep "TEI.*загружается при старте" "${ROOT_DIR}/lib/models.sh"
    [ "$status" -eq 0 ]
}

# --- config.sh provider env generation ---

@test "config.sh replaces __LLM_PROVIDER__ placeholder" {
    run grep "__LLM_PROVIDER__" "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

@test "config.sh replaces __EMBED_PROVIDER__ placeholder" {
    run grep "__EMBED_PROVIDER__" "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

@test "config.sh replaces __VLLM_MODEL__ placeholder" {
    run grep "__VLLM_MODEL__" "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

@test "config.sh replaces __HF_TOKEN__ placeholder" {
    run grep "__HF_TOKEN__" "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

@test "config.sh generates OLLAMA_BASE_URL for ollama provider" {
    run grep 'OLLAMA_BASE_URL=http://ollama:11434' "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}

@test "config.sh generates OPENAI_API_BASE_URL for vllm provider" {
    run grep 'OPENAI_API_BASE_URL=http://vllm:8000/v1' "${ROOT_DIR}/lib/config.sh"
    [ "$status" -eq 0 ]
}
```

**2. Create `tests/test_compose_profiles.bats`:**

```bash
#!/usr/bin/env bats

# test_compose_profiles.bats — COMPOSE_PROFILES builder validation tests

setup() {
    export ROOT_DIR="$(cd "$(dirname "$BATS_TEST_FILENAME")/.." && pwd)"
}

# --- COMPOSE_PROFILES builder (PROV-03) ---

@test "install.sh adds ollama to COMPOSE_PROFILES when LLM_PROVIDER=ollama" {
    run grep 'LLM_PROVIDER.*==.*"ollama".*profiles.*ollama\|LLM_PROVIDER.*ollama.*profiles' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh adds ollama to COMPOSE_PROFILES when EMBED_PROVIDER=ollama" {
    run grep 'EMBED_PROVIDER.*==.*"ollama".*profiles.*ollama\|EMBED_PROVIDER.*ollama.*profiles' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh adds vllm to COMPOSE_PROFILES when LLM_PROVIDER=vllm" {
    run grep 'LLM_PROVIDER.*==.*"vllm".*profiles.*vllm\|LLM_PROVIDER.*vllm.*profiles' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

@test "install.sh adds tei to COMPOSE_PROFILES when EMBED_PROVIDER=tei" {
    run grep 'EMBED_PROVIDER.*==.*"tei".*profiles.*tei\|EMBED_PROVIDER.*tei.*profiles' "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

# --- Nuclear cleanup includes new profiles ---

@test "install.sh nuclear cleanup includes ollama,vllm,tei profiles" {
    run grep "COMPOSE_PROFILES=.*ollama.*vllm.*tei" "${ROOT_DIR}/install.sh"
    [ "$status" -eq 0 ]
}

# --- Docker Compose profiles (PROV-03) ---

@test "docker-compose.yml has ollama profile" {
    run grep -A2 "container_name: agmind-ollama" "${ROOT_DIR}/templates/docker-compose.yml"
    [[ "$output" == *"profiles"* ]] || run grep -B2 -A2 "agmind-ollama" "${ROOT_DIR}/templates/docker-compose.yml"
    run grep -A20 "agmind-ollama" "${ROOT_DIR}/templates/docker-compose.yml"
    [[ "$output" == *"ollama"* ]]
}

@test "docker-compose.yml has vllm service with profile" {
    run grep "agmind-vllm" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml has tei service with profile" {
    run grep "agmind-tei" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml open-webui has no depends_on ollama" {
    # Get the open-webui service block and check no depends_on: ollama
    # Simple check: no line should have both "depends_on" near "ollama" for open-webui
    run bash -c "sed -n '/open-webui:/,/^  [a-z]/p' '${ROOT_DIR}/templates/docker-compose.yml' | grep -c 'ollama'"
    # ollama should not appear in open-webui block (was depends_on)
    [ "$output" -eq 0 ] || [ "$status" -ne 0 ]
}

@test "docker-compose.yml vllm has ipc: host" {
    run grep "ipc: host" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml vllm has start_period 900s" {
    run grep "start_period: 900s" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml tei has start_period 600s" {
    run grep "start_period: 600s" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml has vllm_cache volume" {
    run grep "vllm_cache:" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml has tei_cache volume" {
    run grep "tei_cache:" "${ROOT_DIR}/templates/docker-compose.yml"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml vllm has GPU comment block" {
    run bash -c "sed -n '/agmind-vllm/,/networks:/p' '${ROOT_DIR}/templates/docker-compose.yml' | grep '#__GPU__'"
    [ "$status" -eq 0 ]
}

@test "docker-compose.yml tei has GPU comment block" {
    run bash -c "sed -n '/agmind-tei/,/networks:/p' '${ROOT_DIR}/templates/docker-compose.yml' | grep '#__GPU__'"
    [ "$status" -eq 0 ]
}

# --- versions.env (PROV-03) ---

@test "versions.env has VLLM_VERSION" {
    run grep "VLLM_VERSION=" "${ROOT_DIR}/templates/versions.env"
    [ "$status" -eq 0 ]
    [[ "$output" == *"v0.8.4"* ]]
}

@test "versions.env has TEI_VERSION" {
    run grep "TEI_VERSION=" "${ROOT_DIR}/templates/versions.env"
    [ "$status" -eq 0 ]
    [[ "$output" == *"cuda-1.9.2"* ]]
}

# --- Env templates (PROV-03) ---

@test "env.lan.template has LLM_PROVIDER placeholder" {
    run grep "LLM_PROVIDER=__LLM_PROVIDER__" "${ROOT_DIR}/templates/env.lan.template"
    [ "$status" -eq 0 ]
}

@test "env.lan.template has EMBED_PROVIDER placeholder" {
    run grep "EMBED_PROVIDER=__EMBED_PROVIDER__" "${ROOT_DIR}/templates/env.lan.template"
    [ "$status" -eq 0 ]
}

@test "env.lan.template has VLLM_MODEL placeholder" {
    run grep "VLLM_MODEL=__VLLM_MODEL__" "${ROOT_DIR}/templates/env.lan.template"
    [ "$status" -eq 0 ]
}

@test "env.lan.template has HF_TOKEN placeholder" {
    run grep "HF_TOKEN=__HF_TOKEN__" "${ROOT_DIR}/templates/env.lan.template"
    [ "$status" -eq 0 ]
}

# --- phase_models exports providers ---

@test "install.sh phase_models exports LLM_PROVIDER" {
    run grep -A5 "phase_models" "${ROOT_DIR}/install.sh"
    [[ "$output" == *"LLM_PROVIDER"* ]]
}

@test "install.sh phase_models exports EMBED_PROVIDER" {
    run grep -A5 "phase_models" "${ROOT_DIR}/install.sh"
    [[ "$output" == *"EMBED_PROVIDER"* ]]
}
```
  </action>
  <verify>
    <automated>bash -n tests/test_wizard_provider.bats 2>/dev/null; echo "wizard_syntax=$?"; bash -n tests/test_compose_profiles.bats 2>/dev/null; echo "compose_syntax=$?"; grep -c "@test" tests/test_wizard_provider.bats; grep -c "@test" tests/test_compose_profiles.bats</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_wizard_provider.bats exists and contains at least 20 @test blocks
    - tests/test_compose_profiles.bats exists and contains at least 15 @test blocks
    - tests/test_wizard_provider.bats covers: LLM provider (PROV-01), embedding provider (PROV-02), GPU fallback, NON_INTERACTIVE, HF_TOKEN, vLLM model list, models.sh dispatch, config.sh replacements
    - tests/test_compose_profiles.bats covers: COMPOSE_PROFILES builder (PROV-03), nuclear cleanup, docker-compose profile structure, versions.env, env templates
    - Both test files use the same setup/teardown pattern as existing tests (ROOT_DIR)
  </acceptance_criteria>
  <done>
    BATS test suites created covering PROV-01 (wizard LLM provider), PROV-02 (wizard embed provider), PROV-03 (compose profiles). Tests validate code patterns via grep, not runtime execution (consistent with existing test style).
  </done>
</task>

</tasks>

<verification>
- `bash -n install.sh && bash -n lib/config.sh && bash -n lib/models.sh` all pass
- `bats tests/test_wizard_provider.bats tests/test_compose_profiles.bats` runs (tests may fail until Plan 01 artifacts are in place — but tests themselves must be syntactically valid)
- `grep "LLM_PROVIDER" install.sh | wc -l` shows multiple occurrences
- `grep "__LLM_PROVIDER__" lib/config.sh` confirms placeholder replacement exists
- `grep "need_ollama" lib/models.sh` confirms provider dispatch logic
</verification>

<success_criteria>
The wizard flow guides users through LLM provider, model, embedding provider, and optional HF_TOKEN — with GPU-aware defaults and NON_INTERACTIVE support. Config generation writes all provider vars to .env. Model download dispatches by provider. COMPOSE_PROFILES builder includes ollama/vllm/tei. BATS tests validate all patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-architecture/03-02-SUMMARY.md`
</output>
