---
phase: 03-provider-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docker-compose.yml
  - templates/versions.env
  - templates/env.lan.template
  - templates/env.vpn.template
  - templates/env.vps.template
  - templates/env.offline.template
autonomous: true
requirements:
  - PROV-03

must_haves:
  truths:
    - "Ollama is behind profiles: ['ollama'] and does not start unless COMPOSE_PROFILES includes ollama"
    - "vLLM service exists with profiles: ['vllm'], correct image, ipc:host, GPU block, healthcheck with start_period 900s"
    - "TEI service exists with profiles: ['tei'], correct image, BAAI/bge-m3 hardcoded, healthcheck with start_period 600s"
    - "Open WebUI no longer has depends_on: ollama"
    - "Open WebUI env vars use variable substitution for OLLAMA_BASE_URL, ENABLE_OLLAMA_API, ENABLE_OPENAI_API, OPENAI_API_BASE_URL"
    - "Named volumes vllm_cache and tei_cache declared in volumes section"
    - "versions.env contains VLLM_VERSION and TEI_VERSION"
    - "All four env templates contain LLM_PROVIDER, EMBED_PROVIDER, VLLM_MODEL, HF_TOKEN placeholders"
  artifacts:
    - path: "templates/docker-compose.yml"
      provides: "vllm and tei services with profiles; ollama with profile; open-webui without depends_on ollama"
      contains: "profiles:"
    - path: "templates/versions.env"
      provides: "Pinned versions for vLLM and TEI"
      contains: "VLLM_VERSION"
  key_links:
    - from: "templates/docker-compose.yml"
      to: "templates/versions.env"
      via: "${VLLM_VERSION} and ${TEI_VERSION} variable references"
      pattern: "VLLM_VERSION|TEI_VERSION"
    - from: "templates/docker-compose.yml"
      to: ".env"
      via: "${HF_TOKEN}, ${VLLM_MODEL}, ${ENABLE_OLLAMA_API}, ${OPENAI_API_BASE_URL} substitutions"
      pattern: "HF_TOKEN|VLLM_MODEL|ENABLE_OLLAMA_API|OPENAI_API_BASE_URL"
---

<objective>
Restructure docker-compose.yml for provider architecture: move Ollama to profile, add vLLM and TEI services with profiles, make Open WebUI provider-agnostic, add version pins and env template placeholders.

Purpose: Foundation for Phase 3 — compose profiles must be in place before wizard and config logic can reference them.
Output: Modified docker-compose.yml with 3 provider profiles, updated versions.env, updated env templates.
</objective>

<execution_context>
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/workflows/execute-plan.md
@D:/Agmind/difyowebinstaller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-provider-architecture/03-CONTEXT.md
@.planning/phases/03-provider-architecture/03-RESEARCH.md

<interfaces>
<!-- Existing compose patterns the executor needs -->

From templates/docker-compose.yml (lines 41-48, logging-defaults anchor):
```yaml
x-logging-defaults: &logging-defaults
  sysctls:
    - net.ipv6.conf.all.disable_ipv6=1
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "5"
```

From templates/docker-compose.yml (lines 269-275, GPU comment pattern):
```yaml
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
```

From templates/docker-compose.yml (lines 898-907, volumes section):
```yaml
volumes:
  openwebui_data:
  ollama_data:
  xinference_data:
  grafana_data:
  portainer_data:
  prometheus_data:
  authelia_data:
  loki_data:
  promtail_positions:
```

From templates/versions.env (existing format):
```
OLLAMA_VERSION=0.6.2
XINFERENCE_VERSION=v0.16.3
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure docker-compose.yml — Ollama to profile, add vLLM + TEI services, fix Open WebUI</name>
  <files>templates/docker-compose.yml</files>
  <read_first>
    - templates/docker-compose.yml (full file — executor MUST see current state including open-webui env block at line 225-246, ollama service at line 248-277, volumes at 898-907)
    - .planning/phases/03-provider-architecture/03-RESEARCH.md (Pattern 1-5 for exact service definitions)
  </read_first>
  <action>
**1. Modify Ollama service (currently at ~line 248):**
Add `profiles: ["ollama"]` to make Ollama conditional:
```yaml
  ollama:
    <<: *logging-defaults
    image: ollama/ollama:${OLLAMA_VERSION:-0.6.2}
    container_name: agmind-ollama
    restart: always
    profiles:
      - ollama
    # ... rest stays identical
```

**2. Modify Open WebUI service (currently at ~line 219):**
- Replace hardcoded `OLLAMA_BASE_URL=http://ollama:11434` with `OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-}` (empty default — set by config.sh)
- Replace `ENABLE_OLLAMA_API=${ENABLE_OLLAMA_API:-true}` with `ENABLE_OLLAMA_API=${ENABLE_OLLAMA_API:-false}` (default false; config.sh sets true for ollama)
- Replace `ENABLE_OPENAI_API=false` with `ENABLE_OPENAI_API=${ENABLE_OPENAI_API:-false}` (config.sh sets true for vllm)
- Add `OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-}` line after ENABLE_OPENAI_API
- **Remove** the entire `depends_on:` block (lines 242-244) from open-webui. Ollama is now optional.

Result for open-webui environment section:
```yaml
    environment:
      - WEBUI_NAME=AGMind
      - ENABLE_SIGNUP=${ENABLE_SIGNUP:-false}
      - ENABLE_LOGIN_FORM=true
      - DEFAULT_USER_ROLE=${DEFAULT_USER_ROLE:-user}
      - WEBUI_AUTH=true
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-}
      - ENABLE_OLLAMA_API=${ENABLE_OLLAMA_API:-false}
      - ENABLE_OPENAI_API=${ENABLE_OPENAI_API:-false}
      - OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-}
```

**3. Add vLLM service AFTER the Ollama service block (after line ~277), before PostgreSQL:**
```yaml
  # ==================== vLLM (vllm profile) ====================
  vllm:
    <<: *logging-defaults
    image: vllm/vllm-openai:${VLLM_VERSION:-v0.8.4}
    container_name: agmind-vllm
    restart: always
    profiles:
      - vllm
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
    command: --model ${VLLM_MODEL:-Qwen/Qwen2.5-14B-Instruct} --host 0.0.0.0 --port 8000
    volumes:
      - vllm_cache:/root/.cache/huggingface
    ipc: host
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 900s
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
    networks:
      - agmind-backend
```

**4. Add TEI service AFTER vLLM service:**
```yaml
  # ==================== TEI (tei profile) ====================
  tei:
    <<: *logging-defaults
    image: ghcr.io/huggingface/text-embeddings-inference:${TEI_VERSION:-cuda-1.9.2}
    container_name: agmind-tei
    restart: always
    profiles:
      - tei
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
    command: --model-id BAAI/bge-m3 --port 80
    volumes:
      - tei_cache:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 600s
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
    networks:
      - agmind-backend
```

**5. Add named volumes at the end of the `volumes:` section (after `promtail_positions:`):**
```yaml
  vllm_cache:
  tei_cache:
```

**6. Verify no other service still has `depends_on: ollama`:**
Search entire file for `depends_on:` blocks referencing `ollama`. Remove any found. (Currently only open-webui has it.)
  </action>
  <verify>
    <automated>grep -c "profiles:" templates/docker-compose.yml | head -1 && grep "agmind-vllm" templates/docker-compose.yml && grep "agmind-tei" templates/docker-compose.yml && grep "vllm_cache:" templates/docker-compose.yml && grep "tei_cache:" templates/docker-compose.yml && ! grep -A2 "depends_on:" templates/docker-compose.yml | grep -q "ollama" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/docker-compose.yml contains `container_name: agmind-vllm`
    - templates/docker-compose.yml contains `container_name: agmind-tei`
    - templates/docker-compose.yml ollama service contains `profiles:` followed by `- ollama`
    - templates/docker-compose.yml vllm service contains `profiles:` followed by `- vllm`
    - templates/docker-compose.yml tei service contains `profiles:` followed by `- tei`
    - templates/docker-compose.yml contains `ipc: host` in vllm service
    - templates/docker-compose.yml contains `start_period: 900s` in vllm healthcheck
    - templates/docker-compose.yml contains `start_period: 600s` in tei healthcheck
    - templates/docker-compose.yml open-webui has `OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-}`
    - templates/docker-compose.yml open-webui has `OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-}`
    - templates/docker-compose.yml open-webui does NOT contain `depends_on:` block referencing ollama
    - templates/docker-compose.yml volumes section contains `vllm_cache:` and `tei_cache:`
    - templates/docker-compose.yml contains `#__GPU__deploy:` in both vllm and tei service blocks
    - `grep -c "depends_on" templates/docker-compose.yml` followed by `grep -A2 "depends_on" templates/docker-compose.yml | grep ollama` returns no matches
  </acceptance_criteria>
  <done>
    docker-compose.yml has Ollama behind profile, vLLM and TEI services with profiles and GPU blocks, Open WebUI is provider-agnostic, named volumes declared. No service depends on Ollama.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add version pins and env template placeholders for provider variables</name>
  <files>templates/versions.env, templates/env.lan.template, templates/env.vpn.template, templates/env.vps.template, templates/env.offline.template</files>
  <read_first>
    - templates/versions.env (full file — see existing format)
    - templates/env.lan.template (full file — see existing variable layout, especially lines 72-75 Ollama section and lines 85-88 Open WebUI section)
    - templates/env.vpn.template (check if same structure as lan)
    - templates/env.vps.template (check if same structure)
    - templates/env.offline.template (check if same structure)
  </read_first>
  <action>
**1. Add to `templates/versions.env` after the `# --- ETL ---` section (before `# --- Authelia ---`):**
```
# --- Provider Architecture ---
VLLM_VERSION=v0.8.4
TEI_VERSION=cuda-1.9.2
```

**2. In ALL FOUR env templates (lan, vpn, vps, offline), add a new section after the `# --- Ollama ---` section (after `EMBEDDING_MODEL=__EMBEDDING_MODEL__` line). The new block:**
```
# --- Provider Architecture ---
LLM_PROVIDER=__LLM_PROVIDER__
EMBED_PROVIDER=__EMBED_PROVIDER__
VLLM_MODEL=__VLLM_MODEL__
HF_TOKEN=__HF_TOKEN__
```

**3. In ALL FOUR env templates, in the `# --- Open WebUI ---` section, add these lines after `ENABLE_OLLAMA_API=true`:**
```
ENABLE_OPENAI_API=false
OLLAMA_BASE_URL=
OPENAI_API_BASE_URL=
```

The `ENABLE_OLLAMA_API=true` line stays as the default — config.sh will overwrite these based on provider choice.

**4. Add new placeholder values for config.sh to replace. In the Ollama section of each template, change the static values to placeholders:**
- Change `OLLAMA_API_BASE=http://ollama:11434` to keep as-is (it's a Dify hint, stays regardless of provider)
- The new `LLM_PROVIDER=__LLM_PROVIDER__` etc. will be replaced by config.sh (next plan)
  </action>
  <verify>
    <automated>grep "VLLM_VERSION=v0.8.4" templates/versions.env && grep "TEI_VERSION=cuda-1.9.2" templates/versions.env && grep "LLM_PROVIDER=__LLM_PROVIDER__" templates/env.lan.template && grep "EMBED_PROVIDER=__EMBED_PROVIDER__" templates/env.lan.template && grep "VLLM_MODEL=__VLLM_MODEL__" templates/env.lan.template && grep "HF_TOKEN=__HF_TOKEN__" templates/env.lan.template && grep "OPENAI_API_BASE_URL=" templates/env.lan.template && grep "ENABLE_OPENAI_API=" templates/env.lan.template && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - templates/versions.env contains `VLLM_VERSION=v0.8.4`
    - templates/versions.env contains `TEI_VERSION=cuda-1.9.2`
    - templates/env.lan.template contains `LLM_PROVIDER=__LLM_PROVIDER__`
    - templates/env.lan.template contains `EMBED_PROVIDER=__EMBED_PROVIDER__`
    - templates/env.lan.template contains `VLLM_MODEL=__VLLM_MODEL__`
    - templates/env.lan.template contains `HF_TOKEN=__HF_TOKEN__`
    - templates/env.lan.template contains `ENABLE_OPENAI_API=false`
    - templates/env.lan.template contains `OLLAMA_BASE_URL=`
    - templates/env.lan.template contains `OPENAI_API_BASE_URL=`
    - Same 4 provider variables present in env.vpn.template, env.vps.template, env.offline.template
    - `bash -n templates/versions.env` does not fail (valid bash syntax for sourcing)
  </acceptance_criteria>
  <done>
    versions.env has VLLM_VERSION and TEI_VERSION. All four env templates have LLM_PROVIDER, EMBED_PROVIDER, VLLM_MODEL, HF_TOKEN placeholders and Open WebUI provider-agnostic env vars.
  </done>
</task>

</tasks>

<verification>
- `grep -c "profiles:" templates/docker-compose.yml` returns at least 3 new profile entries (ollama, vllm, tei)
- `grep "depends_on" templates/docker-compose.yml | grep -c ollama` returns 0
- `grep "vllm_cache\|tei_cache" templates/docker-compose.yml` shows both volumes
- All 4 env templates have the 4 new provider placeholders
- versions.env has 2 new version pins
- `docker compose -f templates/docker-compose.yml config` validates syntax (if docker available)
</verification>

<success_criteria>
docker-compose.yml compiles cleanly with all three provider profiles declared, Open WebUI is independent of Ollama, vLLM and TEI services are fully defined with GPU blocks and extended healthchecks, and all env templates carry provider placeholders ready for config.sh substitution.
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-architecture/03-01-SUMMARY.md`
</output>
