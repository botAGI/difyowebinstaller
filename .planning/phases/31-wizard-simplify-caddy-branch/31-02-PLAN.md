---
phase: 31-wizard-simplify-caddy-branch
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - install.sh
autonomous: false
requirements: [WZRD-04]

must_haves:
  truths:
    - "Branch agmind-caddy exists locally, created from main"
    - "Wizard VDS/VPS choice references agmind-caddy branch correctly"
    - "install.sh accepts --vds flag"
  artifacts:
    - path: "install.sh"
      provides: "--vds flag handling in argument parser"
      contains: "--vds"
  key_links:
    - from: "lib/wizard.sh"
      to: "agmind-caddy branch"
      via: "git fetch origin agmind-caddy && git checkout agmind-caddy"
      pattern: "git fetch origin agmind-caddy"
---

<objective>
Create the agmind-caddy branch from main and add --vds flag to install.sh. This branch will later contain the Caddy-based VDS/VPS installer variant. For now it's just created as a placeholder so the wizard's VDS/VPS choice has a target branch.

Purpose: The wizard's VDS/VPS option does `git fetch origin agmind-caddy && git checkout agmind-caddy && exec bash install.sh --vds`. The branch must exist and install.sh must accept --vds for this to work.
Output: Branch agmind-caddy created from main, install.sh with --vds flag, ready for future Caddy work.
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-wizard-simplify-caddy-branch/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agmind-caddy branch and add --vds flag to install.sh</name>
  <files>install.sh</files>
  <read_first>install.sh, lib/wizard.sh</read_first>
  <action>
**Step 1: Create the branch.**

Create a local git branch `agmind-caddy` from `main`:

```bash
git branch agmind-caddy main
```

This creates the branch locally. The branch will need to be pushed to origin by the user (requires git push permissions). The executor should inform the user that they need to run:

```bash
git push origin agmind-caddy
```

**Step 2: Add --vds flag to install.sh.**

In `main()` function, inside the `while [[ $# -gt 0 ]]; do case "$1" in` block (~line 578-585 of install.sh), add a new case after the `--dry-run` case:

```bash
            --vds) DEPLOY_PROFILE="vps"; VDS_MODE=true;;
```

And add `VDS_MODE="${VDS_MODE:-false}"` near the other global defaults (~line 36-41, after `TIMEOUT_MODELS`). This flag will be used later when Caddy-specific logic is implemented in the agmind-caddy branch, but for now it simply sets the VPS profile to allow the branch to function with the existing install flow.

Update the --help line to include --vds:
```bash
            --help|-h) echo "Usage: sudo bash install.sh [--non-interactive] [--force-restart] [--dry-run] [--vds]"; exit 0;;
```
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && git branch --list agmind-caddy | grep -q "agmind-caddy" && echo "PASS: branch exists" || echo "FAIL: branch missing"; grep "\-\-vds" install.sh && echo "PASS: --vds flag in install.sh" || echo "FAIL: --vds flag missing"; bash -n install.sh && echo "PASS: syntax valid" || echo "FAIL: syntax error"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --list agmind-caddy` returns "agmind-caddy"
    - `grep "\-\-vds" install.sh` returns a match showing the --vds case in arg parser
    - `grep "VDS_MODE" install.sh` returns matches (variable declaration and case usage)
    - `bash -n install.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>Branch agmind-caddy exists locally (created from main). install.sh accepts --vds flag with DEPLOY_PROFILE=vps and VDS_MODE=true. User informed they need to push: `git push origin agmind-caddy`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full phase 31 changes</name>
  <files>lib/wizard.sh, lib/compose.sh, lib/detect.sh, lib/config.sh, lib/docker.sh, lib/models.sh, install.sh</files>
  <action>
Present the user with a verification checklist covering all WZRD requirements:

WZRD-01: Wizard simplified to 2 choices (LAN / VDS-VPS)
WZRD-02: Offline profile removed from all files
WZRD-03: build-offline-bundle.sh deleted
WZRD-04: agmind-caddy branch created
WZRD-05: lib/health.sh and lib/detect.sh tracked in git

Run automated verification commands and present results. Then ask the user to approve or report issues.
  </action>
  <verify>
    <automated>cd D:/Agmind/difyowebinstaller && echo "=== WZRD-01 ===" && grep "VDS/VPS" lib/wizard.sh && echo "=== WZRD-02 ===" && offline_count=$(grep -rn "offline" lib/*.sh install.sh 2>/dev/null | wc -l) && echo "Offline refs: $offline_count (expected: 0)" && echo "=== WZRD-03 ===" && test ! -f scripts/build-offline-bundle.sh && echo "Bundle deleted: YES" || echo "Bundle deleted: NO" && echo "=== WZRD-04 ===" && git branch --list agmind-caddy && echo "=== WZRD-05 ===" && git ls-files lib/health.sh lib/detect.sh</automated>
  </verify>
  <acceptance_criteria>
    - User approves all changes
    - `grep -rn "offline" lib/*.sh install.sh | wc -l` returns 0
    - `grep "VDS/VPS" lib/wizard.sh` returns match
    - `git branch --list agmind-caddy` returns the branch
    - `test ! -f scripts/build-offline-bundle.sh` exits 0
    - `git ls-files lib/health.sh lib/detect.sh | wc -l` returns 2
  </acceptance_criteria>
  <done>User verified all 5 WZRD requirements are met. Phase 31 complete.</done>
</task>

</tasks>

<verification>
1. `git branch --list agmind-caddy` shows the branch
2. `grep -rn "offline" lib/*.sh install.sh` returns 0 matches
3. `grep "VDS/VPS" lib/wizard.sh` shows wizard menu item
4. `grep "agmind-caddy" lib/wizard.sh` shows branch fetch code
5. `test ! -f scripts/build-offline-bundle.sh` passes
6. `git ls-files lib/health.sh lib/detect.sh | wc -l` returns 2
7. `bash -n` passes for all modified .sh files
</verification>

<success_criteria>
- Branch agmind-caddy exists locally
- install.sh handles --vds flag
- User has verified all phase 31 changes via checkpoint
- All 5 WZRD requirements addressed: wizard simplified (WZRD-01), offline removed (WZRD-02), bundle deleted (WZRD-03), branch created (WZRD-04), lib files tracked (WZRD-05)
</success_criteria>

<output>
After completion, create `.planning/phases/31-wizard-simplify-caddy-branch/31-02-SUMMARY.md`
</output>
