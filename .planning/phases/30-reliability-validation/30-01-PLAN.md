---
phase: 30-reliability-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - scripts/agmind.sh
  - lib/detect.sh
autonomous: true
requirements: [RLBL-01, RLBL-03]
must_haves:
  truths:
    - "Dify init retry использует интервал 60 секунд между попытками"
    - "Логи retry содержат префикс [dify-init]"
    - "При провале всех retry установка продолжается с warn и инструкцией agmind init-dify"
    - "agmind init-dify защищён flock от параллельного запуска"
    - "install.sh --dry-run запускает preflight_checks и завершается без запуска контейнеров"
    - "preflight_checks проверяет DNS для hub.docker.com и ghcr.io (кроме offline профиля)"
    - "--dry-run exit code 0 при всех PASS, 1 при любом FAIL"
  artifacts:
    - path: "install.sh"
      provides: "Dify init retry 60s + --dry-run arg parsing"
      contains: "sleep 60"
    - path: "scripts/agmind.sh"
      provides: "flock для cmd_init_dify"
      contains: "flock"
    - path: "lib/detect.sh"
      provides: "DNS проверка в preflight_checks"
      contains: "hub.docker.com"
  key_links:
    - from: "install.sh"
      to: "lib/detect.sh"
      via: "preflight_checks() call in --dry-run path"
      pattern: "dry.run.*preflight_checks"
    - from: "install.sh"
      to: "_init_dify_admin"
      via: "retry loop with 60s sleep"
      pattern: "sleep 60"
---

<objective>
Dify init retry с увеличенным интервалом 60 секунд, flock для agmind init-dify, --dry-run preflight с DNS проверкой.

Purpose: Увеличить надёжность инициализации Dify (медленные серверы) и дать оператору возможность проверить совместимость хоста до установки.
Output: Обновлённые install.sh, scripts/agmind.sh, lib/detect.sh
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reliability-validation/30-CONTEXT.md

@install.sh
@scripts/agmind.sh
@lib/detect.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dify init retry 60s + flock для agmind init-dify</name>
  <files>install.sh, scripts/agmind.sh</files>
  <read_first>
    - install.sh (lines 241-333 — _init_dify_admin)
    - install.sh (lines 44-55 — _acquire_lock flock паттерн)
    - scripts/agmind.sh (lines 633-696 — cmd_init_dify)
  </read_first>
  <action>
**install.sh — _init_dify_admin() (lines 241-333):**

1. Изменить `sleep 30` (line 330) на `sleep 60` — интервал между retry.

2. Добавить `[dify-init]` префикс ко всем log_warn/log_info/log_success вызовам внутри _init_dify_admin():
   - line 263: `log_warn "[dify-init] Dify API not ready after 10 min, skipping auto-init"`
   - line 264: `log_warn "[dify-init] Run 'agmind init-dify' manually after API is healthy"`
   - line 270: `log_info "[dify-init] Initializing Dify admin (two-step: init → setup)..."`
   - line 316: `log_info "[dify-init] Dify already initialized"`
   - line 320: `log_success "[dify-init] Dify admin initialized"`
   - line 324: `log_warn "[dify-init] Dify init step failed (attempt ${try}/3): $(echo "$resp" | head -c 200)"`
   - line 326: `log_warn "[dify-init] Dify setup step failed (attempt ${try}/3): $(echo "$resp" | head -c 200)"`
   - line 332: `log_warn "[dify-init] Dify init failed after 3 attempts — run 'agmind init-dify' manually"`

**scripts/agmind.sh — cmd_init_dify() (lines 633-696):**

1. Добавить flock защиту в начало функции cmd_init_dify(), после проверки .env:
```bash
    # Prevent parallel init-dify runs (flock)
    local lock_file="/var/lock/agmind-init-dify.lock"
    if [[ "$(uname)" != "Darwin" ]]; then
        exec 8>"$lock_file"
        if ! flock -n 8; then
            echo -e "${RED}Another init-dify is already running${NC}" >&2
            exit 1
        fi
    fi
```
Вставить после строки 637 (после `fi` блока проверки .env), перед проверкой `.dify_initialized`.

2. НЕ менять retry логику в cmd_init_dify — это CLI команда для ручного запуска, retry остаётся только в install.sh.
  </action>
  <verify>
    <automated>grep -n "sleep 60" install.sh | head -3 && grep -c "\[dify-init\]" install.sh && grep -c "flock" scripts/agmind.sh</automated>
  </verify>
  <acceptance_criteria>
    - install.sh содержит `sleep 60` вместо `sleep 30` в _init_dify_admin
    - install.sh содержит минимум 8 вхождений `[dify-init]` в log_* вызовах
    - scripts/agmind.sh содержит `flock -n 8` в cmd_init_dify
    - scripts/agmind.sh содержит `/var/lock/agmind-init-dify.lock`
    - scripts/agmind.sh содержит `exec 8>` для fd 8
    - Проверка uname != Darwin для совместимости с macOS (mkdir fallback не нужен — CLI не используется на macOS)
  </acceptance_criteria>
  <done>Dify init retry с 60s интервалом и [dify-init] логами; agmind init-dify с flock</done>
</task>

<task type="auto">
  <name>Task 2: --dry-run preflight + DNS проверка</name>
  <files>install.sh, lib/detect.sh</files>
  <read_first>
    - install.sh (lines 576-599 — main() arg parsing)
    - lib/detect.sh (lines 346-556 — preflight_checks)
    - lib/detect.sh (lines 530-541 — internet connectivity check)
  </read_first>
  <action>
**install.sh — main() (lines 576-584):**

1. Добавить `--dry-run` в парсер аргументов (line 580-582). Обновить case блок:
```bash
    while [[ $# -gt 0 ]]; do
        case "$1" in
            --non-interactive) NON_INTERACTIVE=true;;
            --force-restart) FORCE_RESTART=true;;
            --dry-run) DRY_RUN=true;;
            --help|-h) echo "Usage: sudo bash install.sh [--non-interactive] [--force-restart] [--dry-run]"; exit 0;;
        esac; shift
    done
```

2. Добавить early exit после preflight_checks() (около line 612, после вызова preflight_checks).
Найти место вызова preflight_checks в main() и сохранить exit code, затем добавить dry-run проверку:
```bash
    preflight_checks || true
    preflight_rc=$?
    if [[ "${DRY_RUN:-false}" == "true" ]]; then
        log_info "Dry-run complete — exiting without starting containers"
        exit "$preflight_rc"
    fi
```
ВАЖНО: НЕ использовать `local` — main() может быть не функцией. Переменная `preflight_rc` без `local`.
Если preflight_checks вызывается через detect_environment или другую обёртку — найти точный вызов и вставить аналогичную конструкцию после него.

**lib/detect.sh — preflight_checks() (вставить после блока Internet connectivity, перед Summary):**

3. Добавить DNS проверку между блоком 10 (Internet, line 531-541) и Summary (line 543):
```bash
    # --- 11. DNS resolution (skip for offline) ---
    if [[ "${DEPLOY_PROFILE:-}" != "offline" ]]; then
        local dns_ok=true
        for host in hub.docker.com ghcr.io; do
            if getent hosts "$host" >/dev/null 2>&1; then
                echo -e "  ${GREEN}[PASS]${NC} DNS: ${host}"
            elif nslookup "$host" >/dev/null 2>&1; then
                echo -e "  ${GREEN}[PASS]${NC} DNS: ${host}"
            else
                echo -e "  ${RED}[FAIL]${NC} DNS: cannot resolve ${host}"
                errors=$((errors + 1))
                dns_ok=false
            fi
        done
    else
        echo -e "  ${CYAN}[SKIP]${NC} DNS: skipped (offline profile)"
    fi
```
Использовать `getent hosts` как первый вариант (работает без дополнительных пакетов), `nslookup` как fallback.
DNS resolution failure — это FAIL (errors++), не WARN, потому что без DNS невозможно pull образов.

4. Добавить проверку порта 5432 (PostgreSQL) в блок портов (line 493-505). Изменить цикл:
```bash
    for port in 80 443 5432; do
```
Для порта 5432 НЕ проверять agmind_nginx_up (nginx не слушает 5432). Обновить логику:
```bash
    for port in 80 443 5432; do
        if ss -tlnp 2>/dev/null | grep -q ":${port} " || \
           lsof -i ":${port}" -sTCP:LISTEN &>/dev/null 2>&1; then
            if [[ "$agmind_nginx_up" == "true" && "$port" -ne 5432 ]]; then
                echo -e "  ${GREEN}[PASS]${NC} Port ${port}: in use (agmind)"
            else
                echo -e "  ${YELLOW}[WARN]${NC} Port ${port}: in use"
                warnings=$((warnings + 1))
            fi
        else
            echo -e "  ${GREEN}[PASS]${NC} Port ${port}: free"
        fi
    done
```
  </action>
  <verify>
    <automated>grep -n "dry.run\|DRY_RUN\|--dry-run" install.sh | head -10 && grep -n "getent hosts\|hub.docker.com\|ghcr.io" lib/detect.sh | head -10 && grep "5432" lib/detect.sh | head -3</automated>
  </verify>
  <acceptance_criteria>
    - install.sh содержит `--dry-run) DRY_RUN=true;;` в case блоке main()
    - install.sh содержит `DRY_RUN` проверку с early exit после preflight_checks
    - install.sh НЕ использует `local preflight_rc` (используется `preflight_rc=$?` без local)
    - install.sh --help содержит `--dry-run`
    - lib/detect.sh содержит `getent hosts` для DNS проверки
    - lib/detect.sh содержит `hub.docker.com` и `ghcr.io` в DNS проверке
    - lib/detect.sh содержит `[SKIP]` DNS для offline профиля
    - lib/detect.sh содержит порт `5432` в списке проверяемых портов
    - DNS failure использует `[FAIL]` и `errors=$((errors + 1))`, не WARN
    - bash -n install.sh exits 0 (syntax valid)
    - bash -n lib/detect.sh exits 0 (syntax valid)
  </acceptance_criteria>
  <done>--dry-run запускает preflight_checks и выходит; DNS проверка для hub.docker.com и ghcr.io; порт 5432 проверяется</done>
</task>

</tasks>

<verification>
- `grep "sleep 60" install.sh` — retry interval 60s
- `grep -c "\[dify-init\]" install.sh` — минимум 8 вхождений
- `grep "flock" scripts/agmind.sh` — flock в cmd_init_dify
- `grep "DRY_RUN" install.sh` — dry-run логика
- `grep "getent hosts" lib/detect.sh` — DNS проверка
- `bash -n install.sh && bash -n lib/detect.sh && bash -n scripts/agmind.sh` — syntax check
</verification>

<success_criteria>
- Dify init retry 60s с [dify-init] логами работает
- agmind init-dify защищён flock
- install.sh --dry-run проверяет prereqs + DNS и выходит
- DNS failure = FAIL (не WARN)
- Все скрипты проходят bash -n
</success_criteria>

<output>
After completion, create `.planning/phases/30-reliability-validation/30-01-SUMMARY.md`
</output>
