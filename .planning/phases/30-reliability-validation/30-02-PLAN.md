---
phase: 30-reliability-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/compose.sh
  - scripts/update.sh
autonomous: true
requirements: [RELU-04]
must_haves:
  truths:
    - "validate_images_exist() проверяет каждый образ через HTTP HEAD к registry API"
    - "HTTP 404 от registry блокирует установку с сообщением какие образы не найдены"
    - "HTTP 405, timeout, connection error — skip с warn, не блокирует"
    - "Offline профиль (DEPLOY_PROFILE=offline) пропускает валидацию"
    - "SKIP_IMAGE_VALIDATION=true отключает проверку"
    - "validate_images_exist() вызывается внутри compose_pull() в lib/compose.sh до начала pull операции"
    - "validate_images_exist() вызывается перед docker compose pull в update.sh"
    - "Используется HTTP HEAD, не docker manifest inspect и не GET"
  artifacts:
    - path: "lib/compose.sh"
      provides: "validate_images_exist() function"
      contains: "validate_images_exist"
    - path: "scripts/update.sh"
      provides: "pre-pull validation call"
      contains: "validate_images_exist"
  key_links:
    - from: "lib/compose.sh"
      to: "registry API"
      via: "curl HEAD request"
      pattern: "curl.*HEAD"
    - from: "lib/compose.sh"
      to: "compose_pull"
      via: "called before pull"
      pattern: "validate_images_exist"
---

<objective>
HTTP HEAD валидация образов через registry API перед pull — блокирует установку при 404, пропускает при ошибках registry.

Purpose: Предотвратить длительный pull с последующим fail из-за несуществующего тега. HEAD запросы не потребляют Docker Hub rate limit.
Output: validate_images_exist() в lib/compose.sh, интеграция в update.sh
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reliability-validation/30-CONTEXT.md

@lib/compose.sh
@scripts/update.sh
@templates/versions.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper-функции для registry API в lib/compose.sh</name>
  <files>lib/compose.sh</files>
  <read_first>
    - lib/compose.sh (полностью — текущая структура, compose_pull, _validate_pulled_images)
    - templates/versions.env (все образы и registry: docker.io, ghcr.io, quay.io)
  </read_first>
  <action>
**lib/compose.sh — добавить три helper-функции ПЕРЕД compose_pull() (перед line 47):**

```bash
# ============================================================================
# PRE-PULL IMAGE VALIDATION (HTTP HEAD to registry API)
# ============================================================================

# Parse image reference into registry, repo, tag components.
# Examples:
#   nginx:1.25           -> docker.io, library/nginx, 1.25
#   ghcr.io/org/img:v1   -> ghcr.io, org/img, v1
#   quay.io/org/img:v1   -> quay.io, org/img, v1
_parse_image_ref() {
    local image="$1"
    local -n _registry=$2 _repo=$3 _tag=$4

    # Split tag
    if [[ "$image" == *":"* ]]; then
        _tag="${image##*:}"
        image="${image%:*}"
    else
        _tag="latest"
    fi

    # Split registry/repo
    if [[ "$image" == *"/"*"/"* ]]; then
        # Has registry: ghcr.io/org/repo or registry-1.docker.io/library/nginx
        _registry="${image%%/*}"
        _repo="${image#*/}"
    elif [[ "$image" == *"."*"/"* ]]; then
        # Domain-like first segment: gcr.io/repo
        _registry="${image%%/*}"
        _repo="${image#*/}"
    else
        # Docker Hub shorthand: library/nginx or just nginx
        _registry="docker.io"
        if [[ "$image" == *"/"* ]]; then
            _repo="$image"
        else
            _repo="library/${image}"
        fi
    fi
}

# Get anonymous bearer token for registry API.
# Docker Hub requires token from auth.docker.io; others may work without.
_get_registry_token() {
    local registry="$1"
    local repo="$2"

    case "$registry" in
        docker.io|registry-1.docker.io)
            # Docker Hub v2 token
            curl -sf --max-time 5 \
                "https://auth.docker.io/token?service=registry.docker.io&scope=repository:${repo}:pull" \
                2>/dev/null | grep -o '"token":"[^"]*"' | cut -d'"' -f4
            ;;
        ghcr.io)
            # GHCR anonymous token
            curl -sf --max-time 5 \
                "https://ghcr.io/token?scope=repository:${repo}:pull" \
                2>/dev/null | grep -o '"token":"[^"]*"' | cut -d'"' -f4
            ;;
        quay.io)
            # Quay.io — anonymous access without token for public repos
            echo ""
            ;;
        *)
            # Other registries — try without token
            echo ""
            ;;
    esac
}

# Check if a single image:tag exists in its registry via HTTP HEAD.
# Returns: 0 = exists, 1 = not found (404), 2 = registry error (skip)
_check_image_exists() {
    local full_image="$1"

    # Parse image into registry, repo, tag
    local registry repo tag
    _parse_image_ref "$full_image" registry repo tag

    # Get auth token (anonymous)
    local token=""
    token="$(_get_registry_token "$registry" "$repo")" || true

    # HEAD request to manifest endpoint
    local url
    case "$registry" in
        docker.io|registry-1.docker.io)
            url="https://registry-1.docker.io/v2/${repo}/manifests/${tag}"
            ;;
        *)
            url="https://${registry}/v2/${repo}/manifests/${tag}"
            ;;
    esac

    local http_code
    local auth_header=""
    [[ -n "$token" ]] && auth_header="Authorization: Bearer ${token}"

    http_code="$(curl -s -o /dev/null -w '%{http_code}' \
        --max-time 10 \
        -I \
        ${auth_header:+-H "$auth_header"} \
        -H "Accept: application/vnd.docker.distribution.manifest.v2+json" \
        -H "Accept: application/vnd.oci.image.manifest.v1+json" \
        "$url" 2>/dev/null)" || { return 2; }

    case "$http_code" in
        200|301|302) return 0 ;;   # exists (or redirect = exists)
        404)         return 1 ;;   # not found
        *)           return 2 ;;   # registry error (401, 403, 405, 429, 500, etc.)
    esac
}
```
  </action>
  <verify>
    <automated>grep -c "_parse_image_ref\|_get_registry_token\|_check_image_exists" lib/compose.sh && grep -c "curl.*-I" lib/compose.sh && bash -n lib/compose.sh</automated>
  </verify>
  <acceptance_criteria>
    - lib/compose.sh содержит `_parse_image_ref()` для docker.io, ghcr.io, quay.io
    - lib/compose.sh содержит `_get_registry_token()` с anonymous token для Docker Hub и GHCR
    - lib/compose.sh содержит `_check_image_exists()` с HTTP HEAD (curl -I)
    - HTTP 404 возвращает rc=1 (not found), HTTP 405/timeout возвращает rc=2 (skip)
    - bash -n lib/compose.sh exits 0
  </acceptance_criteria>
  <done>Три helper-функции для registry API добавлены в lib/compose.sh</done>
</task>

<task type="auto">
  <name>Task 2: validate_images_exist() + интеграция в compose_pull() и update.sh</name>
  <files>lib/compose.sh, scripts/update.sh</files>
  <read_first>
    - lib/compose.sh (Task 1 helpers — _parse_image_ref, _get_registry_token, _check_image_exists)
    - lib/compose.sh (lines 47-65 — compose_pull)
    - scripts/update.sh (lines 609-620 — docker compose pull в update)
    - scripts/update.sh (lines 725-733 — pull в restart секции)
  </read_first>
  <action>
**lib/compose.sh — добавить validate_images_exist() ПОСЛЕ трёх helper-функций (из Task 1), ПЕРЕД compose_pull():**

```bash
# Validate that all required images exist in their registries before pulling.
# Uses HTTP HEAD (not GET) to avoid Docker Hub rate-limit consumption.
# Returns: 0 = all found, 1 = some not found (blocks install)
validate_images_exist() {
    local docker_dir="${INSTALL_DIR}/docker"

    # Skip for offline profile
    if [[ "${DEPLOY_PROFILE:-}" == "offline" ]]; then
        log_info "Offline profile: skipping image validation"
        return 0
    fi

    # Skip if explicitly disabled
    if [[ "${SKIP_IMAGE_VALIDATION:-false}" == "true" ]]; then
        log_info "Image validation skipped (SKIP_IMAGE_VALIDATION=true)"
        return 0
    fi

    cd "$docker_dir"
    build_compose_profiles
    local profiles="$COMPOSE_PROFILE_STRING"

    local images_raw
    if [[ -n "$profiles" ]]; then
        images_raw="$(COMPOSE_PROFILES="$profiles" docker compose config --images 2>/dev/null)" || return 0
    else
        images_raw="$(docker compose config --images 2>/dev/null)" || return 0
    fi
    [[ -z "$images_raw" ]] && return 0

    local -a images
    mapfile -t images <<< "$images_raw"
    local total=${#images[@]}
    [[ $total -eq 0 ]] && return 0

    log_info "Validating ${total} images exist in registries..."

    local not_found=0
    local -a missing_images=()

    for img in "${images[@]}"; do
        if _check_image_exists "$img"; then
            : # exists, ok
        else
            local rc=$?
            if [[ $rc -eq 2 ]]; then
                # Registry error (405, timeout, etc.) — skip with warn
                log_warn "Cannot verify image: ${img} — skipping check"
            else
                # 404 — image not found
                log_error "WARNING: image not found: ${img}"
                not_found=$((not_found + 1))
                missing_images+=("$img")
            fi
        fi
    done

    if [[ $not_found -gt 0 ]]; then
        log_error "Blocking install: ${not_found} image(s) not found in registries:"
        for m in "${missing_images[@]}"; do
            log_error "  - ${m}"
        done
        log_error "Check tags in versions.env or set SKIP_IMAGE_VALIDATION=true to bypass"
        return 1
    fi

    log_info "All ${total} images verified"
    return 0
}
```

**Интеграция в compose_pull() (line 47-65):**

Добавить вызов validate_images_exist() в начало compose_pull(), ПОСЛЕ проверки offline профиля (line 54-57), ПЕРЕД bind mount safety (line 59-63):

```bash
    # Pre-pull: validate images exist in registries (HTTP HEAD)
    validate_images_exist || {
        log_error "Image validation failed — aborting pull"
        return 1
    }
```

**scripts/update.sh:**

1. Убедиться что `source "${SCRIPT_DIR}/../lib/compose.sh"` есть в начале файла (или что lib/compose.sh уже sourced). Если нет — добавить source:
```bash
# shellcheck source=../lib/compose.sh
source "${SCRIPT_DIR}/../lib/compose.sh" 2>/dev/null || source "$(dirname "$0")/../lib/compose.sh"
```

2. Добавить вызов validate_images_exist() ОДИН РАЗ перед циклом обновления сервисов. Найти точку входа в обновление образов и добавить перед циклом:
```bash
    # Validate images exist before pulling (HTTP HEAD)
    if [[ "${SKIP_IMAGE_VALIDATION:-false}" != "true" ]]; then
        validate_images_exist || {
            log_warn "Some images may not exist — proceeding with caution"
        }
    fi
```

ВАЖНО: В update.sh валидация НЕ блокирует (warn, не error) — при update пользователь может иметь свои образы.
  </action>
  <verify>
    <automated>grep -c "validate_images_exist" lib/compose.sh && grep -c "validate_images_exist" scripts/update.sh && bash -n lib/compose.sh && bash -n scripts/update.sh</automated>
  </verify>
  <acceptance_criteria>
    - lib/compose.sh содержит функцию `validate_images_exist()`
    - validate_images_exist() вызывается в compose_pull() перед _pull_with_progress
    - DEPLOY_PROFILE=offline пропускает валидацию
    - SKIP_IMAGE_VALIDATION=true пропускает валидацию
    - scripts/update.sh содержит вызов `validate_images_exist` перед pull
    - scripts/update.sh source'ит lib/compose.sh (прямо или через другой lib)
    - Валидация в update.sh НЕ блокирует (log_warn, не return 1)
    - bash -n lib/compose.sh exits 0
    - bash -n scripts/update.sh exits 0
  </acceptance_criteria>
  <done>validate_images_exist() проверяет все образы, блокирует install при 404, warn-only в update.sh</done>
</task>

</tasks>

<verification>
- `grep -c "validate_images_exist" lib/compose.sh` — функция + вызов
- `grep "curl.*-I" lib/compose.sh` — HTTP HEAD
- `grep "validate_images_exist" scripts/update.sh` — интеграция в update
- `bash -n lib/compose.sh && bash -n scripts/update.sh` — syntax check
</verification>

<success_criteria>
- validate_images_exist() проверяет все образы через HTTP HEAD к registry API
- 404 = блокирует install, 405/timeout = skip с warn
- Offline и SKIP_IMAGE_VALIDATION пропускают
- Интегрировано в compose_pull() и update.sh
- Все скрипты проходят bash -n
</success_criteria>

<output>
After completion, create `.planning/phases/30-reliability-validation/30-02-SUMMARY.md`
</output>
