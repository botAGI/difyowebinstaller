---
phase: 30-reliability-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-offline-bundle.sh
autonomous: true
requirements: [RLBL-02]
must_haves:
  truths:
    - "build-offline-bundle.sh в конце сборки сравнивает образы в bundle с docker compose config --images"
    - "Missing образы выводятся как ошибки"
    - "Extra образы (в bundle, но не в compose) выводятся как warnings"
    - "Manifest с размерами каждого образа печатается в stdout"
    - "INCLUDE_DOCLING_CUDA=true добавляет CUDA-образ в ожидаемый список"
  artifacts:
    - path: "scripts/build-offline-bundle.sh"
      provides: "Bundle verification + manifest"
      contains: "Bundle Verification"
  key_links:
    - from: "scripts/build-offline-bundle.sh"
      to: "docker compose config --images"
      via: "expected image list"
      pattern: "docker compose config --images"
---

<objective>
Встроенная валидация состава offline bundle при сборке: проверка что все нужные образы включены, manifest с размерами.

Purpose: Предотвратить неполный bundle — оператор узнаёт о пропущенных образах сразу при сборке, а не при установке на airgapped хосте.
Output: Обновлённый scripts/build-offline-bundle.sh с verification этапом
</objective>

<execution_context>
@C:/Users/Gamer161/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gamer161/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-reliability-validation/30-CONTEXT.md

@scripts/build-offline-bundle.sh
@templates/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle verification + manifest в build-offline-bundle.sh</name>
  <files>scripts/build-offline-bundle.sh</files>
  <read_first>
    - scripts/build-offline-bundle.sh (полностью — текущая структура, Stage 1-5, image export)
    - templates/docker-compose.yml (profiles: tei, reranker, docling, etc.)
  </read_first>
  <action>
**scripts/build-offline-bundle.sh — добавить Stage 6 ПОСЛЕ Stage 5 (после line 302, перед завершением скрипта):**

```bash
# ──────────────────────────────────────────
# Stage 6: Bundle Verification
# ──────────────────────────────────────────
echo ""
echo -e "${BOLD}Stage 6: Bundle Verification${NC}"

# Get expected images from all compose profiles
COMPOSE_FILE="${SCRIPT_DIR}/templates/docker-compose.yml"
expected_images=""
# Collect images from all profiles (no profile = default services)
expected_images="$(docker compose -f "$COMPOSE_FILE" config --images 2>/dev/null | sort -u)"
# Also get images from all profiles (tei, reranker, docling)
for profile in tei reranker docling; do
    local_imgs="$(COMPOSE_PROFILES="$profile" docker compose -f "$COMPOSE_FILE" config --images 2>/dev/null | sort -u)" || true
    if [[ -n "$local_imgs" ]]; then
        expected_images="$(echo -e "${expected_images}\n${local_imgs}" | sort -u)"
    fi
done

# Add Docling CPU image (always expected in bundle)
DOCLING_IMAGE_CPU="${DOCLING_IMAGE_CPU:-ghcr.io/docling-project/docling-serve:v1.14.3}"
expected_images="$(echo -e "${expected_images}\n${DOCLING_IMAGE_CPU}" | sort -u)"

# Add Docling CUDA image if flag set
if [[ "${INCLUDE_DOCLING_CUDA}" == "true" ]]; then
    DOCLING_IMAGE_CUDA="${DOCLING_IMAGE_CUDA:-quay.io/docling-project/docling-serve-cu128:v1.14.3}"
    expected_images="$(echo -e "${expected_images}\n${DOCLING_IMAGE_CUDA}" | sort -u)"
fi

# Get actual images saved in bundle (from docker-images.tar.gz)
# We already have the images loaded locally from Stage 2, so check docker image inspect
actual_images=""
if [[ -f "${STAGING}/docker-images.tar.gz" ]]; then
    # List images in tar by loading manifest
    actual_images="$(tar -xzf "${STAGING}/docker-images.tar.gz" manifest.json -O 2>/dev/null \
        | grep -o '"RepoTags":\["[^]]*"\]' \
        | grep -o '[^"[\]]*:[^"[\]]*' \
        | sort -u)" || true
fi

# Fallback: check locally available images (they were pulled in Stage 2)
if [[ -z "$actual_images" ]]; then
    actual_images=""
    while IFS= read -r img; do
        [[ -z "$img" ]] && continue
        if docker image inspect "$img" >/dev/null 2>&1; then
            actual_images="$(echo -e "${actual_images}\n${img}")"
        fi
    done <<< "$expected_images"
    actual_images="$(echo "$actual_images" | sort -u | sed '/^$/d')"
fi

# Compare expected vs actual
local missing=0 extra=0
echo ""
echo -e "  ${BOLD}Expected vs Bundle:${NC}"

while IFS= read -r img; do
    [[ -z "$img" ]] && continue
    if echo "$actual_images" | grep -qF "$img"; then
        echo -e "  ${GREEN}[OK]${NC}      ${img}"
    else
        echo -e "  ${RED}[MISSING]${NC} ${img}"
        missing=$((missing + 1))
    fi
done <<< "$expected_images"

# Check for extra images (in bundle but not expected)
while IFS= read -r img; do
    [[ -z "$img" ]] && continue
    if ! echo "$expected_images" | grep -qF "$img"; then
        echo -e "  ${YELLOW}[EXTRA]${NC}   ${img}"
        extra=$((extra + 1))
    fi
done <<< "$actual_images"

# Image manifest with sizes
echo ""
echo -e "  ${BOLD}Image Manifest:${NC}"
echo ""
printf "  %-60s %s\n" "IMAGE" "SIZE"
printf "  %-60s %s\n" "$(printf '%.0s─' {1..60})" "──────"
while IFS= read -r img; do
    [[ -z "$img" ]] && continue
    local img_size
    img_size="$(docker image inspect "$img" --format='{{.Size}}' 2>/dev/null || echo "0")"
    # Convert bytes to human-readable
    if [[ "$img_size" -gt 1073741824 ]] 2>/dev/null; then
        img_size="$(echo "scale=1; $img_size / 1073741824" | bc 2>/dev/null || echo "?")GB"
    elif [[ "$img_size" -gt 1048576 ]] 2>/dev/null; then
        img_size="$(echo "scale=0; $img_size / 1048576" | bc 2>/dev/null || echo "?")MB"
    else
        img_size="${img_size}B"
    fi
    printf "  %-60s %s\n" "$img" "$img_size"
done <<< "$expected_images"

# Summary
echo ""
if [[ $missing -gt 0 ]]; then
    echo -e "  ${RED}FAIL: ${missing} image(s) missing from bundle!${NC}"
    echo -e "  ${RED}This bundle will fail on air-gapped installation.${NC}"
    exit 1
elif [[ $extra -gt 0 ]]; then
    echo -e "  ${YELLOW}WARN: ${extra} extra image(s) in bundle (not required by compose)${NC}"
    echo -e "  ${GREEN}All required images present.${NC}"
else
    echo -e "  ${GREEN}All required images present. Bundle is complete.${NC}"
fi
```

ВАЖНО: Этот блок должен быть ПЕРЕД финальным выводом "Offline bundle created!" (lines 287-302), чтобы при missing images скрипт завершился с exit 1 до вывода успешного сообщения.

Переместить Stage 6 ПОСЛЕ tar создания (line 282) но ПЕРЕД финальным echo блоком (line 286+). Если missing > 0, скрипт завершается с exit 1 и bundle считается невалидным.

Учесть, что внутри скрипта нет `local` на верхнем уровне (это не функция). Заменить `local missing=0 extra=0` и `local img_size` на обычные переменные без local:
```bash
missing=0
extra=0
```
И вместо `local img_size`:
```bash
img_size="..."
```

Также `local_imgs` заменить на `_profile_imgs`:
```bash
_profile_imgs="$(COMPOSE_PROFILES="$profile" ...)"
```
  </action>
  <verify>
    <automated>grep -c "Bundle Verification" scripts/build-offline-bundle.sh && grep -c "MISSING\|EXTRA" scripts/build-offline-bundle.sh && grep -c "Image Manifest" scripts/build-offline-bundle.sh && grep "INCLUDE_DOCLING_CUDA" scripts/build-offline-bundle.sh | head -5 && bash -n scripts/build-offline-bundle.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-offline-bundle.sh содержит "Stage 6: Bundle Verification"
    - Скрипт содержит `[MISSING]` для отсутствующих образов
    - Скрипт содержит `[EXTRA]` для лишних образов
    - Скрипт содержит `[OK]` для найденных образов
    - Скрипт содержит "Image Manifest" с таблицей размеров
    - При INCLUDE_DOCLING_CUDA=true CUDA-образ добавляется в expected список
    - При missing > 0 скрипт завершается с exit 1
    - Нет `local` переменных на верхнем уровне скрипта (не в функции)
    - bash -n scripts/build-offline-bundle.sh exits 0
  </acceptance_criteria>
  <done>Bundle verification проверяет все образы, показывает manifest с размерами, exit 1 при missing</done>
</task>

</tasks>

<verification>
- `grep "Bundle Verification" scripts/build-offline-bundle.sh` — Stage 6 существует
- `grep "MISSING" scripts/build-offline-bundle.sh` — missing check
- `grep "Image Manifest" scripts/build-offline-bundle.sh` — manifest
- `bash -n scripts/build-offline-bundle.sh` — syntax check
</verification>

<success_criteria>
- Bundle verification сравнивает образы в tar.gz с docker compose config --images
- Missing = error + exit 1, Extra = warning
- Manifest с размерами каждого образа
- INCLUDE_DOCLING_CUDA=true добавляет CUDA-образ
- Скрипт проходит bash -n
</success_criteria>

<output>
After completion, create `.planning/phases/30-reliability-validation/30-03-SUMMARY.md`
</output>
