---
phase: quick-260318-e1f
plan: 01
mode: quick
---

# Quick Task: Fix remaining bugs — atomic sed, BUG-015, restart policies, plugin pool, logrotate

## Task 1: Convert remaining sed -i.bak in config.sh to atomic writes

**files:** lib/config.sh
**action:** Replace all 18 remaining `sed -i.bak ... file; rm -f file.bak` patterns with `sed ... file > file.tmp.$$ && mv file.tmp.$$ file` pattern. Functions: generate_nginx_config (5 sed calls), enable_gpu_compose (7 sed calls), configure_alertmanager (2 sed calls), generate_sandbox_config (1 sed call), enable_authelia_nginx (1 sed call), handle TLS cert paths (1 sed call).
**verify:** `grep -c 'sed -i' lib/config.sh` returns 0
**done:** Zero sed -i.bak patterns remain

## Task 2: Fix BUG-015 workaround in install.sh

**files:** install.sh
**action:** Replace `docker compose up -d || true` in the retry loop with proper error checking. Log retry status. After loop exits, verify no containers stuck in "created" state — if any remain, exit with clear error instead of silent continuation.
**verify:** No `|| true` on compose up in retry loop; explicit failure exit after exhausted retries
**done:** BUG-015 retry loop fails loudly instead of silently

## Task 3: Add restart limits for monitoring services

**files:** templates/docker-compose.yml
**action:** Change monitoring services (prometheus, alertmanager, loki, promtail, node-exporter, cadvisor, grafana, portainer) from `restart: always` to `restart: on-failure` with appropriate limits. This prevents OOM restart loops while still recovering from transient failures.
**verify:** `grep -A1 'restart:' templates/docker-compose.yml` shows on-failure for monitoring services
**done:** Monitoring services won't restart-loop on OOM

## Task 4: Cap plugin_daemon ROUTINE_POOL_SIZE

**files:** templates/docker-compose.yml
**action:** Set `ROUTINE_POOL_SIZE` default to 10 (not 100) to match 2g mem_limit. 10 concurrent plugin workers × ~100MB each = ~1GB peak, fitting within 2g limit.
**verify:** ROUTINE_POOL_SIZE defaults to 10 in compose
**done:** Plugin workers can't exceed mem_limit

## Task 5: Add logrotate config for cron logs

**files:** templates/logrotate-agmind.conf (new), install.sh
**action:** Create logrotate config for /var/log/agmind-*.log and INSTALL_DIR/*.log. Install it in phase_config() alongside cron job setup.
**verify:** logrotate config exists and is installed
**done:** Cron logs won't fill /var partition
